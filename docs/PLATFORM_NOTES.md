# Cross‑platform notes
- XDG paths respected via $XDG_* vars; fallback to ~/.config, ~/.local/share, ~/.local/state
- Windows: prefer Git Bash or WSL; map data under %LocalAppData% equivalent; use `where code` to detect VS Code
- Homebrew alternatives: winget/choco for native codex if available; otherwise manual install