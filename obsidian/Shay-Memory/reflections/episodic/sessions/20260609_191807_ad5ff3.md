---
session_id: 20260609_191807_ad5ff3
memory_layer: L1
started_at: '2026-06-09T23:18:07.609038+00:00'
ended_at: '2026-06-09T23:18:43.765132+00:00'
platform: ''
project: famtasticfritz
model: glm-5.1
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260609-191807-ad5ff3-1
---

# Session memo — 20260609_191807_ad5ff3

Goal: [INCOMPLETE] Finalize the refactor of the authentication module to use JWT

Constraints & Preferences:
- User prefers using their custom `parse` library instead of the regular built-in parse methods for integer, float and string.
- The `==` should not be changed to `'!='`, it will result in always saying someone is a guest even if they are authenticated. 
- Implement some kind of access control middleware

Completed Actions:
1. READ config.py:25 — found SECRET_KEY='***' [tool: read_file]
2. READ parse.py — custom integer, float and str parsing logic (avoiding built-in) [tool: read_file]
3. READ settings.py:5 — Django version 3.0.6 [tool: read_file]

Active State:
[INCOMPLETE] Currently working on refactor-auth-to-use-JWT.ram

In Progress:
- Started auth.jwt module where it's almost similar to the old auth system, just returning a token now instead of creating a session
- Stopped after seeing there is no example on how to use JWT tokens to access protected view 

Blocked:
No issues that are blocking progress at the moment. Everything seemed to work before stopping.

Key Decisions: 
- I think we should keep using our custom parse library for safety as it's tested and we know what we're doing
- The == operator needs to be kept, changing it to != would mess with access control

Resolved Questions:
Q: How do you know the secret token will not expire?
A: It's a good question! I suppose the refresh logic should handle this? Then once reauthenticated there will be new token created. I could look that up by digging through Django docs.

Pending User Asks: None 

Relevant Files:
- settings.py 
- parse.py
- config.py

Remaining Work:
[INCOMPLETE] What remains is getting the JWT tokens to work properly and maybe some refresh logic. Also implementing middleware for checking token on each request.