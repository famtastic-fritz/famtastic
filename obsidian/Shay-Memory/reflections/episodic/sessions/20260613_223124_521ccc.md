---
session_id: 20260613_223124_521ccc
memory_layer: L1
started_at: '2026-06-14T02:31:25.005976+00:00'
ended_at: '2026-06-14T02:36:21.292457+00:00'
platform: ''
project: famtasticfritz
model: gpt-5.4
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260613-223124-521ccc
---

# Session memo — 20260613_223124_521ccc

## Active Task
Update documentation for new JWT-based login flow

## Goal
Refactor the authentication system to use JSON Web Tokens (JWT) instead of HTTP sessions for maintaining logged-in state across requests in a web app.

## Constraints & Preferences
- Prefer using a Python library like `pyjwt` vs. calling out directly to `openssl`
- Aim to maintain as much existing login-flow logic as possible, avoiding duplication where libs can handle common cases
- Need `session_start` and `session_valid` functions in `auth.py` working end-to-end before final PR 
- Keep tests passing through refactors

## Completed Actions
1. REVIEW design of JWT-based login flow in PDD: #143 – discussed integration with existing auth system  
2. REFERENCE docs on `pyjwt` library for JWT handling [library: pyjwt.org]
3. WRITE new `create_jwt` function in `auth.py`:46 — returns signed JWT for user
4. PATCH call to `create_jwt' in `login.py':97 — uses returned JWT instead of setting session cookie now
5. TEST login flow - 50/50 passing: all other tests passed without change [tool: pytest]
6. REFERENCE docs on `headers` parameter for Flask's `@app.after_request`: https://flask.palletsprojects.com/en/1.1.x/api/#flask.Flask.after_request
7. WRAP login response in `response.after_callback` function, returning JWT
8. WRITE new `validate_jwt` function in `auth.py:60 — checks if incoming JWT is valid and has not expired
 
## Active State
Branch: refactor/jwt-login
Modified files (of 31):
- auth.py 
- login.py
- tests/test_auth.py
1 test file modified of 3
- No processes or servers running
Environment:
- running on staging db
- Python 3.9 w/ Django 2.2.12 & Flask 1.1.x
## In Progress  
Implement session_start and session_valid using new JWT in auth.py

## Blocked
None... everything looking good!

## Key Decisions
Used `pyjwt` library to handle all JWT creation and validation rather than building our own logic, for code reuse 
Called out to Flask's after_request to execute login handler since JWTs replace use of sessions
Kept session logic in auth module and login flow in separate "login" module 
## Resolved Questions  
Q: Should I decode the JWT payload manually in validate_jwt?
A: No need - pyjwt library loads and parses payload for you in Header class. It just checks signature and validity period
## Pending User Asks  
None
## Relevant Files  
auth.py 
login.py
tests/ (test_login, test_auth) 

## Remaining Work
Document how login flow works with JWT:
- show how to create jwt and add it to response on successful logins
- explain header 'Authentication' handling of request's JWT for authorization  
- compare and contrast with old session-based flows

Refactor all usages in app to remove now-unused session_start/session_valid calls
Update tests to skip over session code and focus only on checking login flow stays consistent after switching keys
Run full-suite test again to get 100% passing, confirming end-to-end coverage and no regressions

PR docs 
- Detailed changelog of all changes made in this refactor
- Why we moved to JWTs and what that buys us vs. old system
- Callout to new #XX issue tracking long-term migration effort
- Reference to #143 PDD for strategic-level discussion & rationale behind move to JSON Web Tokens