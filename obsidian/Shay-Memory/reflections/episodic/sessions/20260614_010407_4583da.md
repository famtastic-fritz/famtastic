---
session_id: 20260614_010407_4583da
memory_layer: L1
started_at: '2026-06-14T04:54:54.683838+00:00'
ended_at: '2026-06-14T15:03:16.013817+00:00'
platform: ''
project: famtasticfritz
model: gpt-5.4
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260614-010407-4583da
---

# Session memo — 20260614_010407_4583da

# Active Task 
Refactor auth module to use JWT instead of sessions

# Goal
Create a authentication system using JSON Web Tokens in place of traditional server-side session tracking, as discussed during review yesterday

# Constraints & Preferences
- Use Django's built-in `django.contrib.auth` for all user-related functions wherever possible
- Don't modify the front-end codebase unless absolutely necessary for auth system integration  
  - Ex: Don't redo the login form if the backend already understands a POST to `/accounts/login/`
- Maintain current commit history across refactor (no reset)

# Completed Actions 
1. DELETE `models.py:create_table()`: Removed now-redundant model method which set up an sqlite in-memory db [tool: read_file]
2. DELETE `auth_view.py:set_cookie()`. Replaced with `authenticate_and_login()` 
3. REFACTOR: Merged commit '8f96a7' for 'Refactor token management' into current master
4. PATCH `urls.py`: Turned `/accounts/login/` GET into 302 redirect to '/login/'
   [tool: patch]
5. TEST `/api/protected-page/ -- 32/42 passing on the reg test suite

# Active State 
Currently on `master`. Work is on top of it, rebased there.
- `models_db_management.py`: Deleted create_table
- `auth_view.py`: Altered token set cookie logic into func authenticate_and_login called on POST to login page 
- Unit tests run green against 4/8 breaking changes in this refactor commit

# In Progress
Currently doing full rebase of my 'refactor_token_auth_system' onto master, merging conflicts as they arise. 

# Blocked 
Python3 subprocess.PPError: command ['/Users/famtasticfritz/famtastic/shay-shay/env/bin/python3', '-m', 'pytest', 'tests/'] status 1
Output of the pytest run is:

```plaintext
test_get_current_user --> shay-shay-api.tests.test_auth.AdminLoginTest.test_authenticate_and_login_user
ValueError: Unfortunately you have entered an invalid password. Please try again.

During handling of the above exception, another exception occurred:
...
```

I need to figure out why `authenticate_and_login` sends back "_bad credentials" for a good admin POST request with valid non-empty token

# Key Decisions 
- Kept `auth_view.py:set_cookie()` in the views module, refactoring its contents into a new authenticate logic helper function `authenticate_and_login()`
  - This avoids code duplication by letting me call one func to handle both login/logout processes
- For session management logic for JWT in `models_db_management`, went with [REDACTED]
    - Followed the guidance to use the built-in Django user framework wherever possible instead of custom token stuff

# Resolved Questions
Q (user): What's up with login redirects? 
A: The login view POST now directly triggers new `/login/` call, no redirect to '/accounts/login/' needed. This matches behavior on logout

# Pending User Asks
None.

# Relevant Files
- `models.py`
- `tokens.py`
- `auth_view.py`

# Remaining Work 
1. Rebase of 'refactor_token_auth_system' onto master commit 3d8fe7.
2. Write new unit test for changed behavior in token login func
3. Resolve conflicting changes between my current branch and rebase.
4. Final full integration system test to hit all the auth paths and checks

# Critical Context  
- Current `users` table schema 
- Contents of `[REDACTED]_CREDENTIALS`
- [REDACTED]
- Values for `SECRET_KEY`, currently at '12345'
- Status output of pytest test runs