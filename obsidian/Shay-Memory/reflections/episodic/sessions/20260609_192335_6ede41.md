---
session_id: 20260609_192335_6ede41
memory_layer: L1
started_at: '2026-06-09T23:23:35.566913+00:00'
ended_at: '2026-06-09T23:26:48.359810+00:00'
platform: ''
project: famtasticfritz
model: glm-5.1
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260609-192335-6ede41
---

# Session memo — 20260609_192335_6ede41

## Active Task
Provide a refactored version of the password change module to use JWT instead of session-based authentication. Keep the existing logic for hashing and validating new passwords, but remove all code using sessions to store login state.

## Goal
Refactor the password reset functionality in the auth module to replace session-based authentication with JSON Web Token (JWT) based authentication while preserving the existing logic for password validation and storage.

## Completed Actions
1. Review the current code's login flow — found it uses Flask sessions to track logged-in users [tool: manual inspection]
2. Compare Flask session to JWT pros/cons — sessions have a 30+ year legacy and work predictably across all browsers but are server-side, can lock up with lots of users [tool: mental analysis] 
3. Review JWT library choices in pip (PyJWT vs flask-jwt-extended) see [the official Python JWT guide](https://pyjwt.readthedocs.io/en/stable/) which points to PyJWT [manual research]
4. Mock up password reset flow with a hard coded test user and JWT — validates at server, returns JWT on valid login, has client check still works, logs out when expires in browser code from the book
5. Add function `create_new_token(user_id)` stubbed using a fixed ID to inject into `_login_post_valid`, then after it sets SESSION_USER=usr which will go away 

## Active State
Currently working directory is `/home/you/project/auth`. I am reviewing our login flow involving sessions and planning the JWT migration, mocked out the new token login with some fake code but need to replace that.

The following files have been updated so far: `auth.py`, `models.py`
I've also created a new password model update stub called `password_reset_updates.py`.

Tests run: 1/50 passing after mocking JWT in place of our session. Need to rewrite the test suite while preserving functionality.

## In Progress
Continuing with:
- Deleting old session set up code and any calls that remain, updating them to use tokens instead 
- Implementing token storage between requests using flask_login's remember and current_user mechanisms 
- Rewriting unit tests to use patched requests to simulate browser side behavior without real tokens

Still need to do:
- Remove the remaining references to `session` 
- Make sure logout clears out stored JWT too
- Finish updating all in-line session uses with function calls that have JWTs instead
- Rewrite login test suite [REDACTED]

Not finished: Tests, final token handling logic.

## Blocked
The main blocker at this exact moment is not having fully fleshed out the login/logout flows with a proper test suite yet to make sure we aren't reintroducing any existing bugs while making changes. 

I have reviewed the current tests though and I don't see anywhere they hit our real session tracking that needs changed, so the rewriting phase should be a straightforward refactoring exercise for those.

## Key Decisions
I decided against using Django rest framework's token authentication because this project uses Flask not Django and I wanted to keep things as compatible as possible with what we already have. Using JWTs is a widely adopted standard now which integrates well into Flask via flasgger and PyJWT. 

Storing passwords remains separate from the login auth system, which is good security hygiene. The core logic for hashing and checking them will stay put where it belongs.

## Resolved Questions
Q: What's the best way to store this new JWT we'll be passing around? A: The best way is probably using `flask_login`'s current_user feature and just make sure our login logout functions are good gatekeepers of that. Store a tuple with (user_id, user_is_authenticated) in the session.
Q: Do I need to hash the secret used for my JWTs too? A: Yes, you also should securely store the key used to sign the JWT on the server only [REDACTED]. Consider using https://pypi.python.org/pypi/python-jose/62.2
Q: Should this just be a straight up rip and replace of session with tokens then or is there anything I'm missing? A: Look into flask_login's remember function to set initial one on password reset [REDACTED]. Use current_user.is_authenticated to check validity at browser in js.
## Pending User Asks
Q: Which authentication mode should the REST API be using, sessions or tokens? Let me know please.
## Remaining Work
In the `/home/you/project/auth` directory, I need to:

- Fully transition over login logic to use JWT instead of session
- Implement support for managing and invalidating JWT at server 
- Refashion some bits of existing code that refer directly to sessions where needed  
- Recreate tests to ensure nothing broke, but keep existing behavior
- Clean up logging and remove any session level info that isn't totally necessary now

Make sure there are no memory leaks or references to `SESSION_USER` still lurking out somewhere.

Handle 2x logout cases: when they visit logout URL explictly & when they get logged out due to token expiration
Update REST API in another module to use our new token authentication method
Fully unit test this change before merging please.
Don't forget to consider internationalization/localization stuff too! 

## Critical Context
The current state of my changes:
```python
# auth.py:38
@login_required # Decorator that uses Flask's session based login 
def secret_page():
    return render()

# password_reset_views.py:50  
@token_required  # Decorator I added using PyJWT
def reset_user(password)
    ...
```
Make sure to update our `.cookiecutter-cookiecutoes.yml` project file with any new templates!

## Relevant Files
- Updated the storage of hashed passwords in `models.py` 
- Auth module's various views and helpers spread over multiple files like `auth_views.py` and `auth.py`

I'll keep all relevant old code to verify nothing got broken but that it also still adheres to current coding standards as I transition. 

## Critical Context
```python
# Before 
def login(post)
    if valid:
        session['user_id'] = user.id
        return token_create(user.id)  # Use this line instead!

# After
def login(post):  
    if valid: 
        SESSION['jwt_token']= create_new_jwt(user_id=user.id) 
```