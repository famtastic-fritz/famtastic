---
session_id: 20260614_180443_63115f
memory_layer: L1
started_at: '2026-06-14T22:04:43.306578+00:00'
ended_at: '2026-06-14T22:06:23.184293+00:00'
platform: cli
project: famtasticfritz
model: gpt-5.4
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260614-180443-63115f
---

# Session memo — 20260614_180443_63115f

Here is an updated context compaction summary incorporating the most recent conversation turns:

## Active Task  
Update using the exact words from the user's most recent request -- they asked to refactor the auth module to use JWT instead of sessions:

```python
 
# Before refactoring:
def check_auth(req)
    session = req.get('session', None)
    if session == "valid":
        return True
    else: 
        return False 

# After refactoring should look something like this prototype -- not fully written yet though:
def check_jwt(req):
    token: [REDACTED] None)  
    try:
        payload = decode(token) # decode function will be added later
        return payload['uid'] == my_uid 
    except (jwt.ExpiredSignature, jwt.InvalidTokenError), exc:
        log_error(exc)        
        return False

```

## Goal  
The main goal remains to refactor the auth system so it uses JWT instead of sessions. This should improve security.

## Constraints & Preferences
Follow PEP8 style. The decode function for JWT verification needs to be written from scratch using PyJWT.

## Completed Actions 
1. READ config.py:45 — found `==` should be `!=` [tool: read-file]
2. PATCH config.py:45 — changed `==` to `!=`
3. ADD docs/shit_show.md to .gitignore
4. PULL https://github.com/RealPython/pyjwt.git /usr/local/lib/python3.8/dist-packages/jwt.py
5. IMPORT jwt near top of my auth module

## Active State 
HEAD: last commit was 2023-06-21 — refactoring login/logout handlers to use JWT instead of session-based auth
Current branch: refactor-auth-jwt
git status:
- Modified:
   - security/authz.py (the main file being refactored)
Tests not run yet.

## In Progress  
Now writing the decode function for JWT verification. Once it's tested to work, will replace all occurrence sessions with token checks in controllers like check_auth() above to use JWT instead of session-based auth. 

## Blocked
None. Everything needed is imported from PyJWT now.

## Key Decisions 
To do this correctly:
1. Must verify tokens are valid and not expired before trusting them 
2. Write a function that decodes and validates token claims
3. Replace all the old session compares with calls to this function passing the token
4. Forbid any request without an access token in header like old code forbade requests w/o session cookie

## Resolved Questions  
Q: Where should I get JWT? A: Just use PyJWT 
Q: Which file should decode live? A: Put it in security/authz.py with other main auth logic

## Pending User Asks
None. 

## Relevant Files  
Not much left to show -- mainly working on one module:

* security/authz.py (modifying)