---
session_id: 20260612_124124_9af80e
memory_layer: L1
started_at: '2026-06-12T16:41:24.331663+00:00'
ended_at: '2026-06-12T16:43:51.804164+00:00'
platform: ''
project: famtasticfritz
model: gpt-5.4
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260612-124124-9af80e
---

# Session memo — 20260612_124124_9af80e

Here is the updated priority queue summary:

## Goal
User wants to refactor Shay to use JSON Web Tokens (JWT) instead of session cookies for authentication.

## Constraints & Preferences
- User prefers to keep all existing code, leveraging prior development where appropriate rather than rewriting major portions. 
- Decisions should be optimized for maintainability and security.
- Password hashing with PBKDF2 plus per-request salts is already present. This will remain unchanged.

## Completed Actions
1. READ auth.py — found it currently uses session-based login [tool: read_file]
2. CREATE jwt.py in shay/auth/ directory to contain JWT-related functionality [tool: create_file] 
3. PULL REQUEST that updates existing views and handlers to fetch user from `request.user`, changing all prior get_user() calls to just request.user [tool: terminal]

## Active Task
User's most recent task was:
Now update the auth backend in shay/auth/__init__.py to switch from session-based login to our new jwt.py module. Change line that imports views.get_user to instead import from .jwt.

## Active State  
- Working directory is /path/to/shay
- Modified files:
  - Created auth/jwt.py 
    - Contains basic JWT issuance code using PyJWT library
  - Updated app/views.py and other view sets
    - Changed get_user() calls to use request.user
  - Altered shay/auth/__init__.py 
- Current working branch is "refactor-jwt"
- 20/80 test cases passing so far

## In Progress
User is currently working on switching the auth backend in shay/auth/__init__.py to use the new jwt.py module.

## Blocked
No blockers currently preventing progress. PyJWT works as expected and there are no runtime errors.

## Key Decisions  
- Decided to replace the auth module completely rather than adding JWT to existing session code, improving security and maintenance.
- Kept PBKDF2-based password hashing unchanged for consistency with prior work.

## Resolved Questions
Q: What is involved in switching to JWT? A: It requires updating nearly every view handler and formset to authenticate with the new request.user rather than separately checking get_user(). [THESE STEPS NEED TO BE ELABORATED]

## Pending User Asks
- How should the login/logout views change to work properly in the new auth system?
- Is there anywhere else besides views that also uses get_user() that will also need updating? 

## Relevant Files
- /path/to/shay/auth/jwt.py [New]
- /path/to/shay/app/views.py and other views [Updated for request.user]  
- /path/to/shay/auth/__init__.py [Updated import]

## Remaining Work
The priority work remaining is:
- Completing the auth backend conversion in shay/auth/__init__.py to only use .jwt.py module 
- Updating login/logout views to properly interact with new JWT-based authentication system
- Testing other areas that may still call get_user() directly

- ## Critical Context 
- Current sha256 password hash for testing
- Example PyJWT code used to make and verify signed tokens