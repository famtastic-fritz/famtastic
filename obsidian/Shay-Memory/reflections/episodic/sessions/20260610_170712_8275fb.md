---
session_id: 20260610_170712_8275fb
memory_layer: L1
started_at: '2026-06-10T21:07:12.156467+00:00'
ended_at: '2026-06-10T21:09:42.934415+00:00'
platform: cli
project: famtasticfritz
model: claude-opus-4.7
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260610-170712-8275fb
---

# Session memo — 20260610_170712_8275fb

Python 3 summary. 

The loop is built for fetching your PoE status daily via scraping:

- It runs:
  - Every 4 hours—cron adds `poe-scrape.sh` every 240m to '~/.shay/scripts/'
    - To shorten it, run `crontab -e` put "*/15 * * * ? * cd ~/.shay/poe-tracker && PYTHONUNBUFFERED=1 python3 scrape.py" on a line by itself
- Daily 8 AM—cron adds `poe-daily-summary.sh`

It records:
- Daily to `usage.csv`
- JSON of last run to `raw` 

The daily email comes from your "Send usage report every [n] days" cron job that runs `poe-daily-summary.sh`. Update it in the script or rerun:

```bash
crontab -e 
*/10 * * * ? * cd ~/.shay/poe-tracker && PYTHONUNBUFFERED=1 python3 summary.py > /tmp/daily-summary-$(date +'%Y%m%d').txt
```

Run:
```python
pip3 install requests beautifulsoup4 csv
``` 

to install missing Python libs for the scrape.

Revisit in a week to see if we:

- Can shorten `poe-scrape` cron from 4h to 2, or even hourly at that pace 
- Need other columns besides total_usage, total_points, balance

Use my updated Python API:
```python
from mypoetrack import get_daily_totals, get_balance
```

Let me know if you need the code for `mypoetrack/utils.py` to replicate the functionality of these two functions it contains.

2641 tokens.