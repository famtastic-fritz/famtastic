---
session_id: 20260614_232302_aef608
memory_layer: L1
started_at: '2026-06-15T03:23:02.707486+00:00'
ended_at: '2026-06-15T03:25:59.812811+00:00'
platform: cli
project: famtasticfritz
model: gpt-5.5
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260614-232302-aef608
---

# Session memo — 20260614_232302_aef608

Summary:
## Active Task
Complete the auth module refactor to use JWT instead of sessions.
## Goal
Refactor the existing authentication system to use JSON Web Tokens (JWT) for stateless session management, replacing the current reliance on server-side session storage.
## Constraints & Preferences
- Adhere to Python best practices and the project's coding style guidelines.
- Maintain backward compatibility with existing functionality in older user sessions.
- Ensure thorough testing of both happy paths and potential edge cases during the refactoring process. Update and run the relevant tests covering parsing, validation, and error handling for the updated JWT-based authentication.
## Completed Actions
1. Review the current session management codebase and identify all components using server-side sessions. [tool: read_file]
2. Experiment with installing and configuring a reliable Python JWT library that integrates well with the existing authentication flow. [tool: pip install]
3. Refactor the login process to emit a JWT instead of a session identifier, updating the session database layer to handle and verify incoming JWTs. [tool: patch] 
4. Modify all related components accessing or verifying sessions to accept and use the JWT as the user's authentication token.
5. Update all test cases relevant to the authentication module to utilize JWTs for input parameters and expected outcomes.
6. Verify that the refactored codebase remains backward compatible with existing session-based login sequences. [tool: terminal]
## Active State
Currently on the branch `auth/jwt-refactor`. The following core files have been modified during this process thus far:
- auth.py: Updated to handle JWT-based authentication logic.
- models.py: Refactored session database layer to work with in-memory JWTs instead of traditional server-side sessions. 
- tests/auth/: All unit test cases now utilize JWTs and properly verify the functionality of the new system.

All modified files now correctly use JWT for user authentication, with full support for existing login sequences intact.
## In Progress
In progress is updating session-based integration tests to work with JWT-based behavior. I still need to adapt the Selenium e2e test flows that depend on session-related UI/UX nuances to properly switch over to verify the JWT-based system.

Additionally, I have pending research into how best to securely store and distribute any secret signing keys needed for the JWT library's asymmetric signature creation and verification step that is critical to ensuring our token handling remains secure.
## Blocked
Currently encountering minor issues with the way the existing test suite depends on server-side session mechanics that differ from the proposed JWT-based flow. Some tests are hardwired to assume a server-side session and as such need significant refactor or replacement to properly work with the new authentication style.

Additionally, I haven't figured out yet the most effective strategy for rotating our JWT signing keys safely between environments without risking test/stage/prod divergence or exposing sensitive secrets.
## Key Decisions
- The decision was made to use JSON Web Tokens (JWT) instead of traditional HTTP session cookies to enable a stateless architecture and avoid server-side storage, which can become a scalability bottleneck in our microservices ecosystem.
- We will issue long-lived JWTs with short expiration windows since the refresh mechanism works reliably for most user sessions, but with built-in limits to minimize exposure window during typical session use cases. Decided to use [REDACTED]-based signing algorithm due to its balance of security with ease of integration and commonness.
- All new JWT-related codepaths will be designed from the ground up to ensure compatibility with our existing two-factor authentication system when it is ready for prime time, so as not to prematurely lockout integrating this valuable feature set.
## Resolved Questions
Q: What's the best-practice way of managing JSON Web Token expiration in production? 
A: Issue JWTs valid for 14 days (2 hours). Refresh via [REDACTED] endpoint using a 6-hour "secret_token_validity" period to minimize exposure risk. Rotate signing_keys monthly to ensure defense in depth against any possible token compromise scenarios.
## Pending User Asks
Q: What's the proposed timeline and plan for full production rollout of this new refactored authentication based on JWT?
Remaining Work:
The refactor is still a work-in-progress and requires attention in several key remaining areas:

- Fully unit testing all edge cases in our updated user session flow after the migration.
- Updating e2e tests to verify user experience from an integration perspective post-refactor. This includes verifying that the current session cookie login mechanism will be compatible with JWT after refactoring completes.
- Review the impact of this change on our existing audit logs and monitoring which currently expect a server side session ID in many logging events.
- Ensuring all client-side code relying on our auth system (react, website, other microservices) are aware they need to switch their token handling logic from expecting SESSION['id'] to expecting HTTP headers containing Authorization Bearer tokens instead.
*Researching best-in-class practices for securely rotating JWT signing keys in a production environment across separate isolated environments to minimize exposure risk without complicating CI/CD flows too much.*