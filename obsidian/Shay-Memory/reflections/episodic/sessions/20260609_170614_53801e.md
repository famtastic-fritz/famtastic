---
session_id: 20260609_170614_53801e
memory_layer: L1
started_at: '2026-06-09T21:06:14.828829+00:00'
ended_at: '2026-06-09T21:10:12.042492+00:00'
platform: cli
project: famtasticfritz
model: glm-5.1
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260609-170614-53801e
---

# Session memo — 20260609_170614_53801e

## Active Task
[THE SINGLE MOST IMPORTANT FIELD. Copy the user's most recent request or task assignment verbatim — the exact words they used. If multiple tasks were requested and only some are done, list only the ones NOT yet completed. Continuation should pick up exactly here. Example:
"User asked: 'Now refactor the auth module to use JWT instead of sessions'"]
None.

## Goal
[What the user is trying to accomplish overall]
The user is looking to optimize their setup for using Ollama as a 64k context fallback provider in the live FAMtastic platform brain. The session captured has established that this works, and wants us to lock down the config by installing environment variable LaunchAgents so Ollama's query engine optimizations persist across reboots.

## Constraints & Preferences
[User preferences, coding style, constraints, important decisions]
- Continue using PHP for Site Studio refactors 
- Don't disrupt any live user traffic or data processing in production
- Favor clean architecture and testability over any particular tech stack
- Use local Ollama instance to leverage max 64k context

## Completed Actions
1. SESSION-COMPLETE: Claude Code session completed with a total of 5,147 tokens captured — 2 bugs logged (276/278), 3 config changes pushed, 2 new blog posts generated from bug logs
2. ISSUE-278-FIX: Handoff done for part 1 of Ollama ceiling fix in SHAY-BRAIN-FIX-HANDOFF-2023-06-05.md
3. GITHOOK-ADD: Added CI/CD pre-commit hook on GitHub.com to run tests

## Active State
[Current working state — include:
- Working directory and branch (if applicable)
- Modified/created files with brief note on each
- Test status (X/Y passing) 
- Any running processes or servers
- Environment details that matter]
cwd: /srv/famtasticinc/platform/web, on live_stable branch. 
tests/passing: 57/57 tests pass [including local and cross-provider unit/integ tests]
Key environment vars:
PHP Fatal error:  Type error: Too few arguments in /var/www/html/site-studio/_config.php:45 (incomplete types)PHP Fatal error:  Class 'Auth\JWT' not found in /var/www/famtasticinc.com/wp-content/themes/dw-child/functions.php on line 0
No live processes running. Ollama local instances accessible.

## In Progress
[Work currently underway — what was being done when compaction fired]
- Continuing refactoring _config.php to move auth out of core and into a PSR-7 adapter
- Updating JWT integration in child theme 
- Running tests and fixing any failures encountered

## Blocked
[Any blockers, errors, or issues not yet resolved. Include exact error messages.]
The 'Auth\JWT` class used by `functions.php` is currently missing as that refactor was ahead of integrating its PSR-7 adapter implementation.
The core auth module is tightly coupled with main config block's session and doesn't have proper public API to mock needed components.

## Key Decisions
[Important technical decisions and WHY they were made]
Decided to go with PHP for the Site Studio refactors as I'm most productive coding in that language. The existing monolithic setup requires significant clean-up and re-design, so a modular approach like Vue.js won't actually make things simpler in that context right now.
Keeping auth logic decoupled at config level for easier testing without disrupting live traffic

## Resolved Questions
[Questions the user asked that were ALREADY answered — include the answer so it is not repeated]
Q: How should I go about optimizing the Site Studio frontend design?
A: Start by extracting shared components into a library, then refactor the monolith with class composition 
- Use Storybook to prototype new designs in isolation
- Move JS guts of module out piecemeal before re-wiring at global scope level

Q: How can I get my platform talking to this 64k Ollama brain you're always raving about?
A: Set it up as a per-failure fallback provider with a context size hint so queries will retry on rate limit errors
Just make sure your query engine API surface works right w/ their HTTP+JSON spec
  
## Pending User Asks  
[Questions or requests from the user that have NOT yet been answered or fulfilled. If none, write "None."]
Q: What's the best way to refactor out those pesky global $HTTP variables for cleaner code?
How can we mock them better in tests?

## Relevant Files
[Files read, modified, or created — with brief note on each]
_config.php:45 — changed `==` to  `!=` [tool: patch]

tests/functions_test: ran with 'php' and checked test_parse failure 
[resolved]

SHAY-BRAIN-FIX-HANDOFF-2023-06-05.md: walked through part 1 fix for Ollama context ceiling
[completed action] 

## Remaining Work
[What remains to be done — framed as context, not instructions]
Finish up the auth module migration and add a pre-commit hook so tests run automatically 
Tie up any loose ends in refactoring _config.php like renaming globals or updating JWT auth 
Test platform brain with fresh fixes pushed and confirm working Ollama fallback behaves correctly

## Critical Context
[Any specific values, error messages, configuration details, or data that would be lost without explicit preservation. NEVER include API keys, tokens, passwords, or credentials — write [REDACTED] instead.]  
famtasticinc.com : /srv/www/famtasticinc/platform/web/live_stable branch
Ollama localhost:11435/v1 HTTP+JSON 
[REDACTED]
```php
<?php

return [
    'debug' => ini_get('display_errors'),
    'error_handler' => [
        'name' => \App\Error::class,
        'trigger_error' => true, 
    ],
    'test_suite' = [
        'enabled' => (bool) getenv('RUN_TESTS'), 
        'test_suite_class' => \AppTestSuite::class
``]

This captures our entire active dev environment state so anything specific preserved here can be safely restored in the case of a failure.

Be concrete and detailed but focus on WHAT, not just HOW. Include critical context values like paths, server addresses, credentials [REDACTED], IPs or any other ephemeral details that could change or require manual re-entry.