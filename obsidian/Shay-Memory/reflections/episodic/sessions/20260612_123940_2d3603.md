---
session_id: 20260612_123940_2d3603
memory_layer: L1
started_at: '2026-06-12T16:39:40.157903+00:00'
ended_at: '2026-06-12T16:41:24.195087+00:00'
platform: ''
project: famtasticfritz
model: gpt-5.4
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260612-123940-2d3603-1
---

# Session memo — 20260612_123940_2d3603

## Active Task
Update system to use JWT for authentication rather than session tokens.

## Goal
Refactor all code that handles user authentication to use JSON Web Tokens (JWT) instead of server-side session cookies.

## Constraints & Preferences
- Use the `pyjwt` library 
- Replace session cookies with JWT in URL
- Use separate `Authenticator` class
- Keep file structure and naming conventions

## Completed Actions
1. READ ../shay/core/auth.py — current auth handling [tool: terminal]
2. CREATE ../shay/system/auth.py — new modular `Authenticator` class stub [file]
3. READ ../shay/service/cli.py — CLI login logic to adapt [file]
4. PATCH ../shay/service/cli.py:45 — integrate `Auth.create_jwt()` into `login()` [tool: patch] 
5. CREATE ../shay/web/session.py — new `SessionWrapper` class stub for URL generation [file]

## Active State
- WORKING DIR: /home/user/dev/shay/system/auth/
- MODIFIED FILES:
  - Created ../shay/core/crypto.py with `HS256` hasher [file]
  - ../shay/system/auth.py: def create_jwt(user): ...
  - ../shay/service/cli.py changed login and logout to use JWT
    (login: def login(user, force=False): ...)
- 15/100 tests passing in ../tests/

## In Progress
- Creating the new `Authenticator` class with logic to:
   - Hash password via bcrypt into salt 
   - Create an unexpired HS256 signed JWT token to embed in URLs
   - Extract and verify user from current URL's JWT when accessed 
- Upgrading CLI login interaction to create/embed JWT, plus handle logout

## Blocked
No blockers at this point. However, I do not yet have a clear mapping of the code that interacts directly with user sessions, so there may be some yet undiscovered uses of session throughout.

## Key Decisions  
- Use HS256 algorithm as it is simplest and enough for domain
- Extract into crypto module to hash passwords 
- Keep business logic separate in auth system layer
- For now pass around plain texts; later will need better handling

## Resolved Questions
Q: What's the preferred library for JWTs? A: `pyjwt` library, as it's pure python and widely used.

## Pending User Asks
None at this time.

## Relevant Files
- ../shay/core/crypto.py — new module with bcrypt  hashing 
   (Created to avoid importing into unrelated modules)
- ../shay/system/auth.py — stub for `Authenticator` class
- ../shay/service/cli.py — CLI login interaction logic updated  

## Remaining Work  
- Write out complete logic in auth system module 
  - Finish implementing the new "Auth.create_jwt(user)" function
  - Update the logout action using the complementary "verify_jwt" method
  - Integrate with URL generation and parsing mechanism
- Ensure that CLI is correctly adapted
- Thoroughly test all interactions throughout 

## Critical Context 
None specific to this task. Other configurations should be kept constant.
[REDACTED].

Target ~2767 tokens. Be CONCRETE — include file paths, command outputs, error messages, line numbers, and specific values. Avoid vague descriptions like "made some changes" — say exactly what changed.

Write only the summary body. Do not include any preamble or prefix.