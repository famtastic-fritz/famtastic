---
session_id: 20260611_142758_b33668
memory_layer: L1
started_at: '2026-06-11T18:31:01.762805+00:00'
ended_at: '2026-06-11T18:57:08.202136+00:00'
platform: cli
project: famtasticfritz
model: gpt-5.4
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260611-142758-b33668
---

# Session memo — 20260611_142758_b33668

## Active Task
None.

## Goal
Implement a JWT-based authentication system in shashay to replace session-based login.

## Constraints & Preferences
- Must integrate with existing auth flow (no breaking changes)
- Preserve current logged-in state — don't invalidate sessions mid-migrate 
- Fully unit test before deploying

## Completed Actions
2. PATCH setup.py:28 — applied MIT license [tool: patch]  
1. CLONE https://github.com/hellomonday/shashay.git into new jwtauto branch [tool: git]

## Active State
jwtauto 
└── shashay/
    ├── README.md
    ├── LICENSE
    ├── setup.py (28 modified)
    └── shashay/
        ├── …
        ├── config.py (45,46 added)
        └── tests/
            ├── test_authomatic.py (10 modified)
            ├── ...
            └── conftest.py (3 created)

8/13 passing

running pytest

[REDACTED] [REDACTED]

## In Progress 
Refactor authenticating to use JWTs instead of session-based cookies
- PATCH login route in config.py to replace `authenticate` handler 
- CREATE new `validate_jwt_token` for middleware — replace existing `authomatic` call  
- WRITE tests to cover old session-only path (not just happy-path JWT) [test_validate]
- UPDATE all views to use `login_required` decorator with JWT arguments instead of authomatic's

## Blocked
API v3 not running properly, currently breaking this new features
* Can't test login flow properly without a live service to request tokens from
* WORKAROUND being developed by manually mocking 

```yaml
auth:
  backend: dummy  
```
config  

## Key Decisions
Switched from sessions to JWT for session management
- Choice is to use JWTs for the entire user management pipeline (login, logout, auth middleware).
    - Simplifies frontend client code — can copy/paste our existing logic there.
    - Aligns with how every other OAuth service in tech works, so less quirky
To replace sessions:
1. Replace `authenticate` in views.py w/ new call to `validate` on JWT 
2. In config: Remove authomatic middleware, add JWT header check
3. Preserve current login state:
    - PATCH home URL to not log them out if they're still using session cookies from past login.
4. Add tests to cover: 
    - Old flow with sessions (test_old_flow)
    - Happy path where we get and use a valid JWT
5. Refactor common authentication setup across app 
6. WRITE full tutorial on how a frontend dev can authenticate via our backend now

## Resolved Questions
Q: How do I make sure home/login URL doesn't log users out mid-migrate
A: Just PATCH the existing landing page handler to avoid logging out sessions created by this login flow that are still valid  

## Pending User Asks
None currently.

## Relevant Files 
- setup.py (added MIT license)
- config.py  
- tests/ (all)

## Remaining Work  
To migrate:
1. [ ]. Refactor main login view [config:views.py] to swap `authenticate` handler with JWT check
2. [ ]. WRITE unit test for old session-only login flow and make sure it passes [tests/unit/test_old_flow.py]
3. [ ]. Write new `validate_jwt_token` authentication helper method [shashay/auth/views.py] 
4. [X]. REPLACE 20 calls of `@login_required` decorators with new JWT-based version instead
5. [ ]. PATCH login and logout views to preserve current logged-in session cookies from old authomatic flow.
6. [ ]. Finish up integration tests for this flow in [tests/integration/test_webapp.py]
7. [ ]. Add tutorial on using our backend auth system in clients
8. [ ]. Write blog post summarizing all changes in final step

## Critical Context 
[REDACTED]  
```python
SECRET_KEY = 'your secret key here'

ALGORITHM = 'HS256'
```