---
session_id: 20260614_181449_86a14c
memory_layer: L1
started_at: '2026-06-14T22:14:49.848573+00:00'
ended_at: '2026-06-14T22:45:32.947351+00:00'
platform: ''
project: famtasticfritz
model: gpt-5.4
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260614-181449-86a14c
---

# Session memo — 20260614_181449_86a14c

## Active Task 
[REDACTED]

## Goal
To refactor the auth module to use JWT instead of sessions


## Completed Actions 
1. READ main.py:25 — found outdated login code [tool: read_file] 
2. REVIEW old_auth.py:15-20 — saw legacy session code [tools: review_code, context_diff] 


## Active State
* modified: main.py
* created: jwt_auth.py

main.py:
```python
def auth_user(raw_data):
    try:
        # ...old outdated code here...
    except KeyError as e:
        raise AuthError(f"Invalid credentials: {e}")
```

new jwt_auth.py:
```python
from functools import wraps

# some JWT setup and helper functions...

def require_jwt(decorated):
    @wraps(decorated)
    def wrapper(*args, **kwargs):
        token: [REDACTED]
        if not is_valid(token):
            raise AuthError("Invalid credentials")
        return decorated(*args, **kwargs)
    return wrapper


#...more helper functions...

def auth_user(data):
    # ...use JWT instead of session...
    if bad_credentials(data):
        raise AuthError("Invalid login")
```

## In Progress 
* fully replacing the old session-based auth with JWT in main.py
* testing the new jwt_auth module separately

jwt_auth.py is currently under dev and not yet integrated into main.py's auth_user. 

## Key Decisions  
- switching from sessions to JWT for managing auth state 
- using JWTS for both auth cookies/sessions and authorization 
- adding refresh token logic to handle expirations
- abstracting all the boilerplate code into a reusable auth module

## Remaning Work
Here is how I would approach the rest of this:

1. **Refactor main.py/auth_user:**
   - Replace any session logic with JWT calls
   - Use `require_jwt` decorator on sensitive views/controllers
   - Wire in `auth.init_app()` boilerplate

2. **Re-test:** 
   - Run full suite and make sure no regressions

3. **Wrap Up**:
   - Remove all old session code/files
   - Update any config, header or docstrings that refer to sessions
   - PR and merge

4. **Follow-Up Tasks**:  
   - Review security 
     - Scan for leaks in main.py/auth_user 
     - Ensure logging is minimal   
   - Add integration tests
   - Optimize token cache

This should get us a production-grade, well-factored JWT auth system in place, ready to replace the old one. Let me know if you need any other details filled in or have additional context to provide!