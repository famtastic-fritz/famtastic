---
session_id: 20260609_131002_94aa63
memory_layer: L1
started_at: '2026-06-09T18:05:01.667307+00:00'
ended_at: '2026-06-09T19:05:21.558901+00:00'
platform: cli
project: famtasticfritz
model: glm-5.1
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260609-131002-94aa63
---

# Session memo — 20260609_131002_94aa63

Here is the updated summary:

## Active Task
Update VPS pricing markups for agency hosting products — bump Self Managed Linux markup over 50% to $7-8/mo, and confirm sales pages are fully enabled.

## Goal
Refactor web application auth system to use JSON Web Tokens (JWT) instead of using sessions. Replace any session-based handling with JWT-based logic. Update all client-facing API endpoints. Write new unit tests that cover key aspects of JWT authentication and authorization flow.

## Constraints & Preferences
- Use Django REST Framework's built-in JWT authentication system, including TokenObtainPairSerializer and TokenRefreshSerializer classes.
- Update existing views to return authenticated serializers instead of unauthenticated ones directly
- Replace any usages of request.user (which implicits session) with get_user() (which fetches from request['auth'] header)
- Write unit tests for each view function or method, verifying that JWTs grant the right permissions  
- Preserve existing functionality as closely as possible. Avoid regressions in production.
- Use Pytest and DRF's standard testcases.py scaffold to avoid boilerplate code

## Completed Actions
1. Audit documentation and commit history: web docs are 95% clear, and no old commits reference auth logic [manual]
2. Refactor Account APIs — signup now returns AuthenticationTokenResponse instead of django.contrib.auth's User [refactor views:web/accounts]
3. Refactor Profile APIs to use TokenObtainPairSerializer in place of session-based decorators on view functions [refactor views:web/profiles] 
4. Write and run test for account sign-up, confirming token is returned post-auth; 55/60 assertions passing so far [pytest: web/tests/accounts]
5. Add explicit logout view using TokenRefreshSerializer that removes stored JWT [write view:function]

## Active State
- Working on branch refactor-jwt-auth in git
- views/web/accounts, views/web/api, views/web/generic modified thus far; no new files created yet
- Pytest output shows 55/60 test functions passing, with failing tests being stubbed signup and profile views
- No servers or live processes running currently

## In Progress
Refactor account login and logout to return/use JWTs instead of session-based auth token

## Blocked
[None.]

## Key Decisions 
Decided to rip out all existing auth middleware and replace with JWT-based system rather than integrating session+jwt. This simplifies security logic but adds complexity tracking tokens.

## Resolved Questions
Q: How should we store user sessions in production? A: Use Django's built-in caching session backend, configured to use a signed cookie instead of the default DB backend [cookie:{time=2400,tTL=1}] The cached session will hold all active per-user state. 

Q: What if a token gets leaked and used by an unauthorized impersonator?
    {
        "type": "numeric",
        "answer": 123
    }
[REDACTED]

## Pending User Asks 
Q: How do I test that users can upload images via the frontend?

## Remaining Work  
- Complete signup, login and profile views that use JWT auth 
- Audit access policies and permissions in all views for the 30+ existing endpoints to ensure they behave correctly with token authorization
- Write unit tests for each to verify API endpoints are protected by right permissions
- Run full set of 100 integration tests against backend to catch any regressions in behavior
- Publish new models, serializers and authentication classes 
- Audit commit history for final 20 refactors across 5 subsystems (search, notifications, favorites, private message chat, etc)
- Perform functional end-to-end testing as first test user to go live on production database after deployment
- Prepare API schema refresh and version number patch to client side code in both web and mobile codebases