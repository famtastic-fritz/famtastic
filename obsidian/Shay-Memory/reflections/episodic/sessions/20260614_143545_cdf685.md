---
session_id: 20260614_143545_cdf685
memory_layer: L1
started_at: '2026-06-14T18:35:45.406234+00:00'
ended_at: '2026-06-14T19:27:53.499733+00:00'
platform: ''
project: famtasticfritz
model: gpt-5.4
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260614-143545-cdf685-1
---

# Session memo — 20260614_143545_cdf685

## Active Task
Refactor the auth module to use JWT instead of sessions

## Goal
Implement a secure authentication system using JSON Web Tokens (JWT) for the user-facing Python web app.

## Constraints & Preferences
- Integrate with existing password hashing if possible.
- New tokens should be stored next to hashed pwds - not separate.
- Ensure test suite passes after changes.

## Completed Actions 
1. RESEARCH JWT auth — read 'authenticating-users-with-json-web-tokens.md': JWT concepts, flow, and Pythonlib [tool: read_file]
2. SKETCH out initial JWT-based auth module pseudo-code [tool: write_code_pseudocode]
3. START new 'auth.py' as the real implementation file [tool: add_file] 
4. BEGIN 'auth.py' contents below — placeholder for JWT code [tool: write_code_fake_place_holder]

## Active State
- Working dir: `/home/user/wep/app`
- `auth.py`: under construction with pseudo-JWT auth code 
- No tests run yet on the changes.
- No environment notes relevant to this change.

## In Progress
- Still working on fully implementing the 'auth.py' JWT-based login system. 

## Remaining Work  
- Continue developing and testing 'auth.py'.  
- Update views to use new 'auth.py'.
- Possibly modify sessions.py, depending on refactoring needed.
- Finish test suite integration for auth module.

## Critical Context
- The existing `hashedpws.db` file contains 1k hashed password records for the current system in SQLite format — critical data!
- User "admin" already exists with a hashed and salted PW -- [REDACTED] -- which must still be usable after this change.
- Server's SECRET_KEY is `[REDACTED]` and used to securely sign session data there.

## Key Decisions
- Choosing to use JWT for security seemed best post-research, so moving forward with that design decision.
- Storing new tokens alongside existing hashed PWs in same system avoids needing a schema revamp in db layer.  

## Resolved Questions 
Q: Should I hash the JWT secret like the user password?
A: No don't hash it — the JWT signing key is public and only needs to be secret, not encrypted. Just store it DB next hashed pw.

## Pending User Asks  
None yet for this task.

## Relevant Files
- `/home/user/wep/app/hashedpws.db` -- SQLite file with hashed password records 
-  `auth.py` -- new module to hold JWT-based login code
- existing sessions.py I think? Not totally sure if refactor moves there or not. 

And the 'auth.py' start...

```python
# Create a JWT on successful login from existing hased pwd
def create_session(user_id):
    # ... generate TOKEN ...

    return TOKEN
    
```

Continuing with the real implementation of JWT auth after this stub.