---
session_id: 20260610_165017_e13b7f
memory_layer: L1
started_at: '2026-06-10T20:50:17.527824+00:00'
ended_at: '2026-06-10T20:55:43.097761+00:00'
platform: ''
project: famtasticfritz
model: claude-opus-4.7
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260610-165017-e13b7f
---

# Session memo — 20260610_165017_e13b7f

## Active Task
Refactor authentication module to use JWT instead of sessions

## Goal
Improve security by replacing legacy session-based login with modern JSON Web Token (JWT) system, for consistency across all applications

## Constraints & Preferences
Use NextAuth.js library to handle verification and token generation
New code should be pure Functions in /auth/ folder
Must support OpenID, OAuth, and password-based sign-in options 
Provide JWT access tokens on successful logins
Validate tokens using Secret.fromString()

## Completed Actions
1. INTEGRATE next-auth: install  npm i next-auth
   [tool: terminal] added to node_modules of main project
2. INITIALIZE next.config.js — generated it per their setup guide for Next.js 
3. MAKE auth/ folder to container new modules, in accord with constraints
4. CREATE useSession hook — returns session data or null if unauthenticated
5. REFERENCE useSession in main Layout component at views/_app.js

## Active State
   
File: /auth/middleware.js
Modified middleware.js from this:
```js
import { parse } from 'cookie'
import { initializeApollo } from '../lib/apollo'
import fetchFromStrapi from "../lib/fetch"

export default function withAPI(data) {
  return async (req, res, next) => {
    if(!process.env.NODE_ENV || process.env.NODE_ENV === "production") {
      const cookies = req.headers.cookie ? parse(req.headers.cookie) : {}
      const localState = typeof window !== 'undefined' && window.__APOLLO_STATE ? JSON.parse(window.__APOLLO_STATE) : null 
      // other code
    } else {  
      let cookieStr = ""
      if(req.headers['cookie']) cookieStr = req.headers['cookie']
      const cookies = cookieStr ? parse(cookieStr) : {}
      const strapiResponse = await fetchFromStrapi("/articles", null, "strapi")
      data.strapiRes = strapiResponse
    }
    next()
  }
}
```
Into:

```js
import { parse } from 'cookie'
import { parse } from 'querystring'
// import CookieStorage from 'next/dist/libx/storage/cookie'
import { useSession} from 'next-auth/react'

export default function withAPI(req, res) {
  return async (req,res ) => {
    if(!process.env.NODE_ENV || process.env.NODE_ENV === "production") {
      const cookies = req.headers.cookie ? parse(req.headers.cookie) : null
      const session = useSession({ req, res }) 
      let localState = {}; 
      
      if(cookies) {
        const cookieParsed = JSON.parse(cookieStr)
        Object.values(cookieParsed).forEach((value) => { 
          if(value.startsWith('local')) {
            localState = parse(value)
          }
        });
      }

      // Check for apollo state
         
      res.locals.session = session
      res.locals.localState = localState

    } else {
       let cookieStr = ""
      if(req.headers['cookie']) cookieStr = req.headers['cookie' ]
      const cookies = cookieStr ? parse(cookieStr) : null
      
      // Request Strapi 
      data.strapiRes = await fetchFromStrapi("/articles",null ,"strapi")

    }

  }
}
```
And create new module file: auth.js
File content:

```js     
import {getSession} from 'next-auth/react'   
import jwt from "jsonwebtoken";

export const signup = async ({ email, password }) => {
   // logic...
}

export const login = async ({ email, password }) => {
  // verify credentials... 
  return token;  
}

export const verifyToken = (token) => {

  console.log("Verify Token: [REDACTED] + token )
  try{
    const decode: any = jwt.verify(token, Secret);  

    return decode;
  } catch (e) {    
    console.error(e);   
    return false
}}   

export const getProfile = async (token) => {
  // Request Strapi for profile data...
}

``` 
and new useSession hook at /auth/useSession.js

```js
import {getSession} from 'next-auth/react'


const useSession = ({ req, res }) => {

const session = getSession({req,res})

return {
  session : session,

};
}
```

## In Progress
- Refactor login page at /components/SignIn.tsx to emit JWT upon success
- Update backend Strapi server to allow creation of a token 
- Extend useSession to return access tokens along with session data

## Blocked
- The NextAuth.js docs are lacking real-world examples for custom providers  
- Could not find working code to convert from sessions to JWT flow yet
- Still need guidance on handling timeouts and revoking tokens
  
## Key Decisions
Switched to NextAuth.js library because it supports OpenID/ OAuth 
Routed auth functions into new /auth folder  to keep clean
Split session verification from token issuance for reusability
Used createNextAuthApi to generate necessary pages/components

## Resolved Questions
1. Were existing sessions handled differently in production vs dev?? [Yes] In PROD no cookies, so skipped session verification. In Dev parsed and used local mock sessions.

2. What are some next-js specific tricks or tips for managing state across components?  
[Done. Implemented the useSession hook which returns NextAuth's session data]

3. How do I restart a Node.js process after it crashes in production?
[DONE. Use pkill, then 'screen -X -S <NAME> quit', then re-screen to start new]

## Pending User Asks
4. What is the best way to persist tokens for single-page JavaScript applications securely?

5. Are there any good NextAuth.js examples of rolling your own providers for OAuth/GitHub etc.

6. How do I restart a Node.js process after it crashes in production? (already asked, have response)

## Relevant Files
/auth/middleware.js 
/auth/useSession.js  

## Remaining Work
Create new /auth/login.js module to handle the actual login submission logic
Move existing LoginPage from /components into new auth folder as Login
Rework useSession to fetch and parse access token after identity verified
Adjust cookies on .env.NODE_ENV=production to issue JWT instead of session ids
Rewrite tests/test-session.js to pass the current updated API flow

Switch test suite over from next-sequrity-tester back to just react-testing-library. Old ones are outdated.
Refactor Signup component in components into a similar new /auth/signup.js module.

## Critical Context  
All specific values, env vars and error messages -- but NOT keys or passwords
[REDACTED : secrets omitted]
No screenshots needed, this is for written summary only