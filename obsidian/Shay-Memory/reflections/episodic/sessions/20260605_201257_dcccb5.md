---
session_id: 20260605_201257_dcccb5
memory_layer: L1
started_at: '2026-06-06T00:12:58.440846+00:00'
ended_at: '2026-06-06T00:18:50.844438+00:00'
platform: cli
project: famtasticfritz
model: glm-5.1
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260605-201257-dcccb5
---

# Session memo — 20260605_201257_dcccb5

## Active Task
None.

## Goal
Ensure GLM is live on the service and document the deploy script bug.

## Constraints & Preferences
- Use exact file paths and line numbers
- Preserve all provenance and tool outcomes
- Highlight critical infrastructure state
- Maintain explicit separation between resolved and pending work

## Completed Actions
1. READ `~/.shay/.env` — confirmed GLM_API_KEY present [tool: read_file]
2. PULL main branch — commit `77d8f2f` includes GLM wiring [tool: terminal]
3. VERIFY server on :8787 — process started 20:11:37, mtime 20:11:15 [tool: terminal]
4. TEST `/api/chat` with "Say only your model name" — response: "I'm Shay" with `brain_id=glm` [tool: curl]
5. AUDIT deploy script — bug: PID tracking fails, starts duplicate server [tool: search_files]
6. LOG server state — `com.famtastic.shay-phone.plist` manages process via launchd [tool: terminal]

## Active State
- Working directory: `/Users/famtasticfritz/famtastic/shay-phone`
- Modified files: 
  - `server.py`: GLM wiring at ~1438-1450 (context: 3)
  - `com.famtastic.shay-phone.plist`: launchd process management
- Running processes: 
  - `python server.py` (managed by launchd)
  - `curl` test session (completed)
- Environment: 
  - `GLM_API_KEY` configured in `~/.shay/.env`
  - Token authentication endpoint at `/api/chat?k=$TOKEN`

## In Progress
None.

## Blocked
None.

## Key Decisions
- Documented deploy script bug as system-level issue (not a code defect)
- Confirmed GLM is live via endpoint test, not just code audit
- Separated launchd management from deploy script workflow

## Resolved Questions
- **"Is GLM live?"** → Confirmed via `/api/chat` response with `brain_id=glm`
- **"What's the deploy script issue?"** → PID tracking fails, starts duplicate server

## Pending User Asks
None.

## Relevant Files
- `/Users/famtasticfritz/famtastic/shay-phone/server.py`: GLM integration at ~1438 (context: 3)
- `~/.shay/.env`: contains `GLM_API_KEY=***
- `/Library/LaunchDaemons/com.famtastic.shay-phone.plist`: launchd configuration

## Remaining Work
None.

## Critical Context
- Server running GLM-5.1 (flat) via `brain_id=glm`
- Deploy script erroneously starts duplicate server, crashes into launchd-respawned instance
- Token authentication uses query parameter `?k=$TOKEN` (not Bearer)
- `/tmp/shay-phone.log` trampled by dual writers (deploy script + launchd)