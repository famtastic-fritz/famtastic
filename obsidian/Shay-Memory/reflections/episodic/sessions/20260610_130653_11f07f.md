---
session_id: 20260610_130653_11f07f
memory_layer: L1
started_at: '2026-06-10T17:06:54.016558+00:00'
ended_at: '2026-06-10T17:08:12.422007+00:00'
platform: ''
project: famtasticfritz
model: glm-5.1
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260610-130653-11f07f
---

# Session memo — 20260610_130653_11f07f

User asked: 'Now refactor the auth module to use JWT instead of sessions'

The key aim here is to refactor the existing authentication mechanism using JSON Web Tokens (JWT) as opposed to session cookies. This involves several critical aspects:

To start, update auth_middleware.js and auth_helpers.js to:
1. Replace any uses of `fam_session` cookie with reading/writing JWTs. Rename setSessionCookie to setJWTCookie — modify the token format to store a serialized JSON object.
2. Implement jwtPayload(session) to deserialize session data in AuthUser
3. Remove any code related to session cookies and rely on JWT verification instead as per extractSession changes.

Then, also adjust:
4. All routes, helper functions, and views that set and check the fam_session cookie to now use the JWTCookie instead
5. Handle JWT refreshes — implement or adapt a solution to renew expired tokens

This will fully transition the app from session-based auth to JWTs with minimal breaking changes while gaining additional modern features like token expiration, single sign-on, and cross-app session reuse.

The goal is to finish replacing all cookie sessions with stored JWTs; to keep statelessness and flexibility advantages of JWTs. No UI changes are needed — token passing should be fully automated under the hood. Make sure to unit test thoroughly post refactoring especially auth functions to catch regressions.