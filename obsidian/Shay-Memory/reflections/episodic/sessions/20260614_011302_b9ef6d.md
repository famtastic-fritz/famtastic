---
session_id: 20260614_011302_b9ef6d
memory_layer: L1
started_at: '2026-06-14T05:13:02.984284+00:00'
ended_at: '2026-06-14T05:14:57.957691+00:00'
platform: cli
project: famtasticfritz
model: gpt-5.4
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260614-011302-b9ef6d
---

# Session memo — 20260614_011302_b9ef6d

## Active Task
None.

## Goal
The user's overall goal is to refactor a portion of their Python codebase using JWT for authentication instead of sessions.

## Constraints & Preferences
They recently made changes to the session handling that introduced a bug into the application, which broke a key part of the functionality. The user mentioned they want the new JWT approach but need an assurance that this will not break the existing code. They are keen on getting it working as quickly as possible.

## Completed Actions
1. Search latest CVEs for 'Flask' — No new issues [online: search]
2. SEARCH `config.py` for "session" — 14 total matches [tool: grep]

## Active State
The current directory is `/Users/famtastic/git/repo/app`. The branch used was the 'main'. The user changed some files related to session handling, especially in the config.py file. There are a few tests passing and several failed due to changes that haven't been addressed.
```python
config.py — Added new section for JWT configuration
tests/test_jwt.py — New module for testing JWT implementation
tests/test_session.py — Still contains several failing tests related sessions logic
```
## In Progress
Currently, the user is trying to update the test cases in `test_session.py` to make them pass using the session mechanism. They are also working on a feature to implement logging into their system.

## Blocked
The most significant blockers right now are understanding why changing the "==" to "!=" is still not resolving the issue with sessions and how to migrate all parts of code that rely on sessions over to JWT without breaking anything in the process.

## Key Decisions
- To make the JWT implementation, they have decided to use PyJWT for handling tokens. This choice ensures compatibility and makes it easier to maintain.
- The user is also considering whether to refactor session related tests or create new tests for JWT-related scenarios first since current ones may not be fully adapted for the change.

## Resolved Questions
1. What are the differences between using sessions vs JWT?
   - Sessions use server-side storage, which can cause issues in scalability, whereas JWT tokens are stored client-side, reducing load on web servers.
   - JWTs also offer an easy way to manage and track user sessions with less overhead.

## Pending User Asks
1. Confirm whether changing JWT will not break existing session usage.

## Relevant Files
- `/Users/famtastic/git/repo/app/config.py` — This contains the old session configuration code that's being replaced.
- `/Users/famtastic/git/repo/tests/test_jwt.py` — New test module to verify JWT authentication mechanism.
- `/Users/famtastic/gi/repo/tests/test_session.py` — Contains legacy tests; needs updating as the system moves from using session to JWT.

## Remaining Work
There's still significant work ahead, mainly involving:
- Updating and fixing the existing tests that currently fail because of session logic.
- Writing new tests for the JWT authentication.
- Debugging test cases linked with session-related operations failing due to the recent changes in configurations. 

## Critical Context
The following lines from `config.py:58` were changed:
```python
if debug:
    WTF_CSRF_DEFAULT_ = True
    SECRET_KEY='***'
else:
    # Old session config
    SECRET_KEY=os.get...EY')
```
to 
```python
if debug:
    JWT_SECRET='***'
else:
    # New JWT config
    JWT_SECRET=getenv...ET')
```
All changes related to moving from Flask's sessions to JWT are within this file.