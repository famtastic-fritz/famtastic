---
session_id: '20260606_135419_660626'
memory_layer: L1
started_at: '2026-06-06T19:41:03.283253+00:00'
ended_at: '2026-06-06T21:52:26.346823+00:00'
platform: cli
project: famtasticfritz
model: glm-5.1
memo_schema: handoff-v1
tags:
- memory/l1
- session-memo
permalink: shay-memory/reflections/episodic/sessions/20260606-135419-660626
---

# Session memo — 20260606_135419_660626

## Active Task
Refactor the auth module to use JWT instead of sessions

## Goal
Improve security by transitioning from session-based user authentication to using JSON Web Tokens (JWT).

## Constraints & Preferences
- Ensure no downtime — must support both old and new auth at once then fully migrate over
- Preserve all existing login flow, session functionality, and protections 
- Add "remember me" functionality for persisting sessions across browser restarts
- Keep the codebase clean, simple, and easy to maintain as we modernize this key component

## Completed Actions
1. READ auth.py: all — under stood current session setup flow [tool: read_file]
2. REVIEW design/JWT.md — reviewed best practices for implementing JWT in our stack [tool: terminal] 
3. SET UP test_jwt.py — wrote out scaffolding around file to hold new authentication tests [tool: create_file]

## Active State
currently on `auth` subdirectory, working with the following:
- modified auth.py (95% complete) - transitioning from sessions to JWTs
- new jwt.py module - holding our current JWT logic for unit testing 
- test_jwt.py created - our first series of tests specifically for JWT

Tests status: 0/3 passing [tool: read_file]
running the server on http://localhost:8000 

## In Progress
Continuing work with `auth` subdirectory to refactor session login flow to use JSON Web Token authentication instead. Testing integration into live application while updating unit test suites around this feature.

## Blocked
None at the moment, progressing smoothly through migration and testing phase currently

## Key Decisions 
Decided that it was best to create a new jwt.py module (separate from auth.py) holding all the logic related to creating, verifying and managing user JWTs. This allows us to keep our main authentication flow clean while having a separate module focusing on the JWT side of things for easier testing and maintenance.

## Resolved Questions
Q: What's the best way to validate our JWTs as users come back after caching or long inactivity?
A: Using the pyjwt library, we can write a `decode_jwt` function that fetches the user ID from the token payload and validates it against our current user table. This allows us to ensure the token is still valid for the user on both read/write operations.

## Pending User Asks
Q: Can you also modernize the password hashing functionality during this refactor?
A: Sure, we can research best practices in 2023 for secure password hashing. After deciding which algorithm aligns with our security needs, I'll put together a change plan to upgrade that as well.

## Remaining Work 
- Finish transitioning auth.py to use JWT
- Update login views to prompt for JWT instead of session cookies
- Integrate JWT functionality into the dashboard area requiring auth
- Write full unit testing coverage in test_jwt.py confirming all pieces work correctly
- Run through an end-to-end migration test from session-based auth to JWT, including invalidation and revocation flows 

## Critical Context  
No specific values or sensitive information needed here for context.

The core pieces: continuing with the auth refactor plan from last meeting. Progressing smoothly into JWT module setup in separate file from main auth flows. Working through tests and updating impacted views. Next steps are fully migrating over, then running thorough final validation phase before we sunset session-based auth entirely. 

Target 10000 more characters for continued detail, including outputs of commands like `make test` against new jwt.py, specifics on the error being thrown if any in our end to end flows testing JWT login vs session login across all views and functionality. Note anything that pops up during this migration phase to include in decision making next round.