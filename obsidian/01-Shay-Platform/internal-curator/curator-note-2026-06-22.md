---
permalink: famtastic/01-shay-platform/internal-curator/latest-curator-note-1
---

Generated: 2026-06-22

Sources:
- /Users/famtasticfritz/.local/share/famtastic/gaps.jsonl
- /Users/famtasticfritz/.local/share/famtastic/intelligence-promotions.json
- /Users/famtasticfritz/famtastic/obsidian/01-Shay-Platform/gap-research/latest-gap-research.md
- /Users/famtasticfritz/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md
- session_search: platform-conversation-context OR billing-usage-visibility OR godaddy-cpanel-access OR reseller account OR API Usage Watchdog

# Internal Curator Note

## New Observations
- **Observation:** `gaps.jsonl` is still tiny but noisy: 13 rows total, with the open set dominated by routing/process issues rather than net-new missing product capabilities.
- **Observation:** `platform-conversation-context` and `shay-desk-ui-kinks` remain the highest-frequency repeating buckets at 3 each; `video-background` remains a 2-hit bucket already marked as repo-proven but not operationally closed.
- **Observation:** `gap-research/latest-gap-research.md` confirms `billing-usage-visibility` is not a greenfield build gap; the API Usage Watchdog already exists and is paused/stale.
- **Observation:** That same research note says the old Telegram delivery blocker is no longer current reality; the route now exists, but the watchdog has not been re-verified/resumed.
- **Observation:** `godaddy-cpanel-access` is still best described as escalation-order drift plus manual-edge uncertainty, not total lack of platform knowledge.
- **Observation:** `intelligence-promotions.json` now shows 5 promoted findings, but every one of them is still `pending`; promotion has become visible, but closeout discipline still has not landed.
- **Observation:** GitHub bookmarks remain skewed toward `agent-orchestration` (94 unique URLs) over `business-income` (15), while the clearest money-linked artifacts still cluster around reseller/payment rails and practical revenue systems.

## Interpretations
- **Interpretation:** The loudest problem in Shay's intelligence loop is still type confusion. Platform work, media handoffs, client briefs, and reprioritization events are being mistaken for capability failures.
- **Interpretation:** The system is getting better at surfacing patterns, but not yet at converting surfaced patterns into resolved state. The loop is learning to notice, not yet learning to finish.
- **Interpretation:** Fritz's standing priorities are stable: revenue protection and income unlocks keep reappearing as the practical center of gravity, even while research energy continues to fan outward.
- **Interpretation:** `billing-usage-visibility` is a classic Shay pattern: the capability exists in partial form, then drifts because no automatic re-verification or artifact-backed proof loop closes it.
- **Interpretation:** The bookmarks ledger suggests Fritz is still investing heavily in orchestration techniques, but the backlog wants those techniques pressed into protected money lanes, not just explored.

## Repeating Gaps
- **Routing/classification gap:** distinguish `site-build`, `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt` before writing gap rows.
- **Promotion closeout gap:** promoted findings are created but not driven into `implemented`, `verified`, `deferred`, or `rejected` states.
- **Proof-closeout gap:** repo-proven capabilities like `video-background` remain open in the ledger because operational closure proof is missing.
- **Revenue observability gap:** billing/API spend visibility keeps surviving as “important later” even though the scaffolding already exists.
- **UI friction gap:** Shay Desk interaction complaints still sit as repeated historical evidence instead of a single owned remediation packet.

## Candidate Automations
- **Conversation-type gate:** a pre-gap classifier that blocks platform chatter, media requests, client briefs, and interrupts from becoming fake capability debt.
- **Promotion closer:** a scheduled worker that forces every long-pending promoted finding into an action state with owner, proof target, or explicit rejection.
- **Watchdog re-verifier:** a small cron that checks whether paused-but-proven automations now have valid delivery targets and can be safely resumed.
- **Proof/ledger reconciler:** when proof confidence is high or closeout state is `needs-operational-closeout`, generate a closure packet instead of leaving the row to rot.
- **Income-lane bookmark miner:** score business-income bookmarks into actionable modules: payment rails, reseller activation, ad/revenue tooling, lead-gen, and autonomous ops.

## Escalations Worth Building
- **Pre-escalation wrapper for vendor/account ambiguity:** make `repo truth -> capability matrix -> local tools -> vendor docs -> Fritz last` executable, not just doctrinal.
- **Revenue protection lane:** own the API Usage Watchdog, billing visibility, and income telemetry as one protected ops surface.
- **Media intake wrapper:** structured capture for provider, prompt, deliverable, references, consistency constraints, and intended downstream use.
- **Legacy gap hygiene wave:** bulk-close noise/mislogged rows only after the conversation-type gate is in place so cleanup is not instantly undone.

## What To Ignore
- Legacy malformed `capability_id` strings should not be treated as roadmap nouns once the real bucket is known.
- Bookmark volume alone should not dictate priority; it reflects curiosity and scanning behavior more than commitment.
- Old open rows should not be mistaken for current blockers without repo-truth or runtime re-verification.
- Duplicate complaints inside the same bucket should raise severity, not create parallel fake projects.

## Stable Preference / Environment Fact Worth Capturing Explicitly
- **Recommendation:** Capture as a durable user preference: Fritz wants escalation to him after internal proof surfaces are exhausted, not at the first sign of vendor/account uncertainty.
- **Recommendation:** Capture as a durable user preference: income-linked work should be turned into owned execution lanes early or it will keep dissolving back into ambient research.
- **Recommendation:** Capture as a stable environment fact: `latest-gap-research.md` is now a meaningful curator truth surface because it carries repo/runtime validation that raw gap rows do not.
- **Recommendation:** Capture as a stable environment fact: the current system weakness is not feature scarcity; it is routing drift and incomplete closeout between evidence, promotions, and execution.