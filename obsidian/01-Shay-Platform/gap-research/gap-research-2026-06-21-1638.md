---
title: gap-research-2026-06-21-1638
type: note
generated_at: 2026-06-21 16:38 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-21-1638
---

# Gap Research — 2026-06-21 16:38

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`, `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`, `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`, `~/famtastic/site-studio/lib/gap-logger.js`, `~/famtastic/SITE-LEARNINGS.md`, `~/.shay/scripts/usage_watchdog.sh`, current `shay cron list --all`, current Telegram channel directory, and session recall. Vendor-web fetch was attempted but the current Tavily backend returned HTTP 432, so this pass leans on local repo truth plus live runtime verification.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged as open while the existing watchdog path remains paused.
- **Why it matters:** This is wallet protection and provider-spend visibility. It directly supports Fritz’s efficiency-first hierarchy.
- **Recurring?** Yes. This gap keeps surviving repeated research passes without operational closeout.
- **Researched options:**
  1. Re-verify and resume the existing `API Usage Watchdog` cron instead of building a replacement.
  2. Keep a local artifact/report alongside Telegram delivery so telemetry proof survives delivery-route drift.
  3. Keep runtime usage telemetry separate from invoice/billing primitives.
- **Repo truth / live truth:**
  - `gaps.jsonl` still lists this capability as `status: "open"`.
  - `problem-buckets.latest.json` still lists `billing-usage-visibility` as an open `ops_gap`.
  - `shay cron list --all` at `2026-06-21 16:38 EDT` still shows `API Usage Watchdog` present but **paused**, with stale last-error text `no delivery target resolved for deliver=telegram`.
  - The same live cron output shows `morning-briefing-telegram-alert` active and successfully delivering to `telegram:Fritz Medine (dm)`.
  - `~/.shay/channel_directory.json` now contains a live Telegram DM target for Fritz, so the old watchdog delivery failure is almost certainly stale state, not a current platform limitation.
  - `~/.shay/scripts/usage_watchdog.sh` already implements the watchdog loop via `shay insights --days 1`, cost parsing, rate/error detection, and a `$10` threshold alert.
  - `SITE-LEARNINGS.md` proves cost telemetry already exists in Site Studio via `lib/api-telemetry.js` and `GET /api/telemetry/sdk-cost-summary`; the missing part is operationalizing the signal, not inventing telemetry from scratch.
- **Recommended next move:** Reclassify this as **implemented-but-paused operational infrastructure**. Verify the script against the now-valid Telegram route, resume the job, and add a local summary artifact so the proof does not depend on one delivery surface.
- **Fritz authority required?** No for verification/resume. Yes only if threshold policy or budget policy changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before the internal routing/proof ladder was exhausted.
- **Why it matters:** This burns Fritz attention on a cash-adjacent lane tied to reseller recovery and hosting operations.
- **Recurring?** Yes at the pattern level. The raw gap row is single-frequency, but the same confusion keeps reappearing in bucket guidance, docs, and recurring research output.
- **Researched options:**
  1. Split the problem into four lanes: reseller-account recovery, cPanel execution, registrar/DNS fallback, and escalation-order discipline.
  2. Add a pre-escalation wrapper/checklist that enforces `capability matrix -> agent tools -> vendor/repo truth -> Fritz last`.
  3. Default to cPanel UAPI/MCP as primary control plane and treat GoDaddy API as registrar/direct-DNS fallback only.
- **Repo truth / live truth:**
  - `gaps.jsonl` still lists `proactive-gap-escalation-order` as `status: "open"` under bucket `godaddy-cpanel-access`.
  - `problem-buckets.latest.json` still marks `godaddy-cpanel-access` open and recommends splitting vendor access, DNS operations, reseller recovery, and escalation order into separate proof-backed tracks.
  - The gap row itself already contains the correct `expected_sequence`; the missing piece is enforcement.
  - `SITE-LEARNINGS.md` states plainly: for the current GoDaddy-hosted stack, **cPanel UAPI/MCP is the primary hosting/DB/DNS control plane** and GoDaddy developer API keys are optional registrar/direct-DNS fallback, not the credential to chase by default.
  - `SITE-LEARNINGS.md` also preserves the known gap that DNS/addon-domain automation still needs cPanel UAPI/MCP wrapper coverage, which means the path forward is extending the cPanel lane, not escalating generic access uncertainty upward.
- **Recommended next move:** Treat this as a **routing-discipline gap**, not a pure access gap. Build the pre-escalation wrapper, split the work into the four lanes above, and only involve Fritz for reseller ownership/credential decisions he uniquely controls.
- **Fritz authority required?** Yes for reseller-account ownership or irreversible account decisions. No for the wrapper and routing logic.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as capability failures, producing malformed rows like `to` and `to_do_some_platform_work`.
- **Why it matters:** This pollutes the intelligence loop itself, inflates fake gap volume, and wastes future research cycles.
- **Recurring?** Yes, clearly recurring. Current promoted finding frequency is `3`, and the bucket remains open in the latest audit.
- **Researched options:**
  1. Add a conversation-type gate before generic gap logging.
  2. Route first-pass conversation classes such as `site-build`, `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt`.
  3. Backfill/cleanup old junk rows only after the logger can no longer recreate them.
- **Repo truth / live truth:**
  - `gaps.jsonl` still contains multiple rows in this bucket, including the still-open `special_case_for_platfor_convos` row.
  - `intelligence-promotions.json` already promotes `bucket-platform-conversation-context` and explicitly recommends distinguishing platform/orchestration conversations from site-build conversations before logging.
  - `problem-buckets.latest.json` still marks the bucket open at frequency `3`.
  - `site-studio/lib/gap-logger.js` still falls back to `const capId = details.capability_id || slugify(message.slice(0, 60));`, which is the direct source of malformed capability IDs when no explicit route/classification exists.
- **Recommended next move:** Build the conversation-type gate first. Once the logger can no longer regenerate junk rows, close or rewrite the legacy malformed rows.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- No material state change since the earlier same-day passes: the same three highest-value open gaps are still the ones worth attention.
- **Most actionable operational closeout:** `billing-usage-visibility`.
- **Most important business-path discipline fix:** `godaddy-cpanel-access`.
- **Most important recurring intelligence-loop fix:** `platform-conversation-context`.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/SITE-LEARNINGS.md`
- `~/.shay/scripts/usage_watchdog.sh`
- `~/.shay/channel_directory.json`
- `shay cron list --all`
- Session recall via `session_search`