---
title: gap-research-2026-06-22-0935
type: note
generated_at: 2026-06-22 09:35 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-22-0935-1
---

# Gap Research — 2026-06-22 09:35

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, `obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`, `obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`, prior gap-research artifacts, live cron state, repo truth, and session recall.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged open even though the watchdog path already exists; the gap is operational closeout drift, not greenfield capability absence.
- **Why it matters:** This is direct cost protection and should be one of the easiest open gaps to close with proof.
- **Recurring?** **Yes.** This same stale-paused watchdog pattern has now survived multiple gap-research runs.
- **Researched options:**
  1. Resume the existing `API Usage Watchdog` instead of creating a new observability project.
  2. Keep Telegram delivery, but add a local proof artifact/log so delivery drift does not erase operational evidence.
  3. Split API-spend visibility from broader billing/invoicing work so the small fix can close fast.
- **Repo truth / live truth:**
  - `shay cron list --all` at `2026-06-22 09:35 EDT` still shows **API Usage Watchdog** (`8bc7abc447b2`) present but **paused** with old failure text: `no delivery target resolved for deliver=telegram`.
  - The same cron output shows `morning-briefing-telegram-alert` active and delivering to `telegram:Fritz Medine (dm)`.
  - `~/.shay/channel_directory.json` now contains Fritz’s Telegram DM target and was updated at `2026-06-22T09:27:56.687809`.
  - `~/.shay/scripts/usage_watchdog.sh` already implements the core logic via `shay insights --days 1`, cost parsing, and a `$10` threshold alert.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `billing-usage-visibility` as an open `ops_gap` with only one weak external candidate, which is more evidence that the missing move is operational re-verification, not repo shopping.
- **Recommended next move:** Reclassify this as **implemented-but-paused operational infrastructure**. Re-verify the watchdog against the now-populated Telegram route, resume the cron, capture fresh proof, then close or downgrade the gap row.
- **Fritz authority required?** No for verification/resume. Yes only if spend threshold or budget policy changes.
- **Confidence:** High

### 2) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as site-build capability failures, producing malformed rows like `to`, `special_case_for_platfor_convos`, and `to_do_some_platform_work`.
- **Why it matters:** This corrupts the intelligence loop itself. The system is manufacturing fake gap volume and then spending research cycles cleaning up its own logging noise.
- **Recurring?** **Yes, clearly recurring.** The promoted finding remains pending at frequency `3`, and earlier artifacts keep surfacing the same bucket.
- **Researched options:**
  1. Add a conversation-type gate before `logGap()` so platform/orchestration turns never hit generic site-gap fallback logic.
  2. Stop new malformed rows first, then bulk-clean legacy rows.
  3. Reuse the same upstream routing guard for adjacent cleanup buckets like `media-prompt-handoff`, `client-brief-mislogged-as-gap`, and `priority-interrupt-mislogged-as-gap`.
- **Repo truth / live truth:**
  - `site-studio/lib/gap-logger.js` still falls back to `const capId = details.capability_id || slugify(message.slice(0, 60));`, which is the direct malformed-row generator.
  - `~/.local/share/famtastic/intelligence-promotions.json` already promotes `bucket-platform-conversation-context` and explicitly recommends distinguishing platform/orchestration conversations from site-build conversations before logging.
  - `scripts/intelligence/normalize_solution_map.py` codifies this as a canonical bucket with the same recommendation, so the issue is already understood structurally.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `platform-conversation-context` as an open `routing_gap` with `candidate_count: 0`, confirming this is a local routing/design fix rather than an external-tool gap.
- **Recommended next move:** Build the conversation-type gate in front of gap logging, then bulk-clean legacy malformed rows after new row generation is blocked.
- **Fritz authority required?** No
- **Confidence:** High

### 3) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before the internal routing lattice and existing hosting-control-plane truth were fully exhausted.
- **Why it matters:** This sits on the reseller/income path and burns Fritz attention on ambiguity that should usually be resolved with repo truth, platform truth, and tool proof first.
- **Recurring?** **Yes at the bucket level.** The exact row is newer, but GoDaddy/cPanel/reseller routing confusion keeps resurfacing in adjacent proof and planning artifacts.
- **Researched options:**
  1. Split this into three lanes: reseller business reactivation, cPanel/DNS execution, and escalation-order discipline.
  2. Add a pre-escalation wrapper/checklist enforcing `capability matrix -> local tools/proof -> vendor/repo truth -> Fritz last`.
  3. Treat GoDaddy developer API as fallback truth only when cPanel UAPI/MCP proof is insufficient.
- **Repo truth / live truth:**
  - The gap row itself already encodes the expected sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `platform/README.md` says the current GoDaddy-hosted stack uses **cPanel UAPI/MCP as the primary hosting/DB/DNS control plane**, with GoDaddy developer API as optional fallback.
  - `plans/five-stream-state-2026-06-09.md` records current reseller reality: auth works, `/v1/orders` works, while `/v1/customers` returns `401` and `/v1/reseller/orders` returns `404`.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `godaddy-cpanel-access` as an open `execution_routing_gap` with only one weak external candidate, which again points to an internal routing/proof-order fix, not missing external research.
- **Recommended next move:** Treat this as a **routing-discipline gap with manual execution edges**, not as a blank-slate access mystery. Build the pre-escalation wrapper/checklist and reserve Fritz escalation for credentials, authority, or irreversible reseller decisions.
- **Fritz authority required?** Yes for reseller-account decisions/credentials. No for the routing wrapper and proof-first sequencing.
- **Confidence:** High

## Bottom line
- **Most actionable closeout:** `billing-usage-visibility` — existing path, Telegram target now present, gap still open only because the watchdog remains paused.
- **Most important anti-noise fix:** `platform-conversation-context` — still recurring and still corrupting the ledger.
- **Most important income-path discipline fix:** `godaddy-cpanel-access` — routing/proof order still needs hardening more than fresh repo research.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/01-Shay-Platform/gap-research/latest-gap-research.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/scripts/intelligence/normalize_solution_map.py`
- `~/famtastic/platform/README.md`
- `~/famtastic/plans/five-stream-state-2026-06-09.md`
- `~/.shay/scripts/usage_watchdog.sh`
- `~/.shay/channel_directory.json`
- `shay cron list --all`
- `session_search` recall for recurring buckets