---
title: latest-gap-research
type: note
generated: 2026-06-19 10:35 EDT
source: cron gap-research worker
permalink: famtastic/01-shay-platform/gap-research/latest-gap-research-3
---

# Gap Research — 2026-06-19 10:35

## Summary

Still no real priority shift. The same leverage cluster is open: **premature escalation on GoDaddy/cPanel work, reseller reactivation still modeled too broadly, and JJ BA still carrying urgency without a resumable delivery brief**. That is recurring signal, not noise.

---

## 1) `proactive-gap-escalation-order`
- **capability_id:** `proactive-gap-escalation-order`
- **observed failure:** `gaps.jsonl` has a fresh open workflow gap from 2026-06-19: GoDaddy/cPanel uncertainty was escalated to Fritz before internal capability surfaces were exhausted.
- **why it matters:** This is a Shay-core orchestration miss. It burns Fritz attention exactly where the platform should absorb ambiguity.
- **researched options:**
  1. **Repo truth is already cPanel-first.** `platform/README.md` says DNS/control for the current GoDaddy-hosted stack is **cPanel UAPI/MCP primary**, with GoDaddy direct DNS as optional fallback.
  2. **The real missing piece is narrower than “hosting access unknown.”** `SITE-LEARNINGS.md` says the biggest factory gap is missing addon-domain automation via cPanel `AddonDomain` / `addaddondomain`; DNS CRUD is not the whole blocker.
  3. **Truth-surface drift is part of the problem.** `SITE-LEARNINGS.md` still points to `docs/operating-rules/godaddy-mcp-spike.md`, but that file is not present now. Agents can still recover the truth, but they have to dig.
  4. **External vendor surface check:** `developer.godaddy.com/doc` is live, confirming the vendor surface exists, but the local repo already contains the operational truth needed for triage.
- **recommended next move:**
  - Build a small **GoDaddy-hosted stack triage** skill/checklist: `(a) cPanel-first check → (b) vault credential check → (c) wrapper coverage/manual fallback check → (d) owner-login-only escalation last`.
  - Add a direct truth-surface note pointing at the current addon-domain gap so agents stop framing this as generic GoDaddy confusion.
- **Fritz authority required:** No for checklist/truth-surface cleanup. Yes only if the path truly bottoms out in owner login, consent, or 2FA.
- **confidence:** High
- **recurring?:** Yes. Fresh gap row today, and the same control-plane confusion appears across prior same-day research artifacts.

---

## 2) `reseller-management`
- **capability_id:** `reseller-management`
- **observed failure:** `capability-engine/GAP-TRACKER.yaml` and `CAPABILITY-MATRIX.yaml` still mark reseller management as a critical open gap, but the resolution plan is still bundled as one giant lane: reactivate account, integrate API, set pricing, hosting API, billing.
- **why it matters:** Fritz’s own framing still stands: this is the fastest live income unlock. Leaving it monolithic slows the only gap already labeled “30-min cash unlock.”
- **researched options:**
  1. **Repo truth says auth is not the whole problem anymore.** `plans/five-stream-state-2026-06-09.md` says reseller auth works.
  2. **API reality is more limited than the gap wording suggests.** `obsidian/02-Income/FAMtastic-Hosting-Backend-Plan.md` records `/v1/orders` working, `/v1/customers` returning 401 with sso-key, and `/v1/reseller/orders` returning 404.
  3. **That means the next move is ops-first, not API-first.** The account can already support pricing/storefront/reporting work even if customer/reseller endpoints are incomplete or differently authenticated.
  4. **Strategic rule is already documented.** `plans/five-stream-overview-2026-06-09.md` says GoDaddy is backend wholesale only; customers should route through FAMtasticDesigns, not the raw reseller storefront.
- **recommended next move:**
  - Split the gap into two modules:
    1. **Reseller ops reactivation** — storefront/pages, pricing, products, payout visibility, handoff flow.
    2. **API feasibility audit** — which live endpoints are truly usable beyond `/v1/orders` and domain reads.
  - Do **not** keep treating “reactivation” as blocked on full API completion.
- **Fritz authority required:** Likely yes for owner-only settings, payouts, and business policy. No for module split and prep artifacts.
- **confidence:** High
- **recurring?:** Yes. It is still open in canonical capability truth and still echoed in hot-context as the priority cash unlock.

---

## 3) `jj-ba-pipeline-proof`
- **capability_id:** `jj-ba-pipeline-proof`
- **observed failure:** JJ BA remains flagged as personal/income priority in multiple truth surfaces, but the execution state is still loose. `plans/five-stream-overview-2026-06-09.md` literally says: **repo exists, no task docs, embarrassingly basic output, never pushed live**.
- **why it matters:** This is not just another site. It is a relationship-repair job *and* the cleanest full-pipeline proof candidate: domain → design → staging → client review → deploy → payment.
- **researched options:**
  1. **The delivery shape already exists conceptually.** `plans/three-lines-pipeline-2026-06-09.md` lays out the exact seven-step pipeline test for JJ BA.
  2. **What is missing is the resumable execution surface.** There is priority language, but not a dedicated brief/task packet driving the work session-by-session.
  3. **This gap is recurring in intent, even if not yet logged in `gaps.jsonl`.** Hot-context, five-stream state, and pipeline plan all keep pointing back to JJ BA as a priority proof lane.
  4. **Dependency truth is now clearer.** Since reseller auth appears to work and GoDaddy is backend-only, JJ BA can be briefed around staged delivery even before every domain/provisioning automation edge is perfect.
- **recommended next move:**
  - Promote JJ BA into a **real resumable brief** with explicit tasks, blockers, proof, and which steps can run before domain purchase automation is fully polished.
  - Treat it as the proving ground for the reseller/domain/design/deploy/payment chain, not as a vague “important later” item.
- **Fritz authority required:** Yes for client-facing decisions, outreach timing, and any live commercial step. No for producing the brief and execution packet.
- **confidence:** Medium-high
- **recurring?:** Yes. The same urgency appears in multiple current-state documents, but it still has not been converted into a durable execution surface.

---

## Observation vs Interpretation

### Observations
- `intelligence-promotions.json` is still absent.
- The only fresh explicit gap row in `gaps.jsonl` is the GoDaddy/cPanel escalation-order workflow gap.
- `platform/README.md` remains cPanel-first for hosting control.
- `SITE-LEARNINGS.md` still names missing cPanel addon-domain automation as the biggest factory gap.
- Reseller API truth in local docs is more constrained than generic “integrate API” wording suggests: `/v1/orders` works; `/v1/customers` and `/v1/reseller/orders` do not currently behave as hoped.
- JJ BA still appears as a priority proof lane across planning truth surfaces, but not as a tight resumable execution brief.

### Interpretation
- The system is still over-framing hosting/revenue gaps as giant GoDaddy problems when the sharper truth is: **cPanel provisioning gap + reseller ops gap + execution-brief gap**.
- The next leverage move is not more broad research. It is converting repeated understanding into tighter operating surfaces: triage checklist, split reseller modules, JJ BA brief.

---

## Recommended next research packet

1. Build the **GoDaddy-hosted stack triage** skill/checklist from current repo truth.
2. Rewrite **reseller-management** as ops-first vs API-feasibility modules.
3. Draft a **JJ BA resumable brief** so the priority stops living only as emotionally correct context.