---
title: gap-research-2026-06-22-0730
type: note
generated_at: 2026-06-22 07:30 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-22-0730-1
---

# Gap Research — 2026-06-22 07:30

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, `obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`, `obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`, prior gap-research artifacts, current cron state, repo truth, and session recall.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged open while the watchdog path already exists and remains paused behind an old Telegram delivery failure.
- **Why it matters:** This is direct spend protection. The capability is operationally valuable, already partially built, and still not closed out.
- **Recurring?** **Yes, clearly recurring.** This same stale-paused pattern has shown up across repeated gap-research runs and remains unresolved.
- **Researched options:**
  1. Resume the existing `API Usage Watchdog` instead of designing a new billing/observability surface.
  2. Keep push delivery, but add a local artifact/log summary so proof survives channel drift.
  3. Treat invoice/payment-provider work as separate from API-usage watchdog work.
- **Repo truth / source trace:**
  - `shay cron list --all` at `2026-06-22 07:30 EDT` still shows **API Usage Watchdog** (`8bc7abc447b2`) present but **paused**, with stale failure text `no delivery target resolved for deliver=telegram`.
  - The same cron output shows `morning-briefing-telegram-alert` is active and delivering to `telegram:Fritz Medine (dm)`.
  - `~/.shay/channel_directory.json` now contains Fritz’s Telegram DM target, updated `2026-06-22T07:24:36.049274`.
  - `~/.shay/scripts/usage_watchdog.sh` already implements the core loop via `shay insights --days 1`, cost parsing, error parsing, and a `$10` threshold alert.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `billing-usage-visibility` as an open `ops_gap` with only one weak repo candidate, which means no new external repo is needed for the core fix.
- **Recommended next move:** Reclassify this as **implemented-but-paused operational infrastructure**. Re-verify the watchdog against the current Telegram route, resume the cron, and then close or downgrade the gap once fresh proof exists.
- **Fritz authority required?** No for verification/resume. Yes only if spend threshold or budget policy changes.
- **Confidence:** High

### 2) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged like site-build capability failures, producing junk rows such as `to`, `special_case_for_platfor_convos`, and `to_do_some_platform_work`.
- **Why it matters:** This pollutes the intelligence loop itself. Bad rows distort prioritization, make recurring-gap analysis noisy, and waste future worker cycles.
- **Recurring?** **Yes, explicitly recurring.** The promoted bucket frequency is `3`, the finding remains `pending`, and repeated research artifacts keep surfacing the same issue.
- **Researched options:**
  1. Add a conversation-type gate before gap logging so platform/orchestration turns never fall through to generic site-gap logging.
  2. Stop new malformed rows first, then bulk-clean legacy rows.
  3. Extend the same upstream routing guard to nearby buckets like `media-prompt-handoff`, `client-brief-mislogged-as-gap`, and `priority-interrupt-mislogged-as-gap`.
- **Repo truth / source trace:**
  - `~/.local/share/famtastic/intelligence-promotions.json` already promotes `bucket-platform-conversation-context` with the recommendation to distinguish platform/orchestration conversations from site-build conversations before logging.
  - `site-studio/lib/gap-logger.js` still falls back to `slugify(message.slice(0, 60))` when no explicit `capability_id` is supplied. That fallback remains the direct malformed-row generator.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `platform-conversation-context` as an open `routing_gap` with `candidate_count: 0`, confirming this is a local routing/design gap rather than a missing external repo.
  - Prior cleanup/promotion surfaces already bucket the bad rows correctly after the fact, so the missing piece is upstream detection, not more cleanup logic.
- **Recommended next move:** Build the conversation-type gate in front of gap logging, then bulk-clean the legacy malformed rows once new row generation is blocked.
- **Fritz authority required?** No
- **Confidence:** High

### 3) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before local capability surfaces and proof-backed routing were exhausted.
- **Why it matters:** This touches the reseller/income path and burns Fritz attention on ambiguity that should be resolved inside the platform first.
- **Recurring?** **Yes at the bucket level.** The exact row is newer, but the reseller vs cPanel vs DNS routing confusion keeps resurfacing in planning and proof surfaces.
- **Researched options:**
  1. Split the concern into three lanes: reseller business reactivation, cPanel/DNS execution, and escalation-order discipline.
  2. Add a pre-escalation wrapper/checklist enforcing `capability matrix -> local tools/proof -> vendor/repo truth -> Fritz last`.
  3. Treat GoDaddy developer API as fallback truth when cPanel UAPI/MCP proof already exists.
- **Repo truth / source trace:**
  - The gap row itself encodes the expected sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `godaddy-cpanel-access` as an open `execution_routing_gap` with only one weak external candidate.
  - `platform/README.md` states the current GoDaddy-hosted stack uses **cPanel UAPI/MCP as the primary hosting/DB/DNS control plane** and GoDaddy developer API as optional fallback.
  - `plans/five-stream-state-2026-06-09.md` records current reseller reality: auth works, `/v1/orders` works, while `/v1/customers` returns `401` and `/v1/reseller/orders` returns `404`.
  - Existing proof artifacts already document successful cPanel-backed execution patterns for DNS/vhost/backend deploy work, so this is not a blank-slate capability hole.
- **Recommended next move:** Treat this as a **routing-discipline gap with manual execution edges**, not an access-mystery gap. Build the pre-escalation wrapper/checklist and keep Fritz escalation limited to credentials, authority, or irreversible reseller decisions.
- **Fritz authority required?** Yes for reseller-account decisions/credentials. No for the routing wrapper and proof-first sequencing.
- **Confidence:** High

## Bottom line
- **Most actionable closeout:** `billing-usage-visibility` — existing path, stale-paused state, no new architecture needed.
- **Most important anti-noise fix:** `platform-conversation-context` — still recurring and still corrupting the ledger.
- **Most important income-path discipline fix:** `godaddy-cpanel-access` — routing/proof order still needs hardening.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/01-Shay-Platform/gap-research/latest-gap-research.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/platform/README.md`
- `~/famtastic/plans/five-stream-state-2026-06-09.md`
- `~/.shay/scripts/usage_watchdog.sh`
- `~/.shay/channel_directory.json`
- `shay cron list --all`
- `session_search` recall for recurring buckets