---
title: gap-research-2026-06-19-0212
type: note
generated: 2026-06-19 02:12 EDT
generated_iso: 2026-06-19T02:12:26-0400
source_files:
- /Users/famtasticfritz/.local/share/famtastic/gaps.jsonl
- /Users/famtasticfritz/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md
- /Users/famtasticfritz/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md
- /Users/famtasticfritz/famtastic/platform/README.md
- /Users/famtasticfritz/famtastic/platform/registry/capabilities.json
- /Users/famtasticfritz/famtastic/components/video-hero/component.json
- /Users/famtasticfritz/famtastic/components/library.json
vendor_refs:
- https://api.docs.cpanel.net/cpanel-api-2/cpanel-api-2-modules-zoneedit/cpanel-api-2-functions-zoneedit-add_zone_record
- https://api.docs.cpanel.net/cpanel-api-2/cpanel-api-2-modules-zoneedit/cpanel-api-2-functions-zoneedit-fetchzone_records
- https://swarmify.com/blog/how-to-make-an-autoplaying-background-video
permalink: famtastic/01-shay-platform/gap-research/latest-gap-research-1
---

# Gap Research Report

## Scan summary
- Open gap log entries found: 13
- Intelligence promotions file: missing (`/Users/famtasticfritz/.local/share/famtastic/intelligence-promotions.json`)
- Highest-value open gaps researched this run: 3
- Selection logic: one fresh orchestration miss, one clearly recurring product capability gap, and one hot-context execution gap still missing a resumable surface
- Recurrence note: this run largely confirms the same top three items as the 00:09 report; no stronger replacement gap surfaced

## 1) `proactive-gap-escalation-order`
- **Observed failure:** Gap log entry `gap-1781840661232` says GoDaddy/cPanel access uncertainty was escalated to Fritz before the internal routing lattice was exhausted.
- **Why it matters:** This is a direct Fritz-load violation. It burns trust, breaks the “escalate last” doctrine, and turns a solvable platform ambiguity into a human interruption.
- **Repo truth:** Doctrine is already right; execution still has a real hole.
  - `platform/README.md` says cPanel UAPI/MCP is the primary hosting/DB/DNS control plane and GoDaddy API is optional fallback.
  - `platform/registry/capabilities.json` still marks `dns.register-subdomain` as `v1-manual-prompt` with manual step `cPanel/GoDaddy Zone Editor UI until cPanel DNS wrapper is added`.
  - Existing proof artifacts already record that Studio can use cPanel auth and should prefer it.
- **Vendor/doc research:** cPanel exposes ZoneEdit add/edit/fetch APIs, so the missing piece is not “can cPanel do it?” but “has FAMtastic wrapped it yet?” Vendor docs confirm the wrapper path is viable.
- **Researched options:**
  1. Build a thin `platform dns` wrapper around cPanel ZoneEdit/UAPI calls and stop treating DNS as an unknown.
  2. Add a pre-escalation checklist for hosting/auth incidents: capability registry → service proof search → vault/service refs → vendor doc lookup → only then escalate.
  3. Add a lint/guard in the gap workflow that flags “ask Fritz” when local proof already shows cPanel credentials/control-plane availability.
- **Recommended next move:** Build the cPanel DNS wrapper first. That is the cleanest leverage point because it removes the ambiguity that triggered the bad escalation behavior.
- **Fritz authority required:** No for research/spec/implementation prep. Yes only if runtime credentials are actually absent or expired.
- **Confidence:** High
- **Recurring?:** **Yes in root cause.** The exact gap record is new, but the cPanel-vs-GoDaddy ambiguity is explicitly recurring in repo truth.

## 2) `video_background_option` + `video_background_support`
- **Observed failure:** Two separate open gap records from `site-altitude` ask for video background capability.
- **Why it matters:** This is a visible FAMtastic-style differentiator, and the duplicate records mean the system still thinks this is missing.
- **Repo truth:** This looks more like stale/open-loop state than a blank capability hole.
  - `components/video-hero/component.json` defines a reusable video hero with required video slot and poster fallback.
  - `components/library.json` includes `video-hero` in the component catalog.
  - `site-studio/lib/famtastic-skeletons.js` includes a `VIDEO_HERO_SKELETON` and explicitly says the video will be auto-generated and injected after build completes.
- **Vendor/doc research:** Current browser guidance still matches the component pattern already in repo: background video should be muted, looping, and include inline/mobile fallback behavior. The existing component already assumes poster fallback, which is the right direction.
- **Researched options:**
  1. Run an end-to-end proof from brief → hero selection → rendered output and resolve both old gaps if it passes.
  2. If proof fails, narrow the real gap: prompt routing, asset injection, or mobile fallback.
  3. Promote video background to an explicit brief/build switch instead of leaving it as an implicit skeleton-side behavior.
- **Recommended next move:** Treat this as a **verification-and-surfacing gap**. Do not rebuild the capability from scratch until the existing `video-hero` path is proven broken.
- **Fritz authority required:** No
- **Confidence:** Medium-high
- **Recurring?:** **Yes, explicitly.** Two open gap records describe the same missing outcome.

## 3) `jj-ba-transport-tasking-gap`
- **Observed failure:** HOT context still says “JJ BA Transport has no task docs or status — gap.” Search confirms the project exists across multiple repo surfaces but still lacks a clean current resumable brief.
- **Why it matters:** This is income-adjacent and reputation-adjacent work. It is also a clean pipeline-proof candidate, so lack of task shape blocks both delivery and system learning.
- **Repo truth:** The asset/history exists; the active execution surface does not.
  - `famtastic-dna.md` records multiple JJ BA builds.
  - `command-center/state.json` shows `site-jj-ba-transport` and `site-jj-ba-transport2` in both studio-sandbox and deploy-repo.
  - Income research surfaces explicitly name JJ BA as one of the cleanest candidate sites for packaging/monetization.
  - Search did **not** surface a dedicated current brief in the repo’s default resumable shape.
- **Researched options:**
  1. Create a minimal resumable brief now so the site stops living as scattered mentions.
  2. If JJ BA becomes a pipeline proof, promote it into a richer control-plane packet with proof targets and landing path.
  3. Tie it to the marketplace packaging lane after the brief exists, since the income research already points there.
- **Recommended next move:** Create the resumable brief first. Without that, every future JJ BA push will re-fragment into context residue.
- **Fritz authority required:** No for the brief. Yes later for client-facing approval/deploy/payment calls.
- **Confidence:** High
- **Recurring?:** **Yes in practice.** JJ BA repeatedly appears in plans, state, and research, but still has no durable execution lane.

## Overall recommendations
1. **Close the orchestration hole in code:** wrap cPanel DNS/ZoneEdit and stop forcing human escalation for a known platform gap.
2. **Proof before rebuild:** the video background issue looks stale or poorly surfaced, not absent.
3. **Turn JJ BA into a real brief:** it is the clearest operational unlock still sitting in hot-context limbo.

## Notes
- `intelligence-promotions.json` is still missing, so promotion-state filtering was not possible this run.
- No evidence found that these gaps were resolved after the earlier 00:09 report.
- If a follow-up cleanup worker runs later, the fastest admin win is resolving stale gap records once proof exists.