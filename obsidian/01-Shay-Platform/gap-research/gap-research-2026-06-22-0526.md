---
title: gap-research-2026-06-22-0526
type: note
generated_at: 2026-06-22 05:26 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-22-0526-1
---

# Gap Research — 2026-06-22 05:26

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, `obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`, `obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`, prior gap-research artifacts, current cron state, repo truth, and session recall.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged as open while the existing watchdog path remains paused behind an old delivery failure.
- **Why it matters:** This is direct spend protection and supports the efficiency-first rule. Silent provider drain is an avoidable tax.
- **Recurring?** **Yes.** This is the same stale-paused pattern already surfaced in prior gap-research artifacts and it is still unresolved.
- **Researched options:**
  1. Resume the existing `API Usage Watchdog` instead of building a new telemetry surface.
  2. Add a local artifact/log summary alongside Telegram delivery so proof survives channel drift.
  3. Keep invoice/billing primitives separate from runtime usage observability.
- **Repo truth / source trace:**
  - `shay cron list --all` at `2026-06-22 05:26 EDT` still shows **API Usage Watchdog** (`8bc7abc447b2`) present but **paused**, with stale failure text `no delivery target resolved for deliver=telegram`.
  - The same cron output shows `morning-briefing-telegram-alert` is active and delivering to `telegram:Fritz Medine (dm)`, which means the old Telegram-route blocker is no longer current system reality.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `billing-usage-visibility` as an open `ops_gap` with one weak candidate and no closeout.
  - Prior artifact `obsidian/01-Shay-Platform/gap-research/latest-gap-research.md` already identified this as implemented-but-paused; no operational closeout has happened since.
- **Recommended next move:** Reclassify this as **implemented-but-paused operational infrastructure**. Re-verify the watchdog against the current Telegram route, resume the cron, then close or downgrade the gap record once proof exists.
- **Fritz authority required?** No for verification/resume. Yes only if spend threshold or budget policy changes.
- **Confidence:** High

### 2) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged like site-build capability failures, producing junk rows such as `to`, `special_case_for_platfor_convos`, and `to_do_some_platform_work`.
- **Why it matters:** This corrupts the intelligence loop itself. Fake capability debt poisons research prioritization and wastes future worker cycles.
- **Recurring?** **Yes, clearly recurring.** Promotions show frequency `3`, the bucket is still pending, and the same diagnosis appears across repeated cron summaries.
- **Researched options:**
  1. Add a conversation-type gate before gap logging so `platform-work`, `brief-intake`, `media-handoff`, and `priority-interrupt` turns never fall into generic site-gap logging.
  2. Stop new malformed rows first, then run cleanup/backfill on the legacy rows.
  3. Promote routing findings into implementation work instead of leaving them as perpetual pending research.
- **Repo truth / source trace:**
  - `~/.local/share/famtastic/intelligence-promotions.json` already promotes `bucket-platform-conversation-context` with the exact recommendation to distinguish platform/orchestration conversations from site-build conversations before logging.
  - `site-studio/lib/gap-logger.js` still falls back to `slugify(message.slice(0, 60))` when no explicit `capability_id` is supplied. That fallback remains the direct malformed-row generator.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `platform-conversation-context` as an open `routing_gap` with `candidate_count: 0`, which confirms this is a local routing/design problem, not a missing external repo.
  - `scripts/intelligence/cleanup_gap_rows.py` and `scripts/intelligence/normalize_solution_map.py` already know how to bucket these rows after the fact, so the missing piece is upstream detection, not more cleanup logic.
- **Recommended next move:** Build the conversation-type gate in front of gap logging, then bulk-clean the legacy malformed rows once regeneration is impossible.
- **Fritz authority required?** No
- **Confidence:** High

### 3) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before local capability surfaces and proof-backed routing were fully exhausted.
- **Why it matters:** This touches the reseller/cash path. Bad escalation order burns Fritz attention on fog instead of progress.
- **Recurring?** **Yes at the bucket level.** The exact row is newer, but the underlying reseller vs cPanel vs DNS routing confusion keeps resurfacing.
- **Researched options:**
  1. Split the problem into separate lanes: reseller reactivation, cPanel/DNS execution, and escalation-order discipline.
  2. Add a pre-escalation wrapper enforcing `capability matrix -> local tools/proof -> vendor truth -> Fritz last`.
  3. Treat GoDaddy developer API as fallback truth when cPanel proof already exists.
- **Repo truth / source trace:**
  - The gap row itself encodes the expected sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `godaddy-cpanel-access` as an open `execution_routing_gap` with only a weak external candidate and no closeout.
  - `SITE-LEARNINGS.md` states that for the current GoDaddy-hosted stack, **cPanel UAPI/MCP is the primary hosting/DB/DNS control plane** and GoDaddy developer API keys are optional fallback, not the default credential to chase.
  - `scripts/intelligence/normalize_solution_map.py` already aliases `godaddy`, `cpanel`, `dns`, `reseller`, and `proactive-gap-escalation-order` into the same bucket, which confirms this is recognized internally as a routing problem.
- **Recommended next move:** Treat this as a **routing-discipline gap with manual execution edges**, not an access-mystery gap. Build the pre-escalation wrapper/checklist and keep Fritz escalation limited to credentials, authority, or irreversible vendor decisions.
- **Fritz authority required?** Yes for reseller-account decisions/credentials. No for the routing wrapper and proof-first sequencing.
- **Confidence:** High

## Bottom line
- **Most actionable closeout:** `billing-usage-visibility` — existing path, stale-paused state, no new architecture needed.
- **Most important anti-noise fix:** `platform-conversation-context` — still recurring and still polluting the ledger.
- **Most important income-path discipline fix:** `godaddy-cpanel-access` — routing/proof order still needs hardening.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/01-Shay-Platform/gap-research/latest-gap-research.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/scripts/intelligence/cleanup_gap_rows.py`
- `~/famtastic/scripts/intelligence/normalize_solution_map.py`
- `~/famtastic/SITE-LEARNINGS.md`
- `shay cron list --all`
- `session_search` recall for recurring buckets