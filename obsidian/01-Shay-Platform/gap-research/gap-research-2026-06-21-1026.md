---
title: gap-research-2026-06-21-1026
type: note
generated_at: 2026-06-21 10:26 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-21-1026-1
---

# Gap Research — 2026-06-21 10:26

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, hot-context pointers, latest bookmark research, current curator note, current problem verdicts, cron/channel truth, repo truth, and session recall.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility was requested, but the always-on visibility loop is still paused and still not treated as a control-plane invariant.
- **Why it matters:** This protects spend, catches silent provider drain, and supports Fritz’s efficiency-first hierarchy. It is an ops gap tied directly to money and brain-budget protection.
- **Recurring?** Yes. The bucket keeps reappearing across prior gap-research artifacts and the current curator note still names revenue observability as an unresolved repeating gap.
- **Researched options:**
  1. Restore the existing watchdog instead of designing a new system.
  2. Promote watchdog output from “one cron job” into a local artifact plus push-delivery pattern so observability survives channel drift.
  3. Keep billing/invoice generation separate from usage telemetry; both matter, but they are not the same capability.
- **Repo truth:**
  - `shay cron list --all` still shows `API Usage Watchdog` present, paused, and pointed at `usage_watchdog.sh`.
  - The old failure mode is still recorded as `no delivery target resolved for deliver=telegram`.
  - `~/.shay/channel_directory.json` now shows a live Telegram DM target for Fritz, and `shay cron list --all` shows `morning-briefing-telegram-alert` actively delivering to `telegram:Fritz Medine (dm)`. That means Telegram delivery works now; the watchdog is stale-paused, not fundamentally blocked.
  - `~/.shay/cron/jobs.json` confirms the watchdog job remains paused with the old delivery error still attached.
  - `platform/registry/capabilities.json` plus `platform/capabilities/billing/*` prove billing primitives exist, but those artifacts stop at invoice creation/manual payment handling and do not provide runtime usage observability.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `billing-usage-visibility` as an open `ops_gap`.
- **Recommended next move:** Reclassify this as **implemented-but-paused operational infrastructure**. Resume by re-verifying `usage_watchdog.sh` against the now-valid Telegram route, then add a local summary artifact so the signal survives even if push delivery drifts again.
- **Fritz authority required?** No for re-verification/unpause. Yes only if threshold policy, provider budget policy, or paid observability tooling changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated to Fritz before the internal routing/proof surfaces were exhausted.
- **Why it matters:** This touches reseller/account-access work, which sits near the cash-unlock lane. Bad escalation order burns Fritz attention and slows execution.
- **Recurring?** Yes. The exact row is single-frequency, but the underlying confusion between reseller recovery, GoDaddy API, and cPanel execution keeps resurfacing in proofs, capability docs, and prior gap reports.
- **Researched options:**
  1. Split the problem into three lanes: reseller reactivation, cPanel/DNS execution, and escalation-order discipline.
  2. Build a pre-escalation wrapper that checks capability registry, proof artifacts, vault/provider facts, and local tools before surfacing uncertainty.
  3. Treat GoDaddy API as optional fallback truth when cPanel UAPI/MCP proof already exists.
- **Repo truth:**
  - The gap row itself already encodes the expected sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `platform/README.md` states the GoDaddy-hosted stack uses **cPanel UAPI/MCP as the primary hosting/DB/DNS control plane** and GoDaddy developer API as optional fallback.
  - `platform/registry/capabilities.json` documents `platform dns register-subdomain` as a cPanel UAPI / GoDaddy Zone Editor path, with cPanel primary.
  - `proofs/studio-service-auth-mbsh-reunion-2026-05-05.json` and `proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json` both state: `Use cPanel UAPI/MCP for DNS/hosting operations; GoDaddy API is optional fallback`.
  - `proofs/mbsh-db-apply-schema-2026-05-05-final.log` and `proofs/mbsh-deploy-backend-2026-05-05-cpanel-uapi-overwrite.log` prove cPanel fallback paths were used successfully when SSH/rsync paths degraded.
  - `capability-engine/CAPABILITY-MATRIX.yaml` still treats reseller-management as a critical income gap, so reseller access decisions should not be conflated with the already-proven cPanel execution path.
- **Recommended next move:** Treat this as a **routing-discipline gap**, not an “unknown access” gap. Build the pre-escalation wrapper and only escalate when reseller credentials/account ownership decisions are actually required.
- **Fritz authority required?** Yes for reseller-account decisions/credentials. No for the routing wrapper and proof-first execution order.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as if they were site-build capability failures, creating junk `capability_id` rows like `to` and `to_do_some_platform_work`.
- **Why it matters:** This pollutes the intelligence loop itself. Bad typing creates fake gap volume, weak promotions, and wasted research cycles.
- **Recurring?** **Yes, clearly recurring.** `intelligence-promotions.json` promotes `bucket-platform-conversation-context`, the curator note still calls it one of the most repeated buckets, and multiple prior gap-research runs surfaced the same issue.
- **Researched options:**
  1. Add a conversation-type gate before `logGap()` so platform-work turns never fall into generic site-gap logging.
  2. Add a first-pass route set such as `site-build`, `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt`.
  3. Only backfill malformed rows after the routing rule exists; otherwise the garbage regenerates.
- **Repo truth:**
  - `site-studio/lib/gap-logger.js` shows the root failure directly: unmatched rows fall back to `slugify(message.slice(0, 60))`, which is exactly how malformed capability IDs are being created.
  - `~/.local/share/famtastic/intelligence-promotions.json` already promotes `bucket-platform-conversation-context` and recommends distinguishing platform/orchestration conversations from site-build conversations before gap logging writes malformed rows.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists this as a `routing_gap` with `candidate_count: 0`, meaning the problem is well-seen but not yet translated into an implementation lane.
  - `obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md` says routing/typing errors are currently a bigger system drag than missing-feature volume.
- **Recommended next move:** Build the conversation-type gate first, then convert the legacy malformed rows into cleanup/closeout work once the logger can no longer recreate them.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- **Most actionable ops closeout:** `billing-usage-visibility` — the path exists, Telegram delivery exists, and the missing part is re-verification plus unpause.
- **Most important business-path discipline fix:** `godaddy-cpanel-access` — this is still a routing/proof-order gap more than an actual hosting-access mystery.
- **Most important recurring intelligence-loop fix:** `platform-conversation-context` — still open, clearly recurring, and still polluting the ledger.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/platform/README.md`
- `~/famtastic/platform/registry/capabilities.json`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-2026-05-05.json`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json`
- `~/famtastic/proofs/mbsh-db-apply-schema-2026-05-05-final.log`
- `~/famtastic/proofs/mbsh-deploy-backend-2026-05-05-cpanel-uapi-overwrite.log`
- `~/.shay/cron/jobs.json`
- `~/.shay/channel_directory.json`
- `shay cron list --all`
- Session recall via `session_search`