---
title: gap-research-2026-06-19-0009
type: note
generated: 2026-06-19 00:09 EDT
source_files:
- /Users/famtasticfritz/.local/share/famtastic/gaps.jsonl
- /Users/famtasticfritz/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md
- /Users/famtasticfritz/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-19-0009-1
---

# Gap Research Report

## Scan summary
- Open gap log entries found: 13
- Intelligence promotions file: missing (`/Users/famtasticfritz/.local/share/famtastic/intelligence-promotions.json`)
- Highest-value items this run: 3
- Selection logic: newest orchestration miss, recurring capability gap, and one hot-context operational gap with no execution surface

## 1) `proactive-gap-escalation-order`
- **Observed failure:** Gap log says GoDaddy/cPanel access uncertainty was escalated to Fritz before internal routing/capability surfaces were exhausted.
- **Why it matters:** This is a direct orchestration failure. It increases Fritz load and violates the standing doctrine that escalation comes last.
- **Repo truth:** This is **partially solved in doctrine, not fully solved in execution**.
  - `platform/README.md` says cPanel UAPI/MCP is the primary control plane and GoDaddy API is only fallback.
  - `platform/registry/capabilities.json` shows `dns.register-subdomain` still ends in a manual prompt because the cPanel DNS wrapper is not built yet.
  - Existing proof artifacts (`proofs/studio-service-auth-mbsh-reunion-2026-05-05.json`) already document that Studio can use cPanel auth and should prefer it.
- **Researched options:**
  1. **Immediate process fix:** codify a pre-escalation checklist for hosting/auth issues: capability-matrix check → local vault/service-auth check → repo proof search → vendor docs → only then escalate.
  2. **Execution fix:** implement the missing cPanel DNS wrapper so `platform dns register-subdomain` stops dumping users into manual Zone Editor for common cases.
  3. **Safety fix:** add a gap-worker lint/rule that flags any future "ask Fritz" recommendation if cPanel credential proofs already exist locally.
- **Recommended next move:** Build the cPanel DNS wrapper first. That closes the main ambiguity source and makes the escalation rule enforceable by code, not memory.
- **Fritz authority required:** **No** for research/spec/implementation prep. **Yes** only if fresh credentials/consent are actually missing at runtime.
- **Confidence:** High
- **Recurring?:** Not proven as a repeated log item yet, but the underlying manual-DNS/control-plane ambiguity is clearly recurring in repo docs and proof artifacts.

## 2) `video_background_option` + `video_background_support`
- **Observed failure:** Two separate open gap records from `site-altitude` ask for the same missing capability: video background support.
- **Why it matters:** This is recurring user demand and directly affects the visual differentiation FAMtastic wants. It also touches Media Studio ↔ Site Studio handoff.
- **Repo truth:** This gap is **largely solved in assets/components, but not clearly closed in the gap log or surfaced as a reliable product capability**.
  - `components/video-hero/` exists with HTML/CSS/component metadata.
  - `components/library.json` includes the video-hero component.
  - `site-studio/lib/famtastic-skeletons.js` contains a VIDEO HERO skeleton and says video will be auto-generated/injected after build completes.
  - Session recall from 2026-06-06 says animated/video backgrounds are planned for Site Studio using Media Studio/Remotion-style assembly.
- **Researched options:**
  1. **Resolve-as-implemented:** If the current `video-hero` + skeleton flow is actually reachable from live Site Studio prompts, mark both old gap records resolved next session.
  2. **Capability proof pass:** run a focused proof from prompt → chosen hero type → rendered output to confirm the build path still works end to end.
  3. **Productization pass:** expose video-background as an explicit brief/build option instead of an implicit skeleton behavior.
- **Recommended next move:** Treat this as a **verification gap, not a blank-slate build gap**. Run one end-to-end proof on a sample site using the existing `video-hero` path. If it passes, resolve both gap records together.
- **Fritz authority required:** No
- **Confidence:** Medium-high
- **Recurring?:** **Yes.** Two separate open gap records describe the same need.

## 3) `jj-ba-transport-tasking-gap`
- **Observed failure:** HOT context still says “JJ BA Transport has no task docs or status — gap.” Repo has site traces and priority notes, but no clean resumable execution brief for the current push.
- **Why it matters:** This is an income + Fritz reputation item. It is also a natural pipeline proof candidate, so lack of tasking blocks both delivery and system validation.
- **Repo truth:** The project exists, but the execution surface is fragmented.
  - `famtastic-dna.md` shows multiple historical builds for `site-jj-ba-transport` and `site-jj-ba-transport2`.
  - `command-center/state.json` lists JJ BA site tags in both studio-sandbox and deploy-repo.
  - Planning docs (`plans/five-stream-state-2026-06-09.md`, `plans/three-lines-pipeline-2026-06-09.md`) mark JJ BA as high priority and a full-pipeline test.
  - Search did **not** find a dedicated current brief/checklist in the default resumable shape.
- **Researched options:**
  1. **Create a resumable brief now:** title, purpose, goal, tasks, status, proof, blocked-by — minimal but durable.
  2. **Promote to control-plane packet:** if this becomes the formal pipeline proof candidate, add branch/worktree/landing-path/proof fields.
  3. **Package-template route:** income research already points to packaging clean sites for marketplace reuse; JJ BA could serve as both relationship repair and monetizable proof if the site quality is upgraded.
- **Recommended next move:** Create a minimal resumable brief first. Do not start new implementation on JJ BA again until the current lane, proof target, and deploy expectation are written down.
- **Fritz authority required:** No for brief creation. Yes later for client-facing approval/deploy/payment decisions.
- **Confidence:** High
- **Recurring?:** Yes in spirit. JJ BA appears repeatedly across plans and memory surfaces as important, but still lacks a durable execution surface.

## Overall recommendations
1. **Code-close the orchestration gap:** implement cPanel DNS wrapper or at minimum a hardcoded pre-escalation checklist around hosting/auth problems.
2. **Verify before rebuilding:** the video background gap looks more like stale gap state than missing capability.
3. **Turn JJ BA into a real brief:** this is the cleanest operational unlock from the current hot-context list.

## Notes
- `intelligence-promotions.json` was not present, so no promotion-state filtering was possible this run.
- If a follow-up worker handles cleanup, the best immediate admin win is resolving stale gap records once capability proof exists.