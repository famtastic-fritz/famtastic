---
title: gap-research-2026-06-21-1433
type: note
generated_at: 2026-06-21 14:33 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-21-1433
---

# Gap Research — 2026-06-21 14:33

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`, `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`, `site-studio/lib/gap-logger.js`, `SITE-LEARNINGS.md`, `problem-buckets.latest.json`, current `shay cron list --all`, current cron/session recall, and bundled Shay cron/runtime docs.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged open while the existing watchdog job remains paused.
- **Why it matters:** This is spend protection and provider-drift visibility. It directly serves Fritz’s efficiency-first hierarchy.
- **Recurring?** **Yes.** This same gap has now surfaced across multiple same-day research passes with no proof of operational closeout.
- **Researched options:**
  1. Re-verify and resume the existing `API Usage Watchdog` cron instead of building a replacement.
  2. Add a local artifact output alongside push delivery so usage proof survives delivery-target drift.
  3. Keep runtime usage telemetry separate from broader billing/invoice ops.
- **Repo truth / live truth:**
  - `gaps.jsonl` still lists `billing-usage-visibility` as open.
  - `problem-buckets.latest.json` still marks the bucket open.
  - `shay cron list --all` at `2026-06-21 14:33 EDT` still shows `API Usage Watchdog` present, paused, and carrying the stale delivery error `no delivery target resolved for deliver=telegram`.
  - The same live cron output now shows `morning-briefing-telegram-alert` active and successfully delivering to `telegram:Fritz Medine (dm)`, so Telegram delivery exists elsewhere and the old watchdog error is likely stale, not structural.
  - Shay cron docs confirm jobs can be paused/resumed/edited in place; this is an operational closeout problem, not a missing cron capability.
- **Recommended next move:** Treat this as **implemented-but-paused infrastructure**. Re-verify `usage_watchdog.sh`, update the delivery target if needed, resume it, and write proof back into the gap ledger next session.
- **Fritz authority required?** No for verification/resume. Yes only if threshold policy or budget policy changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before the internal proof/routing ladder was exhausted.
- **Why it matters:** This burns Fritz attention on a cash-adjacent path tied to reseller recovery and hosting operations.
- **Recurring?** **Yes, at the pattern level.** The raw row is single-frequency, but the same routing confusion keeps reappearing in current proof docs, bucketing, and recurring research output.
- **Researched options:**
  1. Split the problem into distinct lanes: reseller-account recovery, cPanel/DNS execution, and escalation-order discipline.
  2. Add a pre-escalation wrapper/checklist that enforces `capability matrix -> agent tools -> vendor/repo truth -> Fritz last`.
  3. Treat GoDaddy API as fallback truth when cPanel proof already exists.
- **Repo truth / live truth:**
  - `gaps.jsonl` still lists `proactive-gap-escalation-order` as open under bucket `godaddy-cpanel-access`.
  - `problem-buckets.latest.json` still marks `godaddy-cpanel-access` open and recommends splitting vendor access, DNS operations, reseller recovery, and escalation order into separate proof-backed tracks.
  - `SITE-LEARNINGS.md` documents the cPanel MCP spike and proves cPanel UAPI/MCP is already the primary control surface for hosting operations, with GoDaddy direct API optional fallback.
  - The gap row’s own `expected_sequence` already encodes the correct routing order; the missing piece is enforcement, not discovery.
- **Recommended next move:** Treat this as a **routing-discipline gap**. Build the pre-escalation wrapper and narrow Fritz involvement to real reseller-account ownership/credential decisions.
- **Fritz authority required?** Yes for reseller-account/account-owner decisions. No for the wrapper and routing logic.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as capability failures, producing malformed rows like `to` and `to_do_some_platform_work`.
- **Why it matters:** This pollutes the intelligence loop itself, inflates fake gap volume, and wastes future research cycles.
- **Recurring?** **Yes, clearly recurring.** Frequency is 3 in current bucket truth, it is already promoted in `intelligence-promotions.json`, and the same issue keeps showing up in recall.
- **Researched options:**
  1. Add a conversation-type gate before generic gap logging.
  2. Route first-pass conversation classes such as `site-build`, `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt`.
  3. Only do deeper cleanup/backfill after the logger can no longer recreate junk rows.
- **Repo truth / live truth:**
  - `gaps.jsonl` still contains multiple open rows in this bucket.
  - `problem-buckets.latest.json` still marks `platform-conversation-context` open with frequency `3`.
  - `site-studio/lib/gap-logger.js` still falls back to `const capId = details.capability_id || slugify(message.slice(0, 60));`, which is the direct source of malformed capability IDs when no explicit route/classification exists.
  - `intelligence-promotions.json` already recommends distinguishing platform/orchestration conversations from site-build conversations before gap logging writes malformed rows.
- **Recommended next move:** Build the conversation-type gate first, then close/rewrite legacy junk rows once regeneration is impossible.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- **No material change since the 12:31 pass:** the same three highest-value open gaps are still unresolved.
- **Most actionable operational closeout:** `billing-usage-visibility`.
- **Most important business-path discipline fix:** `godaddy-cpanel-access`.
- **Most important recurring intelligence-loop fix:** `platform-conversation-context`.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/SITE-LEARNINGS.md`
- `~/famtastic/shay-shay/website/docs/user-guide/features/cron.md`
- `shay cron list --all`
- Session recall via `session_search`
