---
title: gap-research-2026-06-19-1241
type: note
generated: 2026-06-19 12:41 EDT
generated_iso: 2026-06-19T12:41:12-0400
source_files:
  - /Users/famtasticfritz/.local/share/famtastic/gaps.jsonl
  - /Users/famtasticfritz/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md
  - /Users/famtasticfritz/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md
  - /Users/famtasticfritz/famtastic/platform/README.md
  - /Users/famtasticfritz/famtastic/platform/registry/capabilities.json
  - /Users/famtasticfritz/famtastic/site-studio/lib/famtastic-skeletons.js
  - /Users/famtasticfritz/famtastic/site-studio/shay-shay/instructions.md
  - /Users/famtasticfritz/famtastic/plans/five-stream-state-2026-06-09.md
  - session_search: '"GoDaddy" OR cPanel OR "gap escalation" OR "video background" OR "JJ BA" OR platform convos'
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-19-1241
---

# Gap Research Report

## Scan summary
- Open gap log entries found: 13
- Intelligence promotions file: missing (`/Users/famtasticfritz/.local/share/famtastic/intelligence-promotions.json`)
- Highest-value open gaps researched this run: 3
- Recurrence note: the same three high-leverage gaps are still open; no stronger replacement surfaced in repo truth or session recall

## 1) `proactive-gap-escalation-order`
- **Observed failure:** Gap `gap-1781840661232` records that GoDaddy/cPanel access uncertainty was escalated to Fritz before internal capability surfaces were exhausted.
- **Why it matters:** This is a direct cognitive-load violation. It turns recoverable platform ambiguity into human interruption.
- **Researched options:**
  1. Treat cPanel as the primary control plane, not raw GoDaddy API chasing.
  2. Add a reusable pre-escalation triage path: capability matrix -> service refs -> proof artifacts -> vendor recovery path -> Fritz last.
  3. Close the tooling hole by wrapping DNS record operations instead of leaving them as manual prompts.
- **Repo truth:**
  - `platform/README.md` says `platform dns register-subdomain` is still a stub and that cPanel UAPI/MCP is primary while GoDaddy API is optional fallback.
  - `platform/registry/capabilities.json` marks `dns.register-subdomain` as `v1-manual-prompt` with manual step `cPanel/GoDaddy Zone Editor UI until cPanel DNS wrapper is added`.
  - Session recall from the 06:21 cron summary reinforces the same root cause: existing cPanel proofs and reseller recovery paths exist, but automation/wrapper coverage is incomplete.
- **Recommended next move:** Build the cPanel DNS wrapper and pair it with a hard pre-escalation checklist. This is still the cleanest leverage point.
- **Fritz authority required:** No for research/spec/implementation prep. Yes only if stored credentials are actually dead or missing.
- **Confidence:** High
- **Recurring?:** **Yes.** Fresh gap record, old root cause.

## 2) `video_background_option` + `video_background_support`
- **Observed failure:** Two separate open gap records from `site-altitude` still claim video background support is missing.
- **Why it matters:** It is a visible FAMtastic-style capability, and duplicate gap records mean the system still does not trust its own implementation surface.
- **Researched options:**
  1. Run proof from brief -> skeleton selection -> post-processing -> rendered output.
  2. If proof fails, isolate whether the miss is prompt routing, slot detection, or Veo/media fill.
  3. If proof passes, close both gap records and surface this as an explicit build option instead of implicit behavior.
- **Repo truth:**
  - `site-studio/lib/famtastic-skeletons.js` exports `VIDEO_HERO_SKELETON`.
  - `site-studio/server.js` injects that skeleton when `spec.character_sets.length > 0`.
  - `site-studio/shay-shay/instructions.md` says Step 11 of post-processing detects `<video data-slot-type="video">` and auto-fires Veo generation.
  - Prior local gap-research artifact at `gap-research-2026-06-19-0212.md` already identified this as a verification/surfacing gap, not a confirmed missing capability.
- **Recommended next move:** Do not rebuild this. Run proof and either resolve stale gap records or narrow the actual failing stage.
- **Fritz authority required:** No
- **Confidence:** Medium-high
- **Recurring?:** **Yes, explicitly.** Two open records for the same outcome, plus the prior research artifact reached the same conclusion.

## 3) `jj-ba-transport-tasking-gap`
- **Observed failure:** HOT context still says `JJ BA Transport has no task docs or status — gap`, while repo surfaces continue to mention it as a personal/reputation priority.
- **Why it matters:** JJ BA is both relationship-sensitive and a strong pipeline-proof candidate. Lack of resumable task shape keeps it trapped as context residue.
- **Researched options:**
  1. Create a minimal resumable brief immediately.
  2. Promote it into a pipeline-proof packet once proof targets are chosen.
  3. Tie it into the income/packaging lane only after the execution surface exists.
- **Repo truth:**
  - `HOT-CONTEXT-POINTERS.md` still lists JJ BA as a known gap.
  - `plans/five-stream-state-2026-06-09.md` calls JJ BA a personal priority, reputation issue, and full pipeline test candidate.
  - `command-center/state.json` still tracks `site-jj-ba-transport` and `site-jj-ba-transport2` in both sandbox and deploy surfaces.
  - `famtastic-dna.md` shows multiple prior JJ BA site builds, but current search still does not surface a clean resumable brief in the default brief shape.
- **Recommended next move:** Create the resumable brief first. No more scattered mentions without a durable execution lane.
- **Fritz authority required:** No for brief creation. Yes later for client-facing approval, deployment, or payment decisions.
- **Confidence:** High
- **Recurring?:** **Yes.** JJ BA keeps appearing as priority state without being converted into an active execution surface.

## Overall recommendation stack
1. **Close the orchestration hole first:** cPanel-first DNS wrapper + pre-escalation checklist.
2. **Proof before rebuild:** video background support looks implemented-but-unproven, not absent.
3. **Turn JJ BA into a real brief:** it remains the clearest execution gap with revenue/reputation upside.

## Notes
- `intelligence-promotions.json` is still missing, so promotion-state filtering was not possible.
- Tavily-backed web search/extract was unavailable during this run, so this report leans on repo truth plus session recall instead of fresh external extracts.
