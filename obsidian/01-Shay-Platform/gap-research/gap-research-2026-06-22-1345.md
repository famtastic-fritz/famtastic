---
title: gap-research-2026-06-22-1345
type: note
generated_at: 2026-06-22 13:45 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-22-1345
---

# Gap Research — 2026-06-22 13:45

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, hot-context pointers, latest bookmark research, current curator note, problem-bucket/verdict audits, cron/runtime truth, local repo truth, and session recall.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged as an open build gap even though the working path already exists as paused ops infrastructure.
- **Why it matters:** This is direct money protection. It reduces silent spend bleed faster than building new billing surfaces.
- **Recurring?** **Yes.** The open row keeps surviving because the watchdog exists but never got operational closeout.
- **Researched options:**
  1. Resume the existing `API Usage Watchdog` after a one-pass verification.
  2. Add local artifact/log proof so the watchdog is not dependent on Telegram alone.
  3. Keep spend visibility separate from invoice/payment-rail work; they are different capability surfaces.
- **Repo/runtime truth:**
  - `shay cron list --all` shows `API Usage Watchdog` still exists as a paused `no-agent` cron, with the stale delivery error `no delivery target resolved for deliver=telegram`.
  - `~/.shay/channel_directory.json` now contains a live Telegram DM target for Fritz, and `shay cron list --all` shows `morning-briefing-telegram-alert` actively delivering to `telegram:Fritz Medine (dm)`.
  - `~/.shay/cron/jobs.json` still carries the stale delivery failure on the watchdog job.
  - `obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md` explicitly calls this out as a partial capability that drifted because no proof-closeout loop finished it.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.md` still lists this bucket as open, with only a weak repo candidate and no closeout.
- **Recommended next move:** Treat this as **implemented-but-paused operations**, not greenfield product work. Re-verify the watchdog against the current Telegram route, then resume it or close the gap with a proof packet if another blocker appears.
- **Fritz authority required?** No, unless threshold/budget policy changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty is still being represented like an access mystery when repo truth already separates reseller recovery, cPanel execution, DNS operations, and escalation discipline.
- **Why it matters:** This sits next to the reseller-income lane. Confused escalation order burns Fritz attention on a cash-adjacent path.
- **Recurring?** **Yes, at the pattern level.** The exact row is newer, but the same blend-error keeps resurfacing around reseller/API/cPanel work.
- **Researched options:**
  1. Split this bucket into `reseller-reactivation`, `dns/cpanel-operations`, and `pre-escalation-discipline` lanes.
  2. Make the sequence `repo truth -> capability matrix -> local tools -> vendor docs -> Fritz last` executable, not just advice.
  3. Keep GoDaddy developer API as optional fallback when cPanel UAPI/MCP proof already covers the operation.
- **Repo/runtime truth:**
  - The gap row itself encodes the expected sequence and marks the observed failure as early user escalation.
  - `plans/five-stream-state-2026-06-09.md` shows reseller auth works enough to hit `/v1/orders`, while `/v1/customers` returns `401` and `/v1/reseller/orders` returns `404`; this is a scoped endpoint/auth reality, not total blackout.
  - `HOT-CONTEXT-POINTERS.md` and the five-stream state both keep reseller reactivation framed as a live income unlock.
  - `obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md` recommends a pre-escalation wrapper for vendor/account ambiguity as an explicit build target.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.md` still says this bucket should be split into separate proof-backed tracks.
- **Recommended next move:** Reclassify this as a **routing-discipline gap with vendor sublanes**, not a single hosting-access gap. Build the wrapper first, then leave only real credential/ownership decisions for Fritz.
- **Fritz authority required?** Yes for credential ownership or irreversible reseller-account decisions. No for proof gathering, vendor pathing, or wrapper design.
- **Confidence:** High

### 3) `to_work_out_some_kinks_with_you` (`shay-desk-ui-kinks`)
- **capability_id:** `to_work_out_some_kinks_with_you`
- **Observed failure:** Historical complaints about newline behavior, oversized text area, and Shay Desk interaction are still sitting as an open UX bucket with no implementation lane attached.
- **Why it matters:** This is the direct human-interface layer. If the chat surface feels clumsy, orchestration quality drops because the bottleneck becomes input friction.
- **Recurring?** **Yes, clearly recurring.** The bucket frequency is `3`, it remains `pending` in promotions, and there is still no linked repo-intelligence candidate.
- **Researched options:**
  1. Treat the bucket as one remediation packet: input semantics, textarea sizing, and interaction polish together.
  2. Separate **CLI-newline solved** from **Desk/Web UI unverified** so solved surfaces are not masking unsolved ones.
  3. Close the historical rows only after one current browser-proof pass on the active Desk/Workspace/PWA input surface.
- **Repo/runtime truth:**
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.md` still marks `shay-desk-ui-kinks` open at frequency `3` and recommends grouping newline + textarea complaints into one UI-fix bucket.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.md` shows `candidate_count: 0`, meaning the bucket still lacks a real execution lane.
  - In `shay-shay`, newline handling is already well-developed on the CLI side: `cli.py`, `tests/cli/test_ctrl_enter_newline.py`, `tests/cli/test_cli_shift_enter_newline.py`, and the bundled `shay-shay` skill all document/verify Alt+Enter/Ctrl+Enter/Shift+Enter behavior.
  - That means at least part of the historical complaint class is **already solved in the CLI host body**, but there is no equivalent current proof tying that solution to the original Shay Desk/browser-facing friction.
- **Recommended next move:** Open a **single UX verification packet** for the current rich surface (Desk/Workspace/PWA), then either (a) close the bucket as stale-historical if current input feels right, or (b) turn the failing surface into a real owned UI task.
- **Fritz authority required?** No for verification or packaging the fix. Yes only if the chosen remedy changes the preferred input model across surfaces.
- **Confidence:** Medium-High

## Bottom line
- **Most actionable operational closeout:** `billing-usage-visibility` — the route exists, the blocker is stale, and this wants resume/closeout instead of new invention.
- **Most important cash-adjacent discipline fix:** `godaddy-cpanel-access` — still a routing-wrapper problem more than a true access mystery.
- **Most likely stale-historical bucket needing proof, not theory:** `shay-desk-ui-kinks` — some of the complaint class is already solved in CLI truth, but the rich-surface proof packet has not happened.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.{md,json}`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.{md,json}`
- `~/.shay/cron/jobs.json`
- `~/.shay/channel_directory.json`
- `shay cron list --all`
- `~/famtastic/plans/five-stream-state-2026-06-09.md`
- `~/famtastic/shay-shay/cli.py`
- `~/famtastic/shay-shay/tests/cli/test_ctrl_enter_newline.py`
- `~/famtastic/shay-shay/tests/cli/test_cli_shift_enter_newline.py`
- `~/famtastic/shay-shay/skills/autonomous-ai-agents/shay-shay/SKILL.md`
- Session recall via `session_search`
