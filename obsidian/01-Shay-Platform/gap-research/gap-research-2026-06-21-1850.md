---
title: gap-research-2026-06-21-1850
type: note
generated_at: 2026-06-21 18:50 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/latest-gap-research-3
---

# Gap Research — 2026-06-21 18:50

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`, `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`, `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`, `~/famtastic/site-studio/lib/gap-logger.js`, `~/famtastic/FAMTASTIC-STATE.md`, `~/.shay/scripts/usage_watchdog.sh`, live `shay cron list --all`, live `~/.shay/channel_directory.json`, and session recall. Web vendor research was attempted again; Tavily-backed web tools still return HTTP 432, so this pass leans on live repo/runtime truth plus direct reachable vendor pages where possible.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged as open while the existing watchdog job remains paused.
- **Why it matters:** This is direct wallet protection. It supports Fritz’s efficiency-first hierarchy and protects the brain/worker lane mix from silent spend drift.
- **Recurring?** Yes. This gap has already survived multiple same-day research passes without operational closeout.
- **Researched options:**
  1. Resume and verify the existing `API Usage Watchdog` instead of designing a replacement.
  2. Add a local markdown/JSON artifact path so the signal survives delivery-route drift.
  3. Keep runtime usage telemetry separate from invoice/billing workflows.
- **Repo truth / live truth:**
  - `gaps.jsonl` still lists this capability as `status: "open"` under bucket `billing-usage-visibility`.
  - `problem-buckets.latest.json` still marks `billing-usage-visibility` open.
  - `shay cron list --all` at `2026-06-21 18:50 EDT` still shows `API Usage Watchdog` present but **paused**, with stale failure text `no delivery target resolved for deliver=telegram`.
  - The same live cron output shows `morning-briefing-telegram-alert` active and delivering to `telegram:Fritz Medine (dm)`.
  - `~/.shay/channel_directory.json` was updated at `2026-06-21T18:43:03` and contains Fritz’s Telegram DM target, so the watchdog’s old delivery error is stale state, not current platform reality.
  - `~/.shay/scripts/usage_watchdog.sh` already implements the watchdog loop via `shay insights --days 1`, cost parsing, error parsing, and a `$10` alert threshold.
  - `FAMTASTIC-STATE.md` still documents existing cost telemetry in Site Studio via `site-studio/lib/api-telemetry.js` and `GET /api/telemetry/sdk-cost-summary`.
  - Shay cron docs (`website/docs/user-guide/features/cron.md`) confirm `no-agent` jobs are the intended script-only watchdog pattern: stdout delivered verbatim, zero LLM involvement.
- **Recommended next move:** Reclassify this as **implemented-but-paused operational infrastructure**. Verify the script against the now-valid Telegram route, resume the cron, and add a local artifact/report output so the proof is not channel-dependent.
- **Fritz authority required?** No for verify/resume. Yes only if threshold/budget policy changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order`
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before the internal routing/proof ladder was exhausted.
- **Why it matters:** This burns Fritz attention on a cash-adjacent path tied to reseller recovery, hosting operations, and DNS execution.
- **Recurring?** Yes at the pattern level. The raw gap row is single-frequency, but the same routing confusion keeps reappearing in bucket guidance and follow-up research.
- **Researched options:**
  1. Split the work into four lanes: reseller-account recovery, cPanel execution, registrar/direct-DNS fallback, and escalation-order discipline.
  2. Add a pre-escalation wrapper that enforces `capability matrix -> agent tools -> vendor/repo truth -> Fritz last`.
  3. Keep cPanel UAPI/MCP as the primary control plane and treat GoDaddy API as registrar/direct-DNS fallback only.
- **Repo truth / live truth:**
  - `gaps.jsonl` still lists `proactive-gap-escalation-order` as `status: "open"` under bucket `godaddy-cpanel-access`.
  - `problem-buckets.latest.json` still marks `godaddy-cpanel-access` open and explicitly recommends splitting vendor access, DNS operations, reseller recovery, and escalation order into separate proof-backed tracks.
  - The gap row itself already contains the correct `expected_sequence`; the missing piece is enforcement, not diagnosis.
  - `proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json` records `studio.dns_control` with detail: `Use cPanel UAPI/MCP for DNS/hosting operations; GoDaddy API is optional fallback`.
  - `platform/registry/capabilities.json` says the DNS register-subdomain capability is backed by `cPanel UAPI/MCP Zone Editor primary; GoDaddy direct DNS API optional fallback`.
  - `platform/README.md` already documents GoDaddy-hosted DB operations through `cpanel-mcp`, proving the local truth surface already prefers cPanel-first execution.
  - Direct fetch of GoDaddy’s developer domains docs page succeeded this run, confirming the vendor surface is reachable; meanwhile the cPanel-specific URL guess used for web verification 404’d, so repo truth remains the stronger source for the cPanel-first rule on this pass.
- **Recommended next move:** Treat this as a **routing-discipline gap**, not a pure access gap. Build the pre-escalation wrapper/checklist and split the lane so only true ownership/credential decisions escalate to Fritz.
- **Fritz authority required?** Yes for reseller ownership or irreversible account actions. No for wrapper/routing work.
- **Confidence:** High

### 3) `special_case_for_platfor_convos`
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as capability failures, producing junk rows like `to` and `to_do_some_platform_work`.
- **Why it matters:** This pollutes the intelligence loop itself, inflates fake gap volume, and wastes future research cycles on garbage artifacts.
- **Recurring?** Yes, clearly recurring. Current promoted finding frequency is `3`, and the bucket remains open in the latest audit.
- **Researched options:**
  1. Add a conversation-type gate before generic gap logging.
  2. Route first-pass conversation classes such as `site-build`, `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt`.
  3. Only backfill/delete legacy junk rows after the logger can no longer recreate them.
- **Repo truth / live truth:**
  - `gaps.jsonl` still contains multiple rows in this bucket, including the still-open `special_case_for_platfor_convos` row.
  - `intelligence-promotions.json` still promotes `bucket-platform-conversation-context` and explicitly recommends distinguishing platform/orchestration conversations from site-build conversations before logging.
  - `problem-buckets.latest.json` still marks the bucket open at frequency `3`.
  - `site-studio/lib/gap-logger.js` still falls back to `const capId = details.capability_id || slugify(message.slice(0, 60));`, which is the direct mechanism that turns unclassified platform talk into malformed capability IDs.
  - The logger already has bucket matching and destination routing, but it has no front-door conversation-type classifier before slug fallback. That is the missing seam.
- **Recommended next move:** Build the conversation-type gate first. Once the logger can no longer regenerate junk rows, close or rewrite the existing malformed rows.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- No meaningful priority change since the earlier same-day artifact: the same three open gaps are still the highest-value ones.
- **Most actionable operational closeout:** `billing-usage-visibility`.
- **Most important cash-path discipline fix:** `godaddy-cpanel-access`.
- **Most important intelligence-loop hygiene fix:** `platform-conversation-context`.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/FAMTASTIC-STATE.md`
- `~/famtastic/platform/README.md`
- `~/famtastic/platform/registry/capabilities.json`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json`
- `~/.shay/scripts/usage_watchdog.sh`
- `~/.shay/channel_directory.json`
- `shay cron list --all`
- `website/docs/user-guide/features/cron.md`
- Session recall via `session_search`
- Direct vendor-page fetch of GoDaddy developer domains docs