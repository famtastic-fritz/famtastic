---
title: gap-research-2026-06-21-1231
type: note
generated_at: 2026-06-21 12:31 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-21-1231
---

# Gap Research — 2026-06-21 12:31

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`, the latest GitHub bookmarks report, current repo truth, current cron truth, current intelligence-audit artifacts, and session recall.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility was requested, but the watchdog loop is still paused even though the delivery path that previously failed now has live proof.
- **Why it matters:** This is direct spend protection, provider-drift detection, and revenue-discipline infrastructure. It serves Fritz’s efficiency-first hierarchy and protects brain budget.
- **Recurring?** **Yes.** This bucket continues to show as open in current gap truth and current intelligence-audit artifacts, and prior gap-research runs already surfaced the same root issue.
- **Researched options:**
  1. Re-verify and resume the existing `API Usage Watchdog` cron instead of designing a replacement.
  2. Add a local markdown/json artifact alongside Telegram push so the signal survives channel drift.
  3. Keep billing/invoice workflows separate from runtime usage telemetry; both matter, but they are different capabilities.
- **Repo truth / live truth:**
  - `gaps.jsonl` still has `billing-usage-visibility` open.
  - `shay cron list --all` at `2026-06-21 12:31 EDT` still shows `API Usage Watchdog` present, paused, and carrying the stale delivery error `no delivery target resolved for deliver=telegram`.
  - The same live cron output now shows `morning-briefing-telegram-alert` active and successfully delivering to `telegram:Fritz Medine (dm)`, which means Telegram delivery is no longer the blocker.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json` still includes `billing-usage-visibility`.
  - Billing primitives exist in repo truth, but runtime usage observability still has no active proof surface.
- **Recommended next move:** Treat this as **implemented-but-paused operational infrastructure**. Verify `usage_watchdog.sh` against the current Telegram target, unpause if clean, and attach a local artifact path so observability is not push-only.
- **Fritz authority required?** No for re-verification/unpause. Yes only if threshold policy, provider budget policy, or paid tooling changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before the internal routing/proof ladder was exhausted.
- **Why it matters:** This sits near the reseller-income lane. Bad escalation order burns Fritz attention and slows down one of the clearest cash-unlock paths.
- **Recurring?** **Yes, at the pattern level.** The exact row is single-frequency, but the confusion between reseller recovery, GoDaddy API, and cPanel execution keeps recurring across proof docs, capability docs, and prior gap-research artifacts.
- **Researched options:**
  1. Split the problem into three lanes: reseller-account recovery, cPanel/DNS execution, and escalation-order discipline.
  2. Build a pre-escalation wrapper/checklist that enforces the encoded order: capability matrix → local tools → vendor/repo truth → Fritz last.
  3. Treat GoDaddy API as optional fallback truth when cPanel proof already exists.
- **Repo truth / live truth:**
  - `gaps.jsonl` still has `proactive-gap-escalation-order` open.
  - The gap row itself encodes the desired sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `SITE-LEARNINGS.md` and platform proof artifacts still state that cPanel UAPI/MCP is the primary hosting/DB/DNS control plane and GoDaddy developer API is fallback.
  - The MBSH proof logs still show successful cPanel fallback execution paths, which means this is not primarily an “unknown hosting access” problem.
  - `problem-buckets.latest.json` still includes `godaddy-cpanel-access`.
- **Recommended next move:** Treat this as a **routing-discipline gap**, not an access-mystery gap. Build the pre-escalation wrapper and keep Fritz involvement limited to actual reseller credentials/account-ownership decisions.
- **Fritz authority required?** Yes for reseller-account decisions/credentials. No for the wrapper and proof-first routing logic.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as site-build capability failures, creating junk `capability_id` rows like `to` and `to_do_some_platform_work`.
- **Why it matters:** This pollutes the intelligence loop itself. Bad routing creates fake gap volume, weak promotions, and wasted research cycles.
- **Recurring?** **Yes, clearly recurring.** The bucket is promoted in `intelligence-promotions.json`, appears in current audit artifacts, and multiple open rows still remain in `gaps.jsonl`.
- **Researched options:**
  1. Add a conversation-type gate before generic gap logging.
  2. Route first-pass conversation types such as `site-build`, `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt`.
  3. Only clean backfilled junk rows after the routing rule exists; otherwise the garbage regenerates.
- **Repo truth / live truth:**
  - `gaps.jsonl` still has multiple open rows in `platform-conversation-context`.
  - `site-studio/lib/gap-logger.js` still falls back to `slugify(message.slice(0, 60))` when no explicit capability id is supplied, which is the direct source of malformed IDs.
  - `intelligence-promotions.json` still recommends distinguishing platform/orchestration conversations from site-build conversations before logging.
  - `problem-buckets.latest.json` still includes `platform-conversation-context`.
- **Recommended next move:** Build the conversation-type gate first, then convert legacy malformed rows into cleanup/closeout work once the logger can no longer recreate them.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- **Most actionable ops closeout:** `billing-usage-visibility` — delivery proof now exists elsewhere, so the blocker looks stale, not structural.
- **Most important business-path discipline fix:** `godaddy-cpanel-access` — still a proof-order/routing problem more than a true access unknown.
- **Most important recurring intelligence-loop fix:** `platform-conversation-context` — still open, still recurring, still polluting the ledger.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/SITE-LEARNINGS.md`
- `shay cron list --all`
- Session recall via `session_search`
