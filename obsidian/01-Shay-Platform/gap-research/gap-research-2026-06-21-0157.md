---
title: gap-research-2026-06-21-0157
type: note
generated_at: 2026-06-21 01:57:00-04:00
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-21-0157
---

# Gap Research — 2026-06-21 01:57

## Scope
Reviewed open gaps from `~/.local/share/famtastic/gaps.jsonl`, promotions, hot-context pointers, current repo truth, cron/runtime state, prior gap-research artifacts, and reachable vendor/web docs.

## Highest-value gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **Observed failure:** Billing/API-usage visibility was requested, but the capability never became a dependable always-on ops surface.
- **Why it matters:** Cost visibility protects the brain lane, catches silent provider drain, and supports Fritz’s efficiency-first / revenue-aware operating order.
- **Recurring?** Yes operationally. The raw gap row appears once, but the same need keeps surfacing through telemetry work, watchdog jobs, and control-plane monitoring.
- **Researched options:**
  1. Repair the existing watchdog path first.
  2. Add a local artifact/report path so visibility survives delivery-target drift.
  3. Later unify telemetry, cron alerts, and command-center reporting into one cost surface.
- **Repo truth / vendor truth:**
  - `site-studio/lib/api-telemetry.js` exists and `site-studio/server.js` exposes `GET /api/telemetry/sdk-cost-summary`.
  - `site-studio/public/js/studio-screens.js` already reads `/api/telemetry/sdk-cost-summary`, so the UI side is not hypothetical.
  - `shay cron list --all` shows `API Usage Watchdog` exists but is **paused**, and its last delivery failed with `no delivery target resolved for deliver=telegram`.
  - `FAMTASTIC-STATE.md` documents telemetry and cost tracking as live system behavior.
  - `https://opentelemetry.io/docs/` was reachable this run and supports the general observability pattern: usage/cost belongs in the telemetry layer, not scattered prompt memory.
- **Recommended next move:** Treat this gap as **implemented but operationally incomplete**. Fix the watchdog delivery target, add a durable local summary artifact, then unpause only after verification.
- **Fritz authority required?** No for wiring/verification. Yes only if thresholds, paid-provider policy, or alerting destinations need a business choice.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated to Fritz before internal capability surfaces, proofs, and routing checks were exhausted.
- **Why it matters:** This sits directly on the reseller/income path. Bad escalation order burns Fritz attention and slows cash-adjacent execution.
- **Recurring?** Yes at the bucket/track level. The exact row is single-frequency, but the underlying hosting/reseller access path has repeated proof, tasks, and memory entries.
- **Researched options:**
  1. Split the work into separate tracks: reseller recovery, DNS/cPanel execution, and escalation-order discipline.
  2. Add a pre-escalation proof check against capability registry, proof artifacts, and vendor-fact memory.
  3. Treat GoDaddy API as fallback truth, not the only possible path, when cPanel proof already exists.
- **Repo truth / vendor truth:**
  - The gap row itself records the expected sequence: `check_capability_matrix` → `consult_agent_tools` → `search_vendor_and_repo_truth` → `escalate_to_fritz_last`.
  - `proofs/studio-service-auth-mbsh-reunion-2026-05-05.json` and the v2 variant both state: `Use cPanel UAPI/MCP for DNS/hosting operations; GoDaddy API is optional fallback`.
  - `memory/vendor-fact/cpanel-uapi-overwrite-is-the-path-for-backend-deploy-on-goda.md` says direct cPanel UAPI calls are the proven programmatic deploy path on this hosting.
  - `proofs/mbsh-deploy-backend-2026-05-05-cpanel-uapi-overwrite.log` shows the real fallback deployment happened through cPanel UAPI after `rsync` failed.
  - `https://developer.godaddy.com/doc/endpoint/domains` is still relevant vendor reference, but local proof is the stronger truth surface here.
- **Recommended next move:** Do **not** treat this as a missing-access mystery. Treat it as a routing-wrapper gap: formalize the pre-escalation checklist, separate the bucket from reseller recovery, and close the “unknown access” framing.
- **Fritz authority required?** Yes for reseller-account credentials/decisions. No for routing discipline, wrapper docs, and proof-first escalation order.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **Observed failure:** Platform/orchestration conversations were treated like site-build conversations, which produced malformed capability IDs and polluted the gap ledger.
- **Why it matters:** This degrades the intelligence loop itself. Bad routing creates bad buckets, bad promotions, and noisy backlog.
- **Recurring?** **Yes. Clearly recurring.** Promotions mark frequency `3`, spanning at least `site-famtastic-com` and `site-mbsh96reunion`.
- **Researched options:**
  1. Add a classifier gate that distinguishes platform/orchestration talk from site-build intake before gap logging.
  2. Route platform-conversation detections into a dedicated process/routing bucket instead of raw capability rows.
  3. Backfill/resolve the malformed rows after the classifier/routing rule exists, so the ledger stops compounding junk.
- **Repo truth / vendor truth:**
  - `~/.local/share/famtastic/intelligence-promotions.json` already promotes `bucket-platform-conversation-context` with the explicit recommendation: distinguish platform/orchestration conversations from site-build conversations before classifier or gap logging writes malformed rows.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json` repeats the same diagnosis and groups the related aliases together.
  - Prior gap-research artifacts from 2026-06-20 already called this out as the most important intelligence-loop fix, which means the gap is recurring across research passes as well, not just raw logs.
  - No repo evidence found that the routing fix itself has been implemented yet; the system has diagnosis and promotion, but not closure proof.
- **Recommended next move:** Treat this as an **open classifier/routing fix**, not just a cleanup note. Build the conversation-type gate first, then mark the existing malformed rows for operational closeout so the bucket stops regenerating.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- **Most actionable immediate closeout:** `billing-usage-visibility` — telemetry exists; the broken part is the always-on delivery/visibility loop.
- **Most important business-path cleanup:** `godaddy-cpanel-access` — this is a routing/proof-wrapper problem more than an actual missing-capability problem.
- **Most important recurring intelligence-loop fix:** `platform-conversation-context` — still open, clearly recurring, and still polluting the ledger.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/gap-research/latest-gap-research.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`
- `~/famtastic/site-studio/lib/api-telemetry.js`
- `~/famtastic/site-studio/public/js/studio-screens.js`
- `~/famtastic/site-studio/server.js`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-2026-05-05.json`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json`
- `~/famtastic/proofs/mbsh-deploy-backend-2026-05-05-cpanel-uapi-overwrite.log`
- `~/famtastic/memory/vendor-fact/cpanel-uapi-overwrite-is-the-path-for-backend-deploy-on-goda.md`
- `~/famtastic/FAMTASTIC-STATE.md`
- `shay cron list --all`
- `https://opentelemetry.io/docs/`
- `https://developer.godaddy.com/doc/endpoint/domains`
