---
title: gap-research-2026-06-22-1139
type: note
generated_at: 2026-06-22 11:39 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-22-1139-1
---

# Gap Research — 2026-06-22 11:39

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, hot-context pointers, latest bookmark research, current curator note, current repo-verdict audit, current cron/channel truth, local repo truth, targeted Shay docs, and session recall.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility was requested, but the protection loop is still paused even though the delivery route that originally blocked it now exists.
- **Why it matters:** This is direct money protection. It protects spend, catches provider drift, and fits Fritz’s efficiency-first hierarchy better than building fresh billing surfaces.
- **Recurring?** **Yes.** It remains an open gap row, the internal curator note still calls revenue observability unresolved, and prior gap-research runs keep landing on the same conclusion: this is stale-paused infrastructure, not missing invention.
- **Researched options:**
  1. Restore the existing `API Usage Watchdog` instead of designing a new telemetry system.
  2. Add a local artifact/log output alongside Telegram delivery so proof survives future channel drift.
  3. Keep invoice generation/payment rails separate from runtime spend visibility; the billing capability docs prove those are different surfaces.
- **Repo/runtime truth:**
  - `shay cron list --all` shows `API Usage Watchdog` still exists, still points at `usage_watchdog.sh`, and is still **paused** with the old delivery error `no delivery target resolved for deliver=telegram`.
  - `~/.shay/channel_directory.json` now has a live Telegram DM target for Fritz, and `shay cron list --all` shows `morning-briefing-telegram-alert` actively delivering to `telegram:Fritz Medine (dm)`. Telegram delivery works now.
  - `~/.shay/cron/jobs.json` confirms the watchdog remains paused with the stale delivery error attached.
  - `platform/capabilities/billing/README.md` proves billing work today is invoice/ledger generation and manual payment handling, **not** runtime API-spend observability.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `billing-usage-visibility` as an open `ops_gap`.
- **Recommended next move:** Reclassify this as **implemented-but-paused ops infrastructure**. Re-verify `usage_watchdog.sh` against the now-valid Telegram route, then either resume it or generate a closeout packet if another runtime blocker appears.
- **Fritz authority required?** No for verification/resume. Yes only if threshold policy, budget policy, or paid observability tooling changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated to Fritz before the internal proof surfaces and routing lattice were fully exhausted.
- **Why it matters:** This sits next to the reseller-income lane. Bad escalation order burns Fritz attention and delays cash-adjacent work.
- **Recurring?** **Yes, at the pattern level.** The exact gap row is newer, but the same confusion keeps resurfacing: reseller access, GoDaddy API, and cPanel execution get blended together when repo truth already separates them.
- **Researched options:**
  1. Split this into three lanes: reseller reactivation, cPanel/DNS execution, and escalation-order discipline.
  2. Build a pre-escalation wrapper that enforces `repo truth -> capability matrix -> local tools -> vendor docs -> Fritz last`.
  3. Treat GoDaddy developer API as optional fallback truth, not the default credential chase, when cPanel UAPI/MCP proof already exists.
- **Repo/runtime truth:**
  - The gap row itself already encodes the expected sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `platform/README.md` says the current GoDaddy-hosted stack uses **cPanel UAPI/MCP as the primary hosting/DB/DNS control plane** and GoDaddy developer API as optional fallback.
  - `platform/registry/capabilities.json` documents `dns.register-subdomain` as `cPanel UAPI/MCP Zone Editor primary; GoDaddy direct DNS API optional fallback`.
  - `obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md` explicitly recommends making the pre-escalation wrapper executable rather than doctrinal.
  - The repo-verdict audit still lists `godaddy-cpanel-access` as an open `execution_routing_gap`, so the proof exists but the wrapper has not been built.
- **Recommended next move:** Treat this as a **routing-discipline gap**, not an unknown-hosting gap. Build the pre-escalation wrapper first; escalate only when reseller-account ownership, credentials, or irreversible account decisions are actually required.
- **Fritz authority required?** Yes for reseller-account decisions/credentials. No for the routing wrapper, vendor-proof gathering, or execution ordering.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as site-build capability failures, producing junk `capability_id` rows like `to` and `to_do_some_platform_work`.
- **Why it matters:** This pollutes the intelligence loop itself. Bad routing creates fake gap volume, weak promotions, and wasted research cycles.
- **Recurring?** **Yes, clearly recurring.** `intelligence-promotions.json` promotes `bucket-platform-conversation-context` at frequency `3`, the repo-verdict audit still shows `candidate_count: 0`, and the current curator note still names routing/type confusion as the loudest systems problem.
- **Researched options:**
  1. Add a conversation-type gate before `logGap()` so platform-work turns never hit generic gap logging.
  2. Use a small first-pass route set such as `site-build`, `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt`.
  3. Only bulk-clean malformed legacy rows after the gate exists, otherwise the junk regenerates.
- **Repo/runtime truth:**
  - `site-studio/lib/gap-logger.js` shows the root failure directly: unmatched rows fall back to `slugify(message.slice(0, 60))`, which is exactly how malformed capability IDs get created.
  - `~/.local/share/famtastic/intelligence-promotions.json` already recommends distinguishing platform/orchestration conversations from site-build conversations before gap logging writes malformed rows.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists this as a `routing_gap` with `candidate_count: 0`, meaning the system sees the problem but has not translated it into an implementation lane.
  - `obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md` says routing/type confusion is now a bigger system drag than missing-feature volume.
- **Recommended next move:** Build the conversation-type gate first, then convert legacy malformed rows into cleanup/closeout work after the logger can no longer recreate them.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- **Most actionable closeout:** `billing-usage-visibility` — the path exists, the old blocker is stale, and the next step is re-verification plus resume/closeout.
- **Most important discipline fix near income:** `godaddy-cpanel-access` — this is still escalation-order drift more than true access mystery.
- **Most important recurring intelligence-loop fix:** `platform-conversation-context` — still recurring, still unresolved, still contaminating the ledger.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/platform/README.md`
- `~/famtastic/platform/registry/capabilities.json`
- `~/famtastic/platform/capabilities/billing/README.md`
- `~/.shay/cron/jobs.json`
- `~/.shay/channel_directory.json`
- `shay cron list --all`
- Session recall via `session_search`