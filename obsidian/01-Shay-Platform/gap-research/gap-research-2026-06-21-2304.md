---
title: gap-research-2026-06-21-2304
type: note
generated_at: 2026-06-21 23:04 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-21-2304-1
---

# Gap Research — 2026-06-21 23:04

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, hot-context pointers, latest bookmark research, prior gap-research artifact, current cron/channel state, repo truth, session recall, and vendor-facing docs/pages reachable from this machine.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged as open even though the watchdog path already exists; the real break is that the watchdog is paused from an old delivery failure.
- **Why it matters:** Spend visibility protects Fritz’s wallet, catches silent model/provider burn, and supports the efficiency-first hierarchy.
- **Recurring?** Yes. This gap keeps resurfacing in prior research, curator notes, and repo-verdict artifacts.
- **Researched options:**
  1. Resume the existing watchdog instead of designing a new observability system.
  2. Add a local artifact/ledger write beside Telegram delivery so visibility survives channel drift.
  3. Keep invoice tooling and runtime usage telemetry separate; they are adjacent but not the same capability.
- **Repo truth / source trace:**
  - `shay cron list --all` shows **API Usage Watchdog** (`8bc7abc447b2`) still exists, is still **paused**, and still shows the stale failure `no delivery target resolved for deliver=telegram`.
  - `~/.shay/channel_directory.json` now contains a live Telegram DM target for Fritz, and `morning-briefing-telegram-alert` is active and delivering there. That means the original blocker is no longer the current reality.
  - `~/.shay/scripts/usage_watchdog.sh` already implements the core loop: `shay insights --days 1`, cost parsing, rate-limit/error scan, and `$10` threshold alerting.
  - `~/.shay/skills/autonomous-ai-agents/shay-shay/references/usage-watchdog.md` documents the watchdog as the intended successor to the earlier bleed-catching guardrail.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `billing-usage-visibility` as an open `ops_gap`.
- **Recommended next move:** Reclassify this as **implemented-but-paused ops infrastructure**. Verify the script against the current Telegram route, resume the cron, and add a local summary artifact so the signal is durable even if delivery routing drifts again.
- **Fritz authority required?** No for verification/resume. Yes only if threshold policy or budget policy changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before the existing local proof/capability surfaces were exhausted.
- **Why it matters:** This sits right next to reseller/account recovery and hosting execution — cash-adjacent work where bad escalation order burns Fritz attention and slows delivery.
- **Recurring?** Yes at the track level. The exact row is fresh, but the underlying reseller-vs-cPanel-vs-proof confusion keeps repeating.
- **Researched options:**
  1. Split the work into three lanes: reseller reactivation, cPanel/DNS execution, and escalation-order discipline.
  2. Add a pre-escalation wrapper that checks capability registry, proof artifacts, vendor facts, and vault/tool surfaces before surfacing uncertainty.
  3. Treat GoDaddy developer API as fallback truth, not default truth, when cPanel proof already exists.
- **Repo truth / source trace:**
  - The gap row itself already encodes the correct sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `platform/README.md` states the current GoDaddy-hosted stack uses **cPanel UAPI/MCP as the primary hosting/DB/DNS control plane** and GoDaddy developer API as optional registrar/direct-DNS fallback.
  - `platform/registry/capabilities.json` documents `dns.register-subdomain` as `cPanel UAPI/MCP Zone Editor primary; GoDaddy direct DNS API optional fallback`.
  - `proofs/mbsh-deploy-backend-2026-05-05-cpanel-uapi-overwrite.log` proves the cPanel fallback path was actually used successfully after rsync/SSH timed out; this was not hypothetical.
  - Vendor-side check from this machine confirms GoDaddy’s developer docs for the domains endpoint are live (`https://developer.godaddy.com/doc/endpoint/domains` returned HTTP 200), which supports keeping GoDaddy in the fallback/toolbox lane rather than treating it as unknown.
- **Recommended next move:** Treat this as a **routing-discipline gap**, not an access-mystery gap. Build the pre-escalation wrapper and only escalate when account ownership, reseller credentials, or irreversible vendor decisions are actually required.
- **Fritz authority required?** Yes for reseller-account decisions/credentials. No for the routing wrapper and proof-first execution order.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as if they were site-build capability failures, producing junk rows like `to`, `to_do_some_platform_work`, and similar malformed IDs.
- **Why it matters:** This pollutes the intelligence loop itself. Fake capability rows create false debt, waste research cycles, and weaken every downstream report built from the gap ledger.
- **Recurring?** **Yes, clearly recurring.** `intelligence-promotions.json` shows the promoted finding at frequency `3`, and the latest curator note still calls routing/typing errors the bigger drag than raw missing-feature volume.
- **Researched options:**
  1. Add a conversation-type gate before `logGap()` so `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt` turns do not fall into generic site-gap logging.
  2. Keep cleanup/backfill second; first stop the logger from recreating garbage.
  3. Convert promoted routing findings into implementation-state transitions instead of leaving them pending.
- **Repo truth / source trace:**
  - `site-studio/lib/gap-logger.js` still falls back to `slugify(message.slice(0, 60))` when no explicit `capability_id` is supplied. That is the direct mechanism creating malformed capability rows.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `platform-conversation-context` as a `routing_gap` with `candidate_count: 0`.
  - `obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md` explicitly says misrouting is the dominant recurring problem and recommends a **conversation-type gate before gap logging**.
  - Session recall (`session_search`) shows the same bucket repeatedly resurfacing across recent cron research runs.
  - External doc lookup tools were rate-limited on this machine tonight (web search/extract returned HTTP 432), so external comparison research did not add new evidence beyond local truth and session recall.
- **Recommended next move:** Build the conversation-type gate first, then bulk-clean the malformed legacy rows after the logger can no longer regenerate them.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- **Best immediate closeout:** `billing-usage-visibility` — not missing, just stale-paused.
- **Best discipline fix on the income path:** `godaddy-cpanel-access` — separate reseller authority from cPanel execution proof.
- **Best anti-drift fix overall:** `platform-conversation-context` — still recurring and still corrupting the ledger.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/gap-research/gap-research-2026-06-21-0814.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json`
- `~/famtastic/obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/platform/README.md`
- `~/famtastic/platform/registry/capabilities.json`
- `~/famtastic/proofs/mbsh-deploy-backend-2026-05-05-cpanel-uapi-overwrite.log`
- `~/.shay/scripts/usage_watchdog.sh`
- `~/.shay/skills/autonomous-ai-agents/shay-shay/references/usage-watchdog.md`
- `~/.shay/channel_directory.json`
- `shay cron list --all`
- `shay gateway status`
- `https://developer.godaddy.com/doc/endpoint/domains` (reachable vendor docs)
- Session recall via `session_search`