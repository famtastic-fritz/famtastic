---
title: gap-research-2026-06-21-0403
type: note
generated_at: 2026-06-21 04:03 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/latest-gap-research-2
---

# Gap Research — 2026-06-21 04:03

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, hot-context pointers, prior gap-research artifacts, session recall, and repo truth.

## Highest-value open gaps researched

### 1) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel access uncertainty got escalated to Fritz before the internal routing lattice was exhausted.
- **Why it matters:** This touches the reseller lane, hosting operations, and cash-adjacent recovery work. Bad escalation order burns Fritz attention on a path that should be proof-led.
- **Recurring?** Yes. The exact raw gap row is single-frequency, but the same underlying track keeps recurring across proof artifacts, capability docs, and prior research passes.
- **Researched options:**
  1. Split the work into three tracks: reseller-account recovery, cPanel/DNS execution, and escalation-order discipline.
  2. Add a pre-escalation checklist that must hit capability matrix, proof ledger, vendor-fact memory, and tool/capability surfaces before surfacing uncertainty.
  3. Treat GoDaddy API as optional fallback truth when cPanel UAPI proof already exists.
- **Repo truth:**
  - The gap row itself records the expected sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `proofs/studio-service-auth-mbsh-reunion-2026-05-05.json` and `proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json` both state: `Use cPanel UAPI/MCP for DNS/hosting operations; GoDaddy API is optional fallback`.
  - `memory/vendor-fact/cpanel-uapi-overwrite-is-the-path-for-backend-deploy-on-goda.md` says direct cPanel UAPI overwrite is the proven programmatic deploy path on this hosting.
  - `capability-engine/CAPABILITY-MATRIX.yaml` still marks reseller-management as a critical revenue gap, which means account recovery and execution routing should not be conflated.
- **Recommended next move:** Formalize the pre-escalation checklist as a reusable routing wrapper, then reclassify this gap from “access mystery” to “discipline/proof-order gap.”
- **Fritz authority required?** Yes for reseller credentials/account decisions. No for the routing wrapper and proof-first escalation discipline.
- **Confidence:** High

### 2) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being treated like site-build intake, which produces malformed `capability_id` rows such as `to` and `to_do_some_platform_work`.
- **Why it matters:** This degrades the intelligence loop itself. Bad conversation typing creates bad gap data, bad promotions, and noisy follow-up research.
- **Recurring?** **Yes, clearly recurring.** It appears in promotions with frequency `3`, and this same bucket was already called out in the two prior gap-research artifacts from 2026-06-20 and 2026-06-21 01:57.
- **Researched options:**
  1. Add a conversation-type gate before `logGap()` so platform/orchestration turns do not fall through to generic site-gap logging.
  2. Expand canonical bucket aliasing for platform-work phrases so even fallback logging lands in the right bucket.
  3. Backfill existing malformed rows only after the classifier/routing rule exists, otherwise the junk regenerates.
- **Repo truth:**
  - `site-studio/lib/gap-logger.js` shows the failure mode directly: if no bucket matches, it falls back to `slugify(message.slice(0, 60))`, which is exactly how malformed capability IDs are born.
  - `~/.local/share/famtastic/intelligence-promotions.json` already promotes `bucket-platform-conversation-context` with the explicit recommendation to distinguish platform/orchestration conversations from site-build conversations before logging.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json` groups the aliases and confirms this as a known recurring bucket.
  - Prior research artifacts at `obsidian/01-Shay-Platform/gap-research/gap-research-2026-06-20-2146.md` and `gap-research-2026-06-21-0157.md` already flagged this as a top intelligence-loop fix, so the recurrence is now at both raw-gap and research layers.
- **Recommended next move:** Treat this as an open classifier/routing fix. Build the conversation-type gate first, then mark the existing malformed rows for operational closeout.
- **Fritz authority required?** No
- **Confidence:** High

### 3) `to_improve_the_process_i_uploaded_the_current_star` (`media-prompt-handoff`)
- **capability_id:** `to_improve_the_process_i_uploaded_the_current_star`
- **Observed failure:** A media-generation handoff request (“send it to Leonardo AI”) got logged as malformed capability prose instead of being routed into a dedicated media workflow.
- **Why it matters:** Media Studio is supposed to turn outside prompts, references, and provider choices into structured generation work. If those requests become junk gap rows, the studio never gets a clean intake surface.
- **Recurring?** Not yet by raw frequency, but strategically yes: this is the same class of routing problem as earlier platform-context drift, now hitting Media Studio intake.
- **Researched options:**
  1. Create a dedicated media-prompt-handoff intake module that captures: source prompt, target provider, asset type, references/uploads, and desired output constraints.
  2. Route Leonardo-specific requests into a provider-aware handoff shape instead of generic gap logging.
  3. Reuse the existing Imagen/Veo/Leonardo provider matrix rather than inventing a new freeform prompt lane.
- **Repo truth:**
  - `FAMTASTIC-COMPLETE-PROMPT-V2.md` already documents a provider matrix with `Google Imagen 4.0`, `Google Veo 2.0`, and `Leonardo.ai` as active paths, plus Leonardo-specific options including `image guidance upload` and `character reference`.
  - `SITE-LEARNINGS.md` documents Leonardo Phoenix 1.0 as **tested and working** with active tokens, while also documenting the stronger Imagen→Veo pipeline for text-to-image→video work.
  - `site-studio/SHAY_CONTEXT.md` explicitly records a prior decision to use Leonardo character-reference mode for consistency when Gemini’s subject-reference path failed.
  - `~/.local/share/famtastic/intelligence-promotions.json` now promotes `bucket-media-prompt-handoff` and recommends routing external image/video prompt handoff requests into a dedicated media workflow instead of logging malformed capability prose.
- **Recommended next move:** Build a structured Media Studio handoff intake instead of treating these requests as gaps. Minimum fields: provider target, asset type, source prompt, uploaded/reference assets, consistency mode, and desired deliverable.
- **Fritz authority required?** No for intake/routing shape. Yes only if provider priority or paid-credit policy needs a business decision.
- **Confidence:** Medium-high

## Bottom line
- **Most important business-path fix:** `godaddy-cpanel-access` is still a routing/proof-order gap more than a true missing-capability gap.
- **Most important recurring intelligence-loop fix:** `platform-conversation-context` is still open and still polluting the ledger.
- **Most important new routing target:** `media-prompt-handoff` should become a structured Media Studio intake, not another malformed gap row.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/gap-research/latest-gap-research.md`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/capability-engine/CAPABILITY-MATRIX.yaml`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-2026-05-05.json`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json`
- `~/famtastic/memory/vendor-fact/cpanel-uapi-overwrite-is-the-path-for-backend-deploy-on-goda.md`
- `~/famtastic/FAMTASTIC-COMPLETE-PROMPT-V2.md`
- `~/famtastic/site-studio/SHAY_CONTEXT.md`
- `~/famtastic/SITE-LEARNINGS.md`
- Session recall for the recurring buckets via `session_search`