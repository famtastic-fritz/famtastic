---
title: gap-research-2026-06-19-0830
type: note
generated: 2026-06-19 08:30 EDT
source: cron gap-research worker
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-19-0830
---

# Gap Research — 2026-06-19 08:30

## Summary

No meaningful priority shift since the earlier same-day passes. The same three open leverage gaps are still the highest-value ones: premature GoDaddy/cPanel escalation, reseller reactivation, and domain registration. That recurrence is real signal. Repo truth still says the hosting control plane is **cPanel-first**, while several gap surfaces still frame the work as **GoDaddy-API-first**. That mismatch is part of why these gaps keep repeating.

---

## 1) `proactive-gap-escalation-order`

- **capability_id:** `proactive-gap-escalation-order`
- **observed failure:** `gaps.jsonl` contains a fresh open workflow gap logged on 2026-06-19 saying GoDaddy/cPanel access uncertainty was escalated to Fritz before the internal routing lattice was exhausted.
- **why it matters:** This is a Shay-core orchestration defect. It adds load to Fritz exactly where the platform is supposed to absorb ambiguity on its own.
- **researched options:**
  1. **cPanel-first truth already exists locally.** `platform/README.md` says the current GoDaddy-hosted stack uses cPanel UAPI/MCP as the primary hosting/DB/DNS control plane; GoDaddy API is optional fallback.
  2. **Direct DNS fallback already exists in code.** `platform/capabilities/dns/register-subdomain.sh` will use vaulted GoDaddy API credentials if present, otherwise it prints the manual registrar flow.
  3. **Known unsolved piece is narrower than “we don’t know how.”** `FAMTASTIC-STATE.md` still says addon-domain creation is manual because MCP/wrapper coverage is missing for `create_addon_domain`.
  4. **Truth-surface drift is contributing noise.** `FAMTASTIC-STATE.md` points to `docs/operating-rules/godaddy-mcp-spike.md`, but that file is not present now. The reference is stale even though the underlying hosting truth is still recoverable elsewhere.
- **recommended next move:**
  - Build a small pre-escalation checklist/skill for GoDaddy-hosted stack triage: **(a) cPanel-first check, (b) vault credential check, (c) existing wrapper/manual fallback check, (d) only then escalate to Fritz if owner login/2FA is truly required**.
  - Update stale doc references so the next agent does not chase a missing spike file.
- **Fritz authority required:** No for the checklist/doc fix. Yes only if the path ends in owner-only login, 2FA, or consent.
- **confidence:** High
- **recurring?:** Yes. Fresh gap row today, and the same access/control-plane confusion was already the top issue in earlier 2026-06-19 research artifacts.

---

## 2) `reseller-management`

- **capability_id:** `reseller-management`
- **observed failure:** `capability-engine/GAP-TRACKER.yaml` still marks reseller management as `critical`, `open`, and explicitly describes it as the “30-min cash unlock.” Hot-context still names reseller reactivation as the priority income lane.
- **why it matters:** This remains the fastest direct revenue unlock in current truth surfaces.
- **researched options:**
  1. **Current tracker framing is too monolithic.** The open resolution plan still bundles reactivation, API integration, domain registration, and wholesale pricing into one step.
  2. **Local platform truth reduces the actual first move.** Because hosting/DNS provisioning is already modeled as cPanel-first, reseller reactivation does not need to solve the full hosting automation story before it becomes useful.
  3. **JJ BA / income adjacency keeps pointing back here.** `plans/five-stream-state-2026-06-09.md` explicitly ties FAMtasticHosting, reseller auth, and JJ BA priority into the same revenue-pressure lane.
  4. **Existing billing capability is only partial.** `platform/registry/capabilities.json` shows invoice generation exists, but payment provider/send/reconciliation are still open. That makes reseller/storefront activation even more important as a near-term commerce surface.
- **recommended next move:**
  - Split this into two modules in the next planning surface:
    1. **Reseller reactivation ops** — access, storefront/settings, pricing, payout visibility, customer/delegate flow.
    2. **API feasibility** — only after ops access is verified, determine what reseller/domain endpoints the live account actually qualifies for.
- **Fritz authority required:** Likely yes for owner-only account settings, 2FA, or payout/business decisions. No for decomposition and prep artifacts.
- **confidence:** High
- **recurring?:** Yes. It remains open in gap tracker, hot-context, and prior same-day research. This is not a stale one-off.

---

## 3) `domain-registration`

- **capability_id:** `domain-registration`
- **observed failure:** `capability-engine/GAP-TRACKER.yaml` and `CAPABILITY-MATRIX.yaml` still mark domain registration as an open critical gap with no automated purchase flow.
- **why it matters:** This blocks true end-to-end site sales. Every serious site pipeline eventually needs availability, purchase, DNS, and propagation.
- **researched options:**
  1. **The dependency is still real.** The gap tracker correctly notes reseller activation is upstream.
  2. **But the downstream shape needs reframing.** `CAPABILITY-MATRIX.yaml` still describes the future flow as `check-availability → purchase-domain → configure-dns → verify-propagation`, while `platform/README.md` says DNS should be cPanel-first / GoDaddy-fallback. Those two truths are not aligned yet.
  3. **There is already partial provisioning code.** `platform dns register-subdomain` exists, but it is still a stub/manual-fallback wrapper rather than a complete provisioning lane.
  4. **The real blocker is split across commerce and provisioning.** Purchase flow is missing; addon-domain/DNS wrapper coverage is also missing.
- **recommended next move:**
  - Reframe the gap as three modules:
    1. **Account/API eligibility probe**
    2. **Registrar purchase workflow**
    3. **cPanel provisioning workflow** (addon-domain + DNS wrapper coverage)
  - Do not treat “domain registration” as one giant GoDaddy-only task.
- **Fritz authority required:** Not yet for decomposition and research. Yes later for provider/account policy decisions and any live commercial action.
- **confidence:** High
- **recurring?:** Yes. This remains the direct downstream companion to the reseller gap and is still formally open in both canonical capability truth surfaces.

---

## Observation vs Interpretation

### Observations
- `intelligence-promotions.json` is still absent.
- The only fresh explicit row in `gaps.jsonl` is the workflow gap around premature GoDaddy/cPanel escalation.
- `GAP-TRACKER.yaml` still marks `reseller-management` and `domain-registration` as critical open gaps.
- `platform/README.md` says the live hosting stack is cPanel-first, but some gap plans still assume GoDaddy-first automation.
- `FAMTASTIC-STATE.md` references `docs/operating-rules/godaddy-mcp-spike.md`, but that file is missing now.

### Interpretation
- The repeated confusion is partly architectural and partly documentation drift: the platform truth moved toward cPanel-first, while some planning/gap surfaces still think in GoDaddy-first terms.
- The fastest cleanup is not “build everything” — it is aligning the truth surfaces, then separating reseller ops, domain commerce, and cPanel provisioning into distinct modules.
- This run produced confirmation, not a new direction. The same three gaps are still the right targets.

---

## Recommended next research packet

1. Build a durable **GoDaddy-hosted stack triage** checklist/skill from the cPanel-first truth.
2. Rewrite the **reseller-management** gap into ops-first vs API-feasibility modules.
3. Run a narrow **cPanel wrapper coverage audit** for addon-domain + DNS automation and update stale doc pointers.