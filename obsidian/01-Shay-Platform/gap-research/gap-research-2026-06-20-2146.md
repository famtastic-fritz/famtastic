---
title: gap-research-2026-06-20-2146
type: note
generated_at: 2026-06-20 21:46:00-04:00
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-20-2146
---

# Gap Research — 2026-06-20 21:46

## Scope
Reviewed open gaps from `~/.local/share/famtastic/gaps.jsonl`, current promotions, hot-context pointers, repo truth in `~/famtastic`, cron state, and relevant vendor docs reachability.

## Highest-value gaps researched

### 1) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **Observed failure:** GoDaddy/cPanel access uncertainty was escalated to Fritz before Shay exhausted internal routing/capability surfaces.
- **Why it matters:** This sits directly on the reseller-income path and on hosting/DNS operations. Wrong escalation order burns Fritz attention and delays cash-unlock work.
- **Recurring?** Not yet high-frequency in the ledger, but it is attached to a broader recurring bucket (`godaddy-cpanel-access`) and the hot-context file separately calls reseller reactivation the priority cash unlock.
- **Repo truth / research:**
  - `platform/registry/capabilities.json` already records `dns.register-subdomain` with **cPanel UAPI/MCP as primary** and GoDaddy DNS API as **optional fallback**.
  - `proofs/studio-service-auth-mbsh-reunion-2026-05-05.json` and `...-v2-2026-05-05.json` show `studio.cpanel.api_token` present and `studio.dns_control` set to cPanel UAPI/MCP primary.
  - Multiple MBSH proof logs show cPanel fallback paths already worked in practice when SSH failed (`mbsh-deploy-backend-2026-05-05-cpanel-uapi-overwrite.log`, `mbsh-db-apply-schema-2026-05-05-final.log`, `mbsh-cors-lockdown-2026-05-05.log`).
  - Vendor-doc reachability check succeeded for GoDaddy docs homepage; direct cPanel doc guess 404'd, but local repo proof is stronger here than web docs.
- **Researched options:**
  1. **Best:** split this bucket into separate tracks: `dns-hosting-execution`, `reseller-account-recovery`, and `escalation-order`. Current bucket is mixing access, vendor choice, and workflow discipline.
  2. Add a pre-escalation check that reads the capability registry + proof artifacts before any Fritz escalation on hosting/DNS topics.
  3. Add a narrow reseller recovery checklist artifact so the income task stops sharing a bucket with general cPanel operations.
- **Recommended next move:** Treat this as **mostly a routing/process gap, not an access gap**. Open a follow-up artifact or task that formalizes: capability-registry check → proof lookup → vendor docs lookup → only then Fritz escalation. Also split reseller recovery out as its own bucket/task.
- **Fritz authority required?** **Yes, for actual reseller-account decisions/credentials. No, for fixing escalation order and bucket split.**
- **Confidence:** **High**

### 2) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **Observed failure:** Billing/API-usage tracking was requested, then left as an acknowledged future need instead of being routed into a dedicated ops workstream.
- **Why it matters:** Cost visibility protects the brain lane, prevents silent subscription drain, and supports revenue-quality tradeoffs.
- **Recurring?** The raw gap only appears once, but the need is reinforced by current runtime evidence and prior system notes.
- **Repo truth / research:**
  - `SITE-LEARNINGS.md` already documents `GET /api/telemetry/sdk-cost-summary` and cost logging via `lib/api-telemetry.js`.
  - `SITE-LEARNINGS.md` also documents the later “brain keeps switching” diagnosis: Max subscription depletion caused fallback churn, proving cost/usage visibility is operationally load-bearing, not cosmetic.
  - `shay cron list --all` shows an existing `API Usage Watchdog` cron job (`usage_watchdog.sh`), but it is **paused** and its last delivery failed because no Telegram delivery target resolved.
  - OpenTelemetry and Langfuse docs were reachable enough to confirm the general external pattern: token/cost telemetry belongs in the observability layer, not scattered across ad hoc logs.
- **Researched options:**
  1. **Fastest closeable move:** revive the existing watchdog delivery path and validate its target resolution.
  2. Add a small local summary artifact that snapshots spend/usage independent of push delivery, so visibility survives messaging drift.
  3. Longer-term: unify `sdk-cost-summary`, watchdog alerts, and control-plane reporting into one cost surface instead of separate partial views.
- **Recommended next move:** Mark this gap as **partially solved in repo truth but operationally incomplete**. The instrumentation exists; the missing piece is making the watchdog/delivery/reporting loop reliable and visible. Next session should verify `usage_watchdog.sh`, fix delivery target resolution, and close or downgrade the gap record accordingly.
- **Fritz authority required?** **No** for watchdog/delivery wiring and artifact generation. **Maybe** if threshold policy or paid provider usage limits need his explicit call.
- **Confidence:** **High**

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **Observed failure:** Platform/orchestration conversations were treated like site-build conversations, producing malformed capability IDs and polluted gap rows.
- **Why it matters:** This degrades the intelligence loop itself — bad routing means bad buckets, bad promotions, and noisy backlog.
- **Recurring?** **Yes. Clearly recurring.** Frequency is 3 across at least two site tags, and promotions already call it out as pending.
- **Repo truth / research:**
  - `site-studio/lib/gap-logger.js` shows why this happens: if a message does not match an existing bucket alias, the logger falls back to `slugify(message.slice(0, 60))`, which explains malformed capability IDs like `to` and `to_do_some_platform_work`.
  - The logger can bucket known aliases, but it does **not** itself distinguish conversation lane/type before logging.
  - `problem-buckets.latest.json` and `intelligence-promotions.json` both show this bucket is already recognized as a recurring routing gap.
  - `reports/intelligence-system-hard-checklist-2026-06-20.md` explicitly says the current live consumer path for `gaps.jsonl` is only partial, with cleanup/control-plane workers doing normalization after the fact.
- **Researched options:**
  1. **Best:** classify lane/context **before** gap logging: platform/orchestration vs site-build vs client-brief vs priority-interrupt.
  2. Short-term patch: extend bucket aliases and pre-normalization rules so malformed platform prose collapses into the correct bucket faster.
  3. Structural fix: move gap logging ownership out of site-studio-only assumptions into an ecosystem capture layer when the message is clearly not site work.
- **Recommended next move:** Add a pre-gap classifier or lane guard ahead of `site-studio/lib/gap-logger.js` so non-site conversations never mint raw slug capability IDs. This is the cleanest fix because post-hoc cleanup is already proving too late.
- **Fritz authority required?** **No**
- **Confidence:** **High**

## Bottom line
- **Most actionable immediate closeout:** `billing-usage-visibility` — instrumentation exists, but the watchdog delivery loop is paused/broken.
- **Most important intelligence-loop fix:** `platform-conversation-context` — clearly recurring and currently creating bad data.
- **Most important business-path clarification:** `godaddy-cpanel-access` — repo truth shows cPanel capability already exists; the real miss is escalation order and bucket overloading, not total access absence.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/platform/registry/capabilities.json`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-2026-05-05.json`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json`
- `~/famtastic/proofs/mbsh-deploy-backend-2026-05-05-cpanel-uapi-overwrite.log`
- `~/famtastic/proofs/mbsh-db-apply-schema-2026-05-05-final.log`
- `~/famtastic/proofs/mbsh-cors-lockdown-2026-05-05.log`
- `~/famtastic/SITE-LEARNINGS.md`
- `shay cron list --all`