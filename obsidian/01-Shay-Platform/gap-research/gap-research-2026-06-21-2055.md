---
title: gap-research-2026-06-21-2055
type: note
generated_at: 2026-06-21 20:55 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/latest-gap-research-4
---

# Gap Research — 2026-06-21 20:55

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`, `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`, `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`, live `shay cron list --all`, live `~/.shay/channel_directory.json`, `~/.shay/scripts/usage_watchdog.sh`, `SITE-LEARNINGS.md`, `platform/README.md`, `platform/registry/capabilities.json`, `proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json`, `site-studio/lib/gap-logger.js`, `specs/006-component-studio-wrapper-foundation/proof.md`, `data-center/jobs/component-20260521151957-wave-6-component-reuse-proof/outputs/component-reuse-proof.json`, `docs/DEEP-DISCOVERY-2.md`, shay cron docs, session recall, and direct vendor-page reachability checks.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged open while the existing `API Usage Watchdog` remains paused with a stale Telegram delivery error.
- **Why it matters:** Direct wallet protection. This is the shortest path from open gap to operational value because the machinery already exists.
- **Recurring?** Yes. This same gap has survived repeated research passes today without operational closeout.
- **Researched options:**
  1. Resume and verify the existing watchdog instead of designing a new system.
  2. Add a local artifact output so cost/usage proof survives messaging-route drift.
  3. Keep runtime usage telemetry separate from invoice/billing workflows.
- **Repo truth / live truth:**
  - `gaps.jsonl` still lists the capability as `status: "open"` under bucket `billing-usage-visibility`.
  - `problem-buckets.latest.json` still marks `billing-usage-visibility` open.
  - Live `shay cron list --all` at `2026-06-21 20:55 EDT` still shows `API Usage Watchdog` present but **paused**, with stale delivery text `no delivery target resolved for deliver=telegram`.
  - The same live cron output shows `morning-briefing-telegram-alert` active and successfully delivering to `telegram:Fritz Medine (dm)`.
  - `~/.shay/channel_directory.json` now contains Fritz’s Telegram DM target and was updated at `2026-06-21T20:51:26`, so the old watchdog delivery error is stale state, not current platform reality.
  - `~/.shay/scripts/usage_watchdog.sh` already implements the intended no-agent watchdog flow via `shay insights --days 1`, cost parsing, error parsing, and a `$10` alert threshold.
  - Shay docs at `website/docs/guides/cron-script-only.md` explicitly position no-agent cron mode for watchdog/threshold alerts where stdout is the message.
  - Site Studio already has telemetry surfaces (`site-studio/lib/api-telemetry.js`, `GET /api/telemetry/sdk-cost-summary`) documented in repo truth, so this is not a greenfield observability problem.
- **Recommended next move:** Reclassify this as **implemented-but-paused ops infrastructure**. Verify the script against the now-valid Telegram route, resume the cron, and add a local markdown/json proof artifact so the gap can be closed with evidence.
- **Fritz authority required?** No for verify/resume. Yes only if spend threshold or budget policy changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order`
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before the internal routing/proof ladder was exhausted.
- **Why it matters:** This burns Fritz attention on an income-adjacent path tied to reseller recovery, hosting operations, and DNS execution.
- **Recurring?** Yes at the pattern level. The raw row appears once, but the same routing confusion persists across the bucket, proof docs, and follow-up research.
- **Researched options:**
  1. Split the problem into four tracks: reseller recovery, cPanel execution, registrar/direct-DNS fallback, and escalation-order discipline.
  2. Add a pre-escalation wrapper that enforces `capability matrix -> agent tools -> vendor/repo truth -> Fritz last`.
  3. Keep cPanel UAPI/MCP as the primary control plane and treat GoDaddy API as fallback only.
- **Repo truth / vendor truth:**
  - `gaps.jsonl` still lists `proactive-gap-escalation-order` as open under bucket `godaddy-cpanel-access`.
  - `problem-buckets.latest.json` still marks `godaddy-cpanel-access` open and already recommends splitting vendor access, DNS operations, reseller recovery, and escalation order into separate proof-backed tracks.
  - The gap row itself already contains the right `expected_sequence`; what is missing is enforcement.
  - `proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json` records `studio.dns_control` as `primary` with detail: `Use cPanel UAPI/MCP for DNS/hosting operations; GoDaddy API is optional fallback`.
  - `platform/registry/capabilities.json` says `dns.register-subdomain` is backed by `cPanel UAPI/MCP Zone Editor primary; GoDaddy direct DNS API optional fallback`.
  - `platform/README.md` repeats the same doctrine: for the current GoDaddy-hosted stack, cPanel UAPI/MCP is the primary hosting/DB/DNS control plane and GoDaddy developer API keys are not the default credential to chase.
  - Direct reachability check succeeded for `https://developer.godaddy.com/doc/endpoint/domains` (HTTP 200), while the guessed cPanel endpoint used in this run returned 404. That strengthens the conclusion that repo truth is currently the more reliable source for the cPanel-first rule.
- **Recommended next move:** Treat this as a **routing-discipline gap**, not an access mystery. Build the pre-escalation wrapper/checklist and split the lane so only true ownership/credential decisions escalate to Fritz.
- **Fritz authority required?** Yes for reseller ownership or irreversible account actions. No for the routing wrapper and preflight discipline.
- **Confidence:** High

### 3) `video_background_option` / `video_background_support`
- **capability_id:** `video_background_option` (`video_background_support` alias in same bucket)
- **Observed failure:** The intelligence audit still shows `video-background` as open even though repo truth now strongly proves a reusable `video-hero` capability exists.
- **Why it matters:** This is classic proof-closeout drift. Leaving solved capability rows open pollutes the gap ledger and makes future research less trustworthy.
- **Recurring?** Yes at the closure-drift level. Two legacy gap rows remain open in the bucket view despite newer proof artifacts and component evidence.
- **Researched options:**
  1. Close the legacy gap rows as solved-in-repo and preserve the proof links in the closeout note.
  2. If desired, rename the bucket from generic “video background” to the concrete reusable capability `video-hero`.
  3. Only reopen if a new user-facing failure shows the existing component/path is broken, not merely undocumented.
- **Repo truth:**
  - `gaps.jsonl` marks both raw rows as `status: "verified"` with mutation reason: `Existing component/library/skeleton paths prove capability exists in repo truth; open gap is proof/closure drift.`
  - `problem-buckets.latest.json` still shows the `video-background` bucket as `status: "open"`, which now appears stale relative to the underlying gap rows.
  - `components/video-hero/video-hero.html` exists as a concrete component path.
  - `data-center/jobs/component-20260521151957-wave-6-component-reuse-proof/outputs/component-reuse-proof.json` shows query `cinematic hero with video background and CTA`, selected component `video-hero`, and describes `autoplay video background ... loops silently with poster image fallback`.
  - `specs/006-component-studio-wrapper-foundation/proof.md` records `video-hero` as the top candidate for that exact query.
  - `docs/DEEP-DISCOVERY-2.md` records `video-hero` used on `site-altitude`, `site-groove-theory`, and `site-lawn-company`, with successful ship history on `site-altitude` and `site-groove-theory`.
- **Recommended next move:** Mark the bucket as **resolved in repo truth / awaiting operational closeout**, then update the bucket audit so it no longer advertises this as an open capability gap.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- The most actionable ops closeout is still `billing-usage-visibility` because the implementation already exists and the blocker now looks stale.
- The most important discipline fix is still `proactive-gap-escalation-order` because it protects Fritz attention on the reseller/hosting path.
- The cleanest **close-the-ledger** candidate this run is `video-background`: repo truth already proves the capability, so the open bucket is drift, not missing functionality.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`
- `~/.shay/channel_directory.json`
- `~/.shay/scripts/usage_watchdog.sh`
- Live `shay cron list --all`
- `site-studio/lib/gap-logger.js`
- `SITE-LEARNINGS.md`
- `platform/README.md`
- `platform/registry/capabilities.json`
- `proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json`
- `specs/006-component-studio-wrapper-foundation/proof.md`
- `data-center/jobs/component-20260521151957-wave-6-component-reuse-proof/outputs/component-reuse-proof.json`
- `docs/DEEP-DISCOVERY-2.md`
- `website/docs/guides/cron-script-only.md`
- Session recall via `session_search`
- Direct reachability checks: `https://developer.godaddy.com/doc/endpoint/domains`, `https://opentelemetry.io/docs/`, guessed cPanel docs URL returning 404