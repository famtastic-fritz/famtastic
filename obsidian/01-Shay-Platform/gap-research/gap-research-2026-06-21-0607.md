---
title: gap-research-2026-06-21-0607
type: note
generated_at: 2026-06-21 06:07 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-21-0607
---

# Gap Research — 2026-06-21 06:07

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, hot-context pointers, latest bookmark research, prior gap-research artifacts, session recall, cron state, and current repo truth.

## Highest-value open gaps researched

### 1) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel access uncertainty was escalated to Fritz before the internal routing lattice was exhausted.
- **Why it matters:** This sits on the reseller/cash lane. Bad escalation order burns Fritz attention on a path that repo truth already narrowed.
- **Recurring?** Yes. The raw gap row is frequency 1, but the underlying routing confusion keeps resurfacing across proof artifacts, capability docs, and repeated gap-research passes.
- **Researched options:**
  1. Split the issue into three separate tracks: reseller-account recovery, cPanel/DNS execution, and escalation-order discipline.
  2. Add a pre-escalation routing wrapper that must verify capability matrix, proof artifacts, vendor-fact memory, and local tooling before surfacing uncertainty.
  3. Treat GoDaddy API as optional fallback truth, not the primary path, when cPanel proof already exists.
- **Repo truth:**
  - The open gap row itself records the expected sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `platform/README.md` says the GoDaddy-hosted stack uses **cPanel UAPI/MCP as the primary hosting/DB/DNS control plane** and GoDaddy API as optional fallback.
  - `platform/registry/capabilities.json` marks `dns.register-subdomain` as `v1-manual-prompt` with **cPanel UAPI/MCP Zone Editor primary; GoDaddy direct DNS API optional fallback**.
  - `proofs/studio-service-auth-mbsh-reunion-2026-05-05.json` and `...-v2-2026-05-05.json` both state: `Use cPanel UAPI/MCP for DNS/hosting operations; GoDaddy API is optional fallback`.
  - `memory/vendor-fact/cpanel-uapi-overwrite-is-the-path-for-backend-deploy-on-goda.md` records cPanel UAPI overwrite as the proven programmatic backend deploy path on this hosting.
  - `capability-engine/CAPABILITY-MATRIX.yaml` still marks `reseller-management` as a **CRITICAL GAP** and calls it the `30-min cash unlock cha-ching lane`, which means account reactivation and execution routing should not be conflated.
- **Recommended next move:** Reclassify this as a proof-order/routing-discipline gap, then build the pre-escalation wrapper so future GoDaddy/cPanel uncertainty gets exhausted internally before surfacing.
- **Fritz authority required?** **Yes** for reseller credentials/account decisions. **No** for the routing wrapper and proof-first escalation discipline.
- **Confidence:** High

### 2) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility was requested, but the capability never became a dependable always-on ops surface.
- **Why it matters:** Spend visibility protects the brain lane, catches provider drift early, and supports Fritz's efficiency-first priority. This is operational, not cosmetic.
- **Recurring?** Yes. The raw gap row is old, and the same issue was already called out in multiple prior gap-research artifacts and intelligence-audit outputs.
- **Researched options:**
  1. Resume the existing watchdog path and point it at a real delivery target.
  2. Promote spend/usage visibility from a side cron into a control-plane invariant with local artifacts plus push delivery.
  3. Keep invoice/billing generation separate from API-usage telemetry; both matter, but they are different capabilities.
- **Repo truth:**
  - `shay cron list --all` shows an existing `API Usage Watchdog` cron job, but it is **paused** and its last delivery failed with `no delivery target resolved for deliver=telegram`.
  - `~/.shay/cron/jobs.json` confirms that failure mode on the watchdog job.
  - `~/.shay/channel_directory.json` now contains a valid Telegram DM target for Fritz, which means the earlier delivery failure is stale state, not a missing surface today.
  - `platform/capabilities/billing/README.md` proves billing/invoice generation exists as a separate capability, but it does **not** solve API-usage observability.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `billing-usage-visibility` as an open `ops_gap` with no strong repo candidate adopted.
  - Prior gap-research artifacts from `2026-06-20 21:46`, `2026-06-20 23:50`, and `2026-06-21 01:57` already flagged this as a top actionable gap, so the recurrence is now explicit at both artifact and ops layers.
- **Recommended next move:** Treat this as an operational closeout lane: unpause/fix the watchdog delivery path, then decide whether its proof should also feed the control-plane loop so cost/usage is continuously visible.
- **Fritz authority required?** No for restoring the visibility loop. Yes only if a new paid telemetry provider or budget threshold policy is needed.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being treated like site-build intake, which produces malformed `capability_id` rows such as `to` and `to_do_some_platform_work`.
- **Why it matters:** This degrades the intelligence loop itself. Bad conversation typing creates junk gap rows, noisy promotions, and wasted research cycles.
- **Recurring?** **Yes, clearly recurring.** Promotions show frequency `3`, and this same bucket has already been called out in multiple prior research artifacts.
- **Researched options:**
  1. Add a conversation-type gate before `logGap()` so platform/orchestration turns never fall through to generic site-gap logging.
  2. Expand canonical bucket aliasing for platform-work phrases so fallback logging still lands in the right bucket.
  3. Backfill malformed rows only after the routing rule exists; otherwise the junk just regenerates.
- **Repo truth:**
  - `site-studio/lib/gap-logger.js` shows the failure mode directly: if no bucket matches, it falls back to `slugify(message.slice(0, 60))`, which is exactly how malformed capability IDs are created.
  - `~/.local/share/famtastic/intelligence-promotions.json` already promotes `bucket-platform-conversation-context` and explicitly recommends distinguishing platform/orchestration conversations from site-build conversations before logging.
  - `obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md` says this is still one of the most repeated buckets.
  - Prior reports at `obsidian/01-Shay-Platform/gap-research/gap-research-2026-06-20-2146.md`, `...2026-06-20-2350.md`, `...2026-06-21-0157.md`, and `...2026-06-21-0403.md` all flagged the same issue, so this is now a recurring research finding, not just a recurring raw gap.
- **Recommended next move:** Build the conversation-type gate first, then mark the malformed rows for operational closeout once the logger cannot recreate them.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- **Most important business-path fix:** `godaddy-cpanel-access` is still a routing/proof-order gap more than a true access mystery.
- **Most actionable ops fix:** `billing-usage-visibility` already has a watchdog lane; the missing part is reliable activation and delivery.
- **Most important recurring intelligence-loop fix:** `platform-conversation-context` is still open, clearly recurring, and still polluting the ledger.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/gap-research/latest-gap-research.md`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/platform/README.md`
- `~/famtastic/platform/registry/capabilities.json`
- `~/famtastic/platform/capabilities/billing/README.md`
- `~/famtastic/capability-engine/CAPABILITY-MATRIX.yaml`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-2026-05-05.json`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json`
- `~/famtastic/memory/vendor-fact/cpanel-uapi-overwrite-is-the-path-for-backend-deploy-on-goda.md`
- `~/.shay/cron/jobs.json`
- `~/.shay/channel_directory.json`
- Session recall via `session_search`
