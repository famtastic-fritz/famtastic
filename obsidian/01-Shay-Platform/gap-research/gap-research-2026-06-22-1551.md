---
title: gap-research-2026-06-22-1551
type: note
generated_at: 2026-06-22 15:51 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-22-1551-1
---

# Gap Research — 2026-06-22 15:51

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, hot-context pointers, latest bookmark research, current curator note, repo-verdict audit, current cron/channel truth, local repo truth, and targeted session recall.

## Heartbeat
No material priority shift since the earlier 2026-06-22 run. The same three highest-value open gaps still dominate, and all three remain unresolved in repo/runtime truth.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility was requested, but the protection loop is still paused even though the delivery route that originally blocked it now exists.
- **Why it matters:** This is direct money protection. It protects spend, catches provider drift, and fits Fritz’s efficiency-first hierarchy better than inventing a new billing surface.
- **Recurring?** **Yes.** It remains open, and multiple prior research runs land on the same conclusion: stale-paused infrastructure, not missing invention.
- **Researched options:**
  1. Restore the existing `API Usage Watchdog` instead of designing a new telemetry system.
  2. Add a local artifact/log output alongside Telegram delivery so proof survives future channel drift.
  3. Keep invoice generation/payment rails separate from runtime spend visibility; repo billing docs prove those are different surfaces.
- **Repo/runtime truth:**
  - `shay cron list --all` still shows `API Usage Watchdog` present, paused, pointed at `usage_watchdog.sh`, with the old delivery error `no delivery target resolved for deliver=telegram`.
  - `~/.shay/channel_directory.json` now contains a live Telegram DM target for Fritz (`telegram -> Fritz Medine`), and `morning-briefing-telegram-alert` is currently active and delivering there.
  - `~/.shay/cron/jobs.json` still records the watchdog as paused with stale `last_delivery_error`.
  - `platform/capabilities/billing/README.md` proves billing work today is invoice/ledger generation plus manual payment handling, not runtime usage observability.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `billing-usage-visibility` as an open `ops_gap`.
- **Recommended next move:** Reclassify this as **implemented-but-paused ops infrastructure**. Re-verify `usage_watchdog.sh` against the now-valid Telegram route, then resume it or close it out with fresh blocker proof.
- **Fritz authority required?** No for verification/resume. Yes only if threshold policy, budget policy, or paid observability tooling changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated to Fritz before the internal proof surfaces and routing lattice were fully exhausted.
- **Why it matters:** This sits adjacent to the reseller-income lane. Bad escalation order burns Fritz attention and delays cash-adjacent work.
- **Recurring?** **Yes, at the pattern level.** The exact row is newer, but the same confusion keeps resurfacing: reseller access, GoDaddy API, and cPanel execution get blended together when repo truth already separates them.
- **Researched options:**
  1. Split this into three lanes: reseller reactivation, cPanel/DNS execution, and escalation-order discipline.
  2. Build a pre-escalation wrapper that enforces `repo truth -> capability matrix -> local tools -> vendor docs -> Fritz last`.
  3. Treat GoDaddy developer API as optional fallback truth, not the default credential chase, when cPanel UAPI/MCP proof already exists.
- **Repo/runtime truth:**
  - The open gap row already encodes the correct expected sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `platform/README.md` states the current GoDaddy-hosted stack uses **cPanel UAPI/MCP as the primary hosting/DB/DNS control plane** and GoDaddy developer API only as optional registrar/direct-DNS fallback.
  - `obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md` explicitly recommends turning the pre-escalation wrapper into executable behavior instead of leaving it doctrinal.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists `godaddy-cpanel-access` as an open `execution_routing_gap`.
  - Session recall from 2026-06-21 keeps landing on the same verdict: this is routing-discipline drift more than genuine platform ignorance.
- **Recommended next move:** Treat this as a **routing-discipline gap**, not an unknown-hosting gap. Build the wrapper first; escalate only when reseller-account ownership, credentials, or irreversible account decisions are actually required.
- **Fritz authority required?** Yes for reseller-account ownership/credentials and irreversible account decisions. No for routing logic, proof gathering, or execution ordering.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as site-build capability failures, producing junk `capability_id` rows like `to` and `to_do_some_platform_work`.
- **Why it matters:** This pollutes the intelligence loop itself. Bad routing creates fake gap volume, weak promotions, and wasted research cycles.
- **Recurring?** **Yes, clearly recurring.** `intelligence-promotions.json` still promotes `bucket-platform-conversation-context` at frequency `3`, and repeated research/session recall keeps surfacing the same failure.
- **Researched options:**
  1. Add a conversation-type gate before `logGap()` so platform-work turns never hit generic gap logging.
  2. Use a small first-pass route set such as `site-build`, `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt`.
  3. Only bulk-clean malformed legacy rows after the gate exists, otherwise the junk regenerates.
- **Repo/runtime truth:**
  - `site-studio/lib/gap-logger.js` still falls back to `const capId = details.capability_id || slugify(message.slice(0, 60));`, which is exactly how malformed capability IDs are created.
  - `~/.local/share/famtastic/intelligence-promotions.json` already recommends distinguishing platform/orchestration conversations from site-build conversations before generic gap logging writes malformed rows.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json` still lists this as a `routing_gap` with `candidate_count: 0`.
  - `obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md` still names type confusion as the loudest systems problem.
  - Session recall from 2026-06-21 again points to the same missing fix: build the gate first, then clean legacy rows.
- **Recommended next move:** Build the conversation-type gate first, then convert legacy malformed rows into cleanup/closeout work after the logger can no longer recreate them.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- **Most actionable closeout:** `billing-usage-visibility` — existing path, stale blocker, re-verification next.
- **Most important income-adjacent discipline fix:** `godaddy-cpanel-access` — routing drift more than access mystery.
- **Most important recurring intelligence-loop fix:** `platform-conversation-context` — still recurring and still contaminating the ledger.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/internal-curator/latest-curator-note.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-repo-verdicts.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/platform/README.md`
- `~/famtastic/platform/capabilities/billing/README.md`
- `~/.shay/channel_directory.json`
- `~/.shay/cron/jobs.json`
- `shay cron list --all`
- Session recall via `session_search`