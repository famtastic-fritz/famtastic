---
title: gap-research-2026-06-22-0318
type: note
generated_at: 2026-06-22 03:18 EDT
source_job: shay-gap-silent-research
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-22-0318
---

# Gap Research — 2026-06-22 03:18

## Scope
Reviewed `~/.local/share/famtastic/gaps.jsonl`, `~/.local/share/famtastic/intelligence-promotions.json`, `obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`, `obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`, prior gap-research artifacts, current cron/channel state, repo truth, session recall, and reachable vendor docs from this machine.

## Highest-value open gaps researched

### 1) `to_be_able_to_track_billing._api_use._etc.._bt_lat` (`billing-usage-visibility`)
- **capability_id:** `to_be_able_to_track_billing._api_use._etc.._bt_lat`
- **Observed failure:** Billing/API-usage visibility is still logged as open even though the watchdog path already exists; the real break is that the watchdog remains paused from an old Telegram delivery failure.
- **Why it matters:** This is direct wallet protection. It catches silent spend/rate-limit drift and supports the efficiency-first rule.
- **Recurring?** Yes. This same stale-paused shape has survived multiple prior research runs.
- **Researched options:**
  1. Resume the existing watchdog instead of building a new observability system.
  2. Add a local artifact/ledger write beside Telegram delivery so usage proof survives channel drift.
  3. Keep invoice tooling and runtime spend telemetry separate; adjacent, not the same capability.
- **Repo truth / source trace:**
  - `shay cron list --all` at `2026-06-22 03:18 EDT` still shows **API Usage Watchdog** (`8bc7abc447b2`) present but **paused**, with stale failure text `no delivery target resolved for deliver=telegram`.
  - `~/.shay/channel_directory.json` now contains a live Telegram DM target for Fritz, and `morning-briefing-telegram-alert` is active/delivering there. The old blocker is no longer current reality.
  - `~/.shay/scripts/usage_watchdog.sh` already implements the loop: `shay insights --days 1`, cost parsing, rate/error scan, and `$10` threshold alerting.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json` still marks `billing-usage-visibility` open.
  - `platform/capabilities/billing/README.md` proves billing primitives exist, but they stop at invoice generation/state tracking and do not cover runtime usage observability.
- **Recommended next move:** Reclassify this as **implemented-but-paused ops infrastructure**. Re-verify the script against the current Telegram route, resume the cron, and add a local summary artifact so the signal is durable even if push delivery drifts again.
- **Fritz authority required?** No for verification/resume. Yes only if spend threshold or budget policy changes.
- **Confidence:** High

### 2) `proactive-gap-escalation-order` (`godaddy-cpanel-access`)
- **capability_id:** `proactive-gap-escalation-order`
- **Observed failure:** GoDaddy/cPanel uncertainty was escalated before existing local proof and capability surfaces were exhausted.
- **Why it matters:** This touches the reseller/cash path. Bad escalation order burns Fritz attention on account-access fog when the execution lane is already partly mapped.
- **Recurring?** Yes at the bucket level. The exact row is newer, but the underlying reseller-vs-cPanel-vs-proof confusion is clearly repeating.
- **Researched options:**
  1. Split this into three lanes: reseller reactivation, cPanel/DNS execution, and escalation-order discipline.
  2. Add a pre-escalation wrapper/checklist that enforces `capability matrix -> local tools/proof -> vendor truth -> Fritz last`.
  3. Keep GoDaddy developer API as fallback truth, not default truth, when cPanel proof already exists.
- **Repo truth / source trace:**
  - The gap row itself already encodes the expected sequence: `detect_gap -> log_gap -> check_capability_matrix -> consult_agent_tools -> search_vendor_and_repo_truth -> escalate_to_fritz_last`.
  - `platform/registry/capabilities.json` documents `dns.register-subdomain` as `cPanel UAPI/MCP Zone Editor primary; GoDaddy direct DNS API optional fallback`.
  - The same registry shows `db.add` backed by `GoDaddy MariaDB via cpanel-mcp + local Homebrew/Docker for dev`, which means repo truth already separates registrar/vendor ownership from runtime execution.
  - `platform/README.md` adds an important constraint: DNS registration is still a `manual_required`/stub path today, so the gap is not “GoDaddy unknown,” it is “proof-backed lane exists but still has manual execution edges.”
  - `proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json` records `studio.dns_control` with detail `Use cPanel UAPI/MCP for DNS/hosting operations; GoDaddy API is optional fallback`.
  - Vendor reachability check from this machine returned HTTP 200 for `https://developer.godaddy.com/doc/endpoint/domains`.
- **Recommended next move:** Treat this as a **routing-discipline gap with a partial-manual execution edge**, not an access-mystery gap. Build the pre-escalation wrapper and only surface uncertainty to Fritz when ownership, credential consent, or irreversible vendor choices are actually required.
- **Fritz authority required?** Yes for reseller-account decisions/credentials. No for the routing wrapper and proof-first sequencing.
- **Confidence:** High

### 3) `special_case_for_platfor_convos` (`platform-conversation-context`)
- **capability_id:** `special_case_for_platfor_convos`
- **Observed failure:** Platform/orchestration conversations are still being logged as if they were site-build capability failures, producing junk rows like `to`, `to_do_some_platform_work`, and similar malformed IDs.
- **Why it matters:** This corrupts the intelligence loop itself. Fake capability debt wastes research cycles and makes every downstream report noisier.
- **Recurring?** **Yes, clearly recurring.** Promotions show frequency `3`, the bucket remains open, and prior reports keep surfacing the same issue.
- **Researched options:**
  1. Add a conversation-type gate before `logGap()` so `platform-work`, `media-intake`, `brief-intake`, and `priority-interrupt` turns do not fall into generic site-gap logging.
  2. Stop regeneration first; do cleanup/backfill second.
  3. Turn promoted routing findings into implementation-state transitions instead of leaving them perpetually pending.
- **Repo truth / source trace:**
  - `site-studio/lib/gap-logger.js` still falls back to `slugify(message.slice(0, 60))` when no explicit `capability_id` is supplied. That fallback is the direct mechanism creating malformed rows.
  - `~/.local/share/famtastic/intelligence-promotions.json` already promotes `bucket-platform-conversation-context` and recommends distinguishing platform/orchestration conversations from site-build conversations before gap logging writes malformed rows.
  - `obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json` still marks `platform-conversation-context` open at frequency `3`.
  - `site-studio/lib/gap-logger.js` already has bucket normalization, promotion, and existing-context lookup, so the missing piece is upstream routing/type detection, not another cleanup pass.
- **Recommended next move:** Build the conversation-type gate first, then bulk-clean the malformed legacy rows after the logger can no longer recreate them.
- **Fritz authority required?** No
- **Confidence:** High

## Bottom line
- **Best immediate closeout:** `billing-usage-visibility` — not missing, just stale-paused.
- **Best income-path discipline fix:** `godaddy-cpanel-access` — separate reseller authority from cPanel execution proof.
- **Best anti-drift fix overall:** `platform-conversation-context` — still recurring and still poisoning the ledger.

## Sources checked
- `~/.local/share/famtastic/gaps.jsonl`
- `~/.local/share/famtastic/intelligence-promotions.json`
- `~/famtastic/obsidian/Shay-Memory/research/github-bookmarks-report-latest.md`
- `~/famtastic/obsidian/01-Shay-Platform/HOT-CONTEXT-POINTERS.md`
- `~/famtastic/obsidian/01-Shay-Platform/gap-research/latest-gap-research.md`
- `~/famtastic/obsidian/01-Shay-Platform/intelligence-audits/problem-buckets.latest.json`
- `~/famtastic/site-studio/lib/gap-logger.js`
- `~/famtastic/platform/registry/capabilities.json`
- `~/famtastic/platform/README.md`
- `~/famtastic/proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json`
- `~/.shay/channel_directory.json`
- `~/.shay/cron/jobs.json`
- `~/.shay/scripts/usage_watchdog.sh`
- `shay cron list --all`
- `session_search` recall for recurring buckets
- `https://developer.godaddy.com/doc/endpoint/domains`
