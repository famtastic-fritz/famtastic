---
title: latest-gap-research
type: note
generated: 2026-06-19 04:19:00-04:00
source: cron gap-research worker
permalink: famtastic/01-shay-platform/gap-research/latest-gap-research-2
---

# Gap Research — 2026-06-19 04:19

## Summary

Reviewed the open gap ledger at `/Users/famtasticfritz/.local/share/famtastic/gaps.jsonl` plus current repo truth surfaces. Highest-value unresolved gaps this run were the GoDaddy/cPanel access-recovery orchestration miss and the two revenue-critical reseller/domain gaps already declared in the capability engine. None are solved yet, but one of them is partly reframed by newer repo truth: for the current GoDaddy-hosted stack, cPanel UAPI/MCP is the primary control plane, not raw GoDaddy API chasing.

---

## 1) proactive-gap-escalation-order

- **capability_id:** `proactive-gap-escalation-order`
- **observed failure:** Gap log records that GoDaddy/cPanel access uncertainty was escalated to Fritz before internal routing was exhausted. Expected sequence explicitly says: detect gap → log gap → check capability matrix → consult agent tools → search vendor/repo truth → escalate to Fritz last. Source: `gaps.jsonl` line 13.
- **why it matters:** This is a cognitive-load failure, not just a missing credential. It burns Fritz time exactly where the system is supposed to reduce it.
- **researched options:**
  1. **Repo truth first:** Current platform docs say cPanel UAPI/MCP is the primary hosting/DB/DNS control plane for the GoDaddy-hosted stack; GoDaddy developer API keys are only optional fallback (`SITE-LEARNINGS.md:1116-1130`, `platform/README.md:40-46`).
  2. **Known local proof exists:** Studio auth proof already records `studio.cpanel.api_token` as present and `studio.dns_control` as `primary`, with GoDaddy API described as optional fallback (`proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json:55-62`).
  3. **Vendor recovery path exists when customer-account access is the issue:** Session recall and GoDaddy docs both point to Reseller Control Center → Reports → Customer Lookup → Request/Use delegate access. This was specifically recalled in prior sessions around `famtasticthoughts.com` recovery, and vendor docs confirm it.
  4. **Remaining real unsolved piece:** cPanel automation still lacks wrapper coverage for addon-domain/DNS steps, so some actions may still require manual cPanel or a wrapper extension rather than Fritz intervention (`FAMTASTIC-STATE.md:562-574`, `SITE-LEARNINGS.md:1128-1130`).
- **recommended next move:**
  - Treat this as an orchestration policy fix first: add a hard pre-escalation checklist to the gap-handling lane so GoDaddy/cPanel questions must verify `studio.cpanel.api_token`, `studio.dns_control`, existing proof packets, and Reseller Control Center recovery paths before involving Fritz.
  - After that, create a small reusable skill/checklist for “GoDaddy-hosted stack access triage” with branches for **cPanel-first**, **delegate-access recovery**, and **addon-domain wrapper missing**.
- **Fritz authority required:** **No** for the process fix. **Yes** only if a real human-owned login/2FA consent step remains after internal checks are exhausted.
- **confidence:** High
- **recurring?:** Yes. This exact miss is newly logged, and the underlying reseller/access theme is recurring across hot-context notes, prior sessions, and MBSH deployment blockers.

---

## 2) reseller-management

- **capability_id:** `reseller-management`
- **observed failure:** Capability matrix and gap tracker both mark reseller management as a critical open gap: account exists, neglected ~10 years, “30-min cash unlock,” not activated into an actual operating income lane (`GAP-TRACKER.yaml:11-21`, `CAPABILITY-MATRIX.yaml:606-620`).
- **why it matters:** This is one of the highest revenue-leverage gaps in the whole system. It sits directly inside Fritz’s stated priority order: efficiency, automation, revenue.
- **researched options:**
  1. **Reseller Control Center operations path:** GoDaddy vendor docs show the control center already supports customer lookup, delegate access, navigation, pricing, promotions, custom domain, and nameserver defaults. That means some reactivation work is operational/configurational before it is API engineering.
  2. **API path is available but gated:** Current vendor/public references indicate GoDaddy domain API access is constrained by account/domain thresholds for some endpoints, especially availability. This means the “integrate API” step should be validated against actual account eligibility before architecture depends on it.
  3. **Current stack truth reduces scope:** Repo truth says hosting/DB/DNS control should stay cPanel-first. So reseller activation can focus on storefront/pricing/customer/delegate workflows first, instead of wrongly making GoDaddy API the mandatory first move.
  4. **Operational split emerges:** There are really two submodules here: **reactivate reseller business operations** and **later automate reseller APIs**. Treating them as one blob is slowing the cash-unlock path.
- **recommended next move:**
  - Split this gap into two modules:
    - **Module A: reseller reactivation ops** — verify RCC access, pricing, product nav, support settings, payout/commission visibility, customer lookup, branding.
    - **Module B: API automation feasibility** — verify actual API eligibility, supported endpoints, and whether domain purchase/availability automation is commercially worth it now.
  - Prioritize Module A first because it is the fastest path to actual revenue signal.
- **Fritz authority required:** **Likely yes** for any final business-owner decisions, 2FA, payouts, or legal/account settings. **No** for the research split and prep artifact.
- **confidence:** High
- **recurring?:** Yes. It is repeated in hot-context pointers, capability tracker, capability matrix, and recent session recall as a standing “cash unlock” lane.

---

## 3) domain-registration

- **capability_id:** `domain-registration`
- **observed failure:** Domain registration remains declared as a critical gap with no automated purchase flow; capability matrix still says “NOT BUILT” and depends on the neglected reseller lane (`GAP-TRACKER.yaml:23-33`, `CAPABILITY-MATRIX.yaml:132-145`).
- **why it matters:** This blocks true end-to-end site sales. Every client site eventually hits domain purchase, DNS, and propagation verification.
- **researched options:**
  1. **Keep dependency explicit:** Domain registration is downstream of reseller activation. That dependency is correct and should remain explicit.
  2. **Separate purchase from DNS control:** Repo truth now says DNS/hosting control is cPanel-first in the current stack. So the future flow should likely be: availability/purchase via registrar or reseller path, then DNS/addon-domain provisioning through cPanel where possible.
  3. **Known blocker already documented:** cPanel wrapper coverage for addon-domain/DNS automation is still missing, and `create_addon_domain` remains a known manual or extension point (`FAMTASTIC-STATE.md:572-574`, `SITE-LEARNINGS.md:1128-1130`).
  4. **API risk:** If account/API eligibility is limited, availability checking may not be safe to assume as the first automated surface.
- **recommended next move:**
  - Reframe the implementation plan into three explicit modules:
    1. **Eligibility probe** — confirm what GoDaddy/reseller APIs the live account can actually use.
    2. **Purchase workflow** — design the registrar-side domain buy flow only after eligibility is proven.
    3. **Provisioning workflow** — extend cPanel MCP/UAPI coverage for addon-domain + DNS automation, because that is the current stack’s real bottleneck.
  - If eligibility is weak, consider a fallback architecture where reseller ops stay manual first while cPanel-side provisioning gets automated immediately.
- **Fritz authority required:** **Not yet** for research/planning. **Yes later** for registrar/business-policy choices if a fallback provider or account consolidation decision is needed.
- **confidence:** Medium-High
- **recurring?:** Yes. This is the direct downstream companion to the reseller gap and appears in both capability truth surfaces as still-open critical infrastructure.

---

## Observation vs Interpretation

### Observations
- The only fresh explicit `gaps.jsonl` item is the orchestration miss around premature GoDaddy/cPanel escalation.
- Repo truth strongly says the current GoDaddy-hosted stack should be handled cPanel-first, not GoDaddy-API-first.
- Reseller activation and domain registration remain formally open critical gaps in both the gap tracker and capability matrix.
- Local proof already shows cPanel token presence and primary DNS-control direction.

### Interpretation
- The biggest immediate improvement is not a new API integration; it is correcting the decision lattice so future access problems route through existing cPanel/proof/vendor paths before touching Fritz.
- The reseller gap is probably oversized in its current framing. Splitting “reactivate the business lane” from “automate the API lane” is the cleanest way to get to cha-ching sooner.
- Domain registration should not move into build mode until account/API eligibility is verified, otherwise the plan risks anchoring on a non-available vendor path.

---

## Recommended next research packet

1. Build a **GoDaddy-hosted access triage** skill/checklist from the cPanel-first truth.
2. Create a **reseller reactivation ops brief** separate from API automation.
3. Run a narrow **GoDaddy eligibility probe** artifact: which live endpoints/thresholds are actually usable for Fritz’s current account state.