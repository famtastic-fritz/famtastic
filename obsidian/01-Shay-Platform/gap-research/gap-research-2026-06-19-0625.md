---
title: gap-research-2026-06-19-0625
type: note
generated: 2026-06-19 06:25 EDT
source: cron gap-research worker
permalink: famtastic/01-shay-platform/gap-research/gap-research-2026-06-19-0625
---

# Gap Research — 2026-06-19 06:25

## Summary

Reviewed the open gap ledger at `/Users/famtasticfritz/.local/share/famtastic/gaps.jsonl`, current repo truth, hot-context pointers, prior same-day research artifacts, and targeted vendor docs. Highest-value unresolved gaps this run remain the same three leverage points: the premature GoDaddy/cPanel escalation miss, reseller reactivation, and domain registration. That recurrence is signal, not noise: the platform keeps circling the same income/control-plane holes because they are still open.

---

## 1) proactive-gap-escalation-order

- **capability_id:** `proactive-gap-escalation-order`
- **observed failure:** `gaps.jsonl` logs a fresh workflow gap where GoDaddy/cPanel access uncertainty was escalated to Fritz before the internal routing lattice was exhausted. The expected sequence is explicitly recorded in the gap row itself: detect gap → log gap → check capability matrix → consult agent tools → search vendor/repo truth → escalate to Fritz last.
- **why it matters:** This is a load-bearing orchestration miss. It adds cognitive load exactly where Shay is supposed to remove it.
- **researched options:**
  1. **cPanel-first truth already exists in repo:** `platform/README.md` says the current GoDaddy-hosted stack uses cPanel UAPI/MCP as the primary hosting/DB/DNS control plane, with GoDaddy direct API only as optional fallback.
  2. **Proof packet already confirms that direction:** `proofs/studio-service-auth-mbsh-reunion-v2-2026-05-05.json` records `studio.cpanel.api_token` present and DNS control as primary through cPanel.
  3. **Known unsolved piece is narrower than “access uncertainty”:** `FAMTASTIC-STATE.md` and `platform/README.md` both say addon-domain / DNS wrapper coverage is still incomplete, which means the remaining gap is wrapper coverage, not total platform blindness.
  4. **Vendor recovery path exists for reseller-customer access issues:** GoDaddy help confirms Reseller Control Center → Reports → Customer Lookup → request/use delegate access, with 2FA as prerequisite.
  5. **cPanel API surface exists for the missing wrapper lane:** cPanel docs still expose addon-domain and ZoneEdit operations, even though some surfaces are older/deprecated API families. That means the missing wrapper is buildable; it is not blocked by vendor impossibility.
- **recommended next move:**
  - Add a hard pre-escalation checklist/skill for GoDaddy-hosted stack triage with three branches: **cPanel-first**, **delegate-access recovery**, **wrapper-missing/manual-cPanel fallback**.
  - Treat future escalations in this lane as a process defect unless those checks are attached as proof.
- **Fritz authority required:** **No** for the checklist/process fix. **Yes** only if the flow ends in a real account-owner login/2FA or business-owner consent step.
- **confidence:** High
- **recurring?:** **Yes.** Freshly logged today, and the same reseller/GoDaddy/cPanel access confusion appears across session recall and deployment truth surfaces.

---

## 2) reseller-management

- **capability_id:** `reseller-management`
- **observed failure:** `capability-engine/GAP-TRACKER.yaml` and `CAPABILITY-MATRIX.yaml` still mark reseller management as a critical open gap. Repo truth continues to describe it as a neglected but live “30-min cash unlock” lane.
- **why it matters:** This is the highest direct revenue-leverage open gap in current truth surfaces. It sits upstream of domain sales, storefront ops, and a real income lane.
- **researched options:**
  1. **Current gap framing is too big:** the gap tracker’s resolution plan bundles reactivation, API integration, registration flow, and wholesale pricing into one blob. That framing is slowing the fastest cash path.
  2. **Vendor docs point to an ops-first lane:** GoDaddy RCC documentation and help/tutorial surfaces show customer lookup, branding, support settings, marketing, pricing, and storefront management as immediate control-center actions before any API engineering.
  3. **Repo truth reduces technical scope:** the platform already decided cPanel-first for hosting/DNS/DB. Reseller reactivation does not need to solve hosting automation first.
  4. **API feasibility remains conditional:** GoDaddy docs indicate basic Domains API access can exist with one active domain, but availability endpoints require either 50+ domains or roughly $20/month average spend. That makes “integrate API” a validation task, not a safe assumption.
- **recommended next move:**
  - Split the gap into two explicit modules:
    1. **Reseller reactivation ops** — RCC access, pricing, storefront/support settings, commission/payout visibility, customer/delegate flow.
    2. **API automation feasibility** — what endpoints the live account actually qualifies for, especially availability/purchase surfaces.
  - Prioritize module 1 first because it is the shortest path to actual revenue proof.
- **Fritz authority required:** **Likely yes** for owner-only actions like 2FA, payout/account settings, or final business-policy choices. **No** for the research split and preparatory artifact work.
- **confidence:** High
- **recurring?:** **Yes.** This exact lane repeats across hot-context, capability truth surfaces, and prior same-day gap research.

---

## 3) domain-registration

- **capability_id:** `domain-registration`
- **observed failure:** `GAP-TRACKER.yaml` and `CAPABILITY-MATRIX.yaml` still mark domain registration as a critical open gap with no automated purchase flow. It explicitly depends on reseller reactivation.
- **why it matters:** This blocks true end-to-end site sales. Every serious client pipeline eventually hits availability, purchase, DNS, and propagation.
- **researched options:**
  1. **Dependency remains real:** the gap tracker is right that reseller reactivation is upstream.
  2. **But the downstream architecture needs reframing:** repo truth says DNS/provisioning should be cPanel-first, so the future domain flow should separate **registrar purchase** from **cPanel provisioning** instead of treating it as one GoDaddy-only blob.
  3. **API eligibility is the gating research fact:** GoDaddy help says domain availability API access is limited to qualifying accounts (50+ domains or spend threshold), so availability checking cannot be assumed.
  4. **Provisioning bottleneck is already known locally:** current docs say addon-domain / DNS automation is still missing wrapper coverage, which is likely the real blocker on the provisioning side.
- **recommended next move:**
  - Restate the gap as three modules:
    1. **Eligibility probe** — verify which GoDaddy/reseller API surfaces the live account can actually use.
    2. **Purchase workflow** — only design purchase/availability automation after eligibility is proven.
    3. **Provisioning workflow** — build/extend cPanel addon-domain + ZoneEdit wrapper coverage.
  - If eligibility is weak, keep purchase/manual for now and automate provisioning first.
- **Fritz authority required:** **Not yet** for research and decomposition. **Yes later** if provider/account-policy decisions or commercial fallback choices are needed.
- **confidence:** High
- **recurring?:** **Yes.** It is the direct downstream companion to the reseller gap and remains formally open in both canonical capability truth surfaces.

---

## Observation vs Interpretation

### Observations
- `intelligence-promotions.json` is not present.
- The only fresh explicit `gaps.jsonl` row is the workflow-gap around premature GoDaddy/cPanel escalation.
- Reseller management and domain registration remain open critical gaps in the capability engine.
- Repo truth still says the live hosting stack is cPanel-first, not GoDaddy-API-first.
- Vendor docs confirm delegate-access recovery exists and API eligibility for some domain endpoints is conditional.

### Interpretation
- The immediate fix is architectural discipline, not brute-force escalation: future access issues should route through cPanel truth, proof packets, and delegate-access recovery before touching Fritz.
- Reseller reactivation is still the fastest revenue unlock, but only if it is decomposed into ops-first and API-feasibility-second.
- Domain registration should not enter build mode as one monolithic “GoDaddy API” task; it needs separate eligibility, purchase, and provisioning lanes.

---

## Recommended next research packet

1. Build a durable **GoDaddy-hosted stack access triage** checklist/skill from the cPanel-first truth.
2. Create a **reseller reactivation ops brief** separate from API automation.
3. Run a narrow **GoDaddy endpoint eligibility probe** artifact and a **cPanel wrapper coverage audit** for addon-domain/ZoneEdit.