#!/usr/bin/env bash
set -euo pipefail
. "$(dirname "$0")/../../scripts/_cj_common.sh"


tag="${1:-latest-convo}"
echo "[set-claude] implement publish back to Claude if desired (reading $CJ_SOURCES_DIR/claude/$tag.jsonl)"
