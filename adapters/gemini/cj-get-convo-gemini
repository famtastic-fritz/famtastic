#!/usr/bin/env bash
set -euo pipefail
. "$(dirname "$0")/../../scripts/lib/hub-common"


tag="${1:-latest-convo}"
demo="${2:-}"
dir="$CJ_SOURCES_DIR/gemini"; mkdir -p "$dir"
out="$dir/$tag.jsonl"
if [ "$demo" = "--demo" ]; then
  echo '{"messages":[{"role":"user","content":"demo from gemini"}]}' >> "$out"
  echo "[get-gemini] wrote demo line → $out"
  exit 0
fi
echo "[get-gemini] implement real fetch here (CLI/API) → append JSON lines to $out"
