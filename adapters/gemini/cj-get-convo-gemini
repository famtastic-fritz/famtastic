#!/usr/bin/env bash
set -euo pipefail
. "$(dirname "$0")/../../scripts/lib/hub-common"


TAG="${1:-latest-convo}"
GEMINI_API_KEY="$(secret_get GEMINI_API_KEY || true)"
export GEMINI_API_KEY


# existing demo append (keep)
PROV="$(basename "$(dirname "$0")")"
DIR="$CJ_SOURCES_DIR/$PROV"; mkdir -p "$DIR"
echo '{"role":"user","content":"stub from '"$PROV"'","ts":"'"$(date -u +%FT%TZ)"'","agent":"'"$PROV"'"}' >> "$DIR/$TAG.jsonl"
echo "[$PROV] wrote demo line → $DIR/$TAG.jsonl"
