#!/usr/bin/env bash
set -euo pipefail
TAG="${1:-latest-convo}"; SRC="${XDG_DATA_HOME:-$HOME/.local/share}/famtastic/agent-hub/sources/gemini/$TAG.jsonl"
[ -f "$SRC" ] || { echo "[gemini:set] no source for $TAG"; exit 0; }
echo "[gemini:set] (stub) would publish back from $SRC"