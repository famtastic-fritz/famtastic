#!/usr/bin/env bash
set -euo pipefail
. "$(dirname "$0")/../../scripts/_cj_common.sh"


tag="${1:-latest-convo}"
echo "[set-codex] implement publish back to Codex if desired (reading $CJ_SOURCES_DIR/codex/$tag.jsonl)"
