#!/usr/bin/env bash
set -euo pipefail
. "$(dirname "$0")/../../scripts/_cj_common.sh"


tag="${1:-latest-convo}"
demo="${2:-}"
dir="$CJ_SOURCES_DIR/codex"; mkdir -p "$dir"
out="$dir/$tag.jsonl"
if [ "$demo" = "--demo" ]; then
  echo '{"messages":[{"role":"user","content":"demo from codex"}]}' >> "$out"
  echo "[get-codex] wrote demo line → $out"
  exit 0
fi
echo "[get-codex] implement real fetch here (CLI/API) → append JSON lines to $out"
