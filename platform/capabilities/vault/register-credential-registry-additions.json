{
  "_note": "Additions only — do NOT edit platform/registry/capabilities.json directly. The parent session merges this record into the capabilities[] array. Shape matches platform/registry/capabilities.json exactly.",
  "capabilities": [
    {
      "id": "vault.register-credential",
      "invocation": "platform vault register-credential <service> [--key <vault-key>]",
      "implementation": "capabilities/vault/register-credential.sh",
      "backing_service": "macOS Keychain (via vault/vault.sh write) — friendly-name allowlist mapping service -> canonical vault key, with per-capability verification guidance",
      "status": "v1",
      "reads_from_vault": [],
      "writes_to_vault": [
        "studio.resend.api_key (service: resend)",
        "reach/resend.api_key (service: reach-resend)",
        "reach/email.to (service: reach-email-to)",
        "reach/email.from (service: reach-email-from)",
        "studio.godaddy.api_key (service: godaddy)",
        "studio.godaddy.api_secret (service: godaddy-secret)",
        "netlify.auth_token (service: netlify)",
        "reach/telegram.bot_token (service: telegram)",
        "reach/telegram.chat_id (service: telegram-chat)",
        "reach/twilio.account_sid (service: twilio-sid)",
        "reach/twilio.auth_token (service: twilio-token)",
        "reach/twilio.from_number (service: twilio-from)",
        "reach/twilio.to (service: twilio-to)",
        "jira.api_token (service: jira)",
        "github.token (service: github)",
        "cpanel.api_token (service: cpanel)",
        "<custom> (via --key override)"
      ],
      "spec_fields_read": [],
      "spec_fields_written": [
        "platform/invocations/<date>.jsonl (capability=vault.register-credential audit line, value-free)"
      ],
      "manual_steps": [
        "Run on Fritz's Mac: vault is Keychain-backed and provider APIs are firewalled from the cloud sandbox",
        "Supply the secret via the CRED_VALUE env var or stdin — NEVER as a CLI argument (shell history + process table leak)",
        "After registering, run the printed verification step and confirm live vs manual_required"
      ]
    }
  ]
}
