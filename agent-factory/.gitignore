# Sandbox runtime artifacts — never committed
.venv/
__pycache__/
*.pyc
data/*.db
data/*.db-journal
data/*.db-wal
data/*.db-shm
.env

# Logs are regenerated every run; keep the dir, ignore contents
logs/*.log

# Generated dashboard snapshot (regenerated on demand)
public/dashboard.html
