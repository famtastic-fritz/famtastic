# Isolated sandbox git-dir (kept out of the parent repo)
.sandbox-git/

# Python
__pycache__/
*.pyc
.venv/
venv/

# Local secrets — never commit
.env

# Runtime database (regenerated by run_demo.py)
data/*.db
data/*.db-journal
