# secrets
.env

# python env
.venv/
__pycache__/
*.pyc

# runtime data (the DB is regenerated by seed.py)
data/*.db
data/*.db-wal
data/*.db-shm

# minted worker instances (regenerated from template by the orchestrator)
agents/worker_*.py

# logs and generated artifacts are KEPT in git as proof-of-run by default.
# If you prefer clean diffs, uncomment:
# logs/*.log
# outputs/*.md
# dashboard/status.html
