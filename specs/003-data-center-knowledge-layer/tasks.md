# Tasks

- [x] Extend Data Center schemas and primitives for sources, claims, and decisions.
- [x] Ingest `captures/inbox` and `captures/review` into source records with stable IDs and provenance.
- [x] Add `scripts/data-center-ingest.js` with `--dry-run` and `--json`.
- [x] Add deterministic tests for idempotency, redaction, and linkage.
- [x] Write Wave 3 proof/report/docs updates.
