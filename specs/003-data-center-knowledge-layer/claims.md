# Claims

1. The Data Center can safely treat the existing capture inbox/review folders as raw intake and project them into source records without duplicating the inbox surface.
2. Stable source IDs plus a compact local index are sufficient for bounded idempotent ingestion in the current repo.
3. Claim and decision artifacts can stay local JSON files for now while still carrying enough linkage to support later graph/export work.
