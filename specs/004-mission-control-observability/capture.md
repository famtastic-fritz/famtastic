# Capture: Wave 4 Mission Control Observability

Raw capture behavior is intentionally unchanged. Mission Control reads `captures/inbox/` only to report a count and bounded sample paths, preserving the existing inbox as raw intake.

No raw files were moved, deleted, or promoted by this wave.
