# Learn

- The existing Data Center sanitization rules are enough for witness metadata too; no separate redaction path was needed.
- The Site Studio research-router coverage is best registered through its existing vitest command, not a duplicated test harness.
- A low-diversity run with mixed success needed a stricter boundary (`diversity <= 0.5`) to classify as suspicious instead of productive.
