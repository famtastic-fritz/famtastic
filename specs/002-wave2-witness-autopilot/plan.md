# Plan

1. Extend `lib/famtastic/data-center/index.js` with witness storage and schema support.
2. Add `scripts/witness-check.js` with bounded local checks and baseline registration.
3. Add a shared autopilot classifier under `lib/famtastic/autopilot/`.
4. Add deterministic Node tests for witness and autopilot behavior.
5. Run the witness checks for real, write proof/status observations, update docs, and verify.
