# P0.4 Hotfix — Gate Re-run Report
**Date:** 2026-04-27
**Hotfix commit:** `a2c0baf` (`fix(p0.4): flip design_brief.approved=true at brief synthesis time`)
**Reference:** `architecture/2026-04-27-p0-exit-gate-results.md` (original failure diagnosis)
**Scope:** Re-run P0 exit gates 1, 2, 4 only — Gates 3 and 5 already passed and were not re-run.

---

## What this report verifies

The P0.4 hotfix was a single root-cause fix: `design_brief.approved` was never being set to `true` post-build. The fix has two mechanical preconditions for the gate re-run:

1. **Built sites must have `design_brief.approved === true`** — verifiable from disk without running Studio.
2. **The classifier must route post-build chat messages to non-`new_site` intents** — verifiable by reading the classifier code path against the new spec state.

These are mechanically verified below. The full UI round-trip (Shay confirm → build → chat edit → preview reflects → deploy → URL match) requires a live operator with browser + Claude API access, which is not reproducible from this session. The operator-side runbook for finishing the gate verification is at the bottom of this report.

---

## Mechanical verification

### Pre-check 1 — `design_brief.approved` across built sites

```
site-tonys-barber-shop:           state=built, design_brief.approved=True
site-jj-ba-transport:             state=built, design_brief.approved=True
site-jj-ba-transport2:            state=built, design_brief.approved=True
site-the-daily-grind-in-atlanta:  state=built, design_brief.approved=True
site-small-accounting-firm:       state=built, design_brief.approved=True
```

All five sites confirmed via direct spec.json read post-migration. The `spec-repair-all` migration backfilled 7 sites; the remaining 19 either had `approved=true` already, were not in a built state, or had no `design_brief` (correctly skipped).

### Pre-check 2 — Smoke assertion script

```
$ ./scripts/smoke-assert-built-spec site-tonys-barber-shop
state: "built"
design_brief.approved: true
interview_completed: true
pages: ["home","services","contact"]
=== Exit: 0 (all invariants hold) ===

$ ./scripts/smoke-assert-built-spec site-jj-ba-transport
state: "built"
design_brief.approved: true
interview_completed: true
pages: ["home","services","contact"]
=== Exit: 0 (all invariants hold) ===
```

The new `scripts/smoke-assert-built-spec` (P0.4) returns exit 0 on both targets. Step 5b of the smoke suite is now wired and will gate post-build runs going forward.

### Pre-check 3 — Classifier code path against new spec state

The regression flow (per gate diagnostic):

```js
// server.js:11308
const hasBrief = spec && spec.design_brief && spec.design_brief.approved;

// server.js:11453
if (!hasBrief) {
  return 'new_site';  // ← what fired pre-P0.4 on every post-build message
}
```

With `design_brief.approved === true` now persisted on all built sites, `hasBrief` evaluates `true`, and the classifier proceeds past line 11453 into the keyword/pattern gates that handle `content_update`, `deploy`, `restyle`, etc. The classifier itself was not changed by P0.4 — only the spec data it reads.

### Pre-check 4 — Test suite

```
Test Files  4 passed (4)
Tests       181 passed (181)
```

No regressions from P0.4 changes.

---

## Operator-side gate verification (UI round-trip required)

The remaining gate verification depends on driving the actual UI. The operator should run the following with Studio + browser open:

### Gate 1 — Smoke suite (re-run from Step 6)

Steps 1-5 already passed in the original run; the regression hit at Step 6. Steps 1-5b should be re-run as a sanity check (Step 5b is new in P0.4) before resuming at Step 6.

Per `architecture/2026-04-27-smoke-suite.md`:

- **Step 5b (NEW):** `./scripts/smoke-assert-built-spec site-<tag>` should exit 0 immediately after the build completes (i.e. before any chat edit is attempted). If this fails on a fresh build, **stop** — the P0.4 fix didn't take, and Steps 6-9 will silently misclassify.
- **Step 6:** Send `Change the hero headline to "<X>"` via `#chat-input`. Expected: classifier returns `content_update`, surgical edit applies, `mutations.jsonl` gets a new entry, no `parallelBuild` log lines for this turn.
- **Step 7:** Verify the edit applied without rebuild.
- **Step 8:** Click → Staging. Expected: classifier returns `deploy`, `runDeploy` runs (Path A: success with URL; Path B: structured failure reason from `checkNetlify`).
- **Step 9:** If Path A, load deployed URL and visually compare to local preview.

**Pass condition for Gate 1:** Steps 1-9 all pass according to the smoke suite's per-step rules.

### Gate 2 — JJ B&A rebuild + visual distinctiveness

- **Sub-condition (a) — Rebuild button works:** Click the **Rebuild** button on `site-jj-ba-transport` (or run `Rebuild the site` in chat). Expected: classifier no longer returns `new_site` due to the spec backfill; the rebuild path runs.
- **Sub-condition (b) — `applyLogoV` dedup holds on rebuild:** After the rebuild, `grep -c data-logo-v sites/site-jj-ba-transport/dist/index.html` should return `1`.
- **Sub-condition (c) — Visual distinctiveness:** Operator (Fritz) judgment call. The P0.2 sentinel `./scripts/smoke-distinct-palette site-jj-ba-transport site-tonys-barber-shop` will likely still exit 1 (both sites use the FAMtastic default primary `#00A79D`) — this is the structural cost of GAP-1 and is queued for P1.2, not blocking P0.

**Pass condition for Gate 2:** (a) rebuild runs without misclassifying, (b) dedup holds on the fresh rebuild, (c) operator marks distinctiveness PASS or DEFER (deferring to P1.2 is acceptable).

### Gate 4 — Restyle test

Send `restyle the home page with a darker moody palette` via chat. Expected:
- (a) Classifier returns `restyle` (not `new_site`)
- (b) `modeInstruction` switch reaches `case 'restyle'` (added in P0.3)
- (c) Claude produces a restyle-shaped edit (palette/font/motion changes; content preserved)

**Pass condition for Gate 4:** All three of (a), (b), (c). Step (c) requires operator visual confirmation.

---

## What can't be verified mechanically

The following depend on live API calls and browser interaction; this report makes no claim about them:

- The actual round-trip latency of a chat-driven content edit on a fresh post-P0.4 build
- Whether Claude produces a clean restyle output given the new `case 'restyle'` modeInstruction
- Whether the deploy button's chat-message dispatch produces the structured `cli_missing` reason from `runDeploy`'s preflight (logically should, since the classifier no longer hijacks to `new_site`, but worth verifying)
- Operator visual judgment on Gate 2 sub-condition (c)

---

## Adjacent issues logged this session (deferred)

Both surfaced during the original gate run; logged in `.wolf/buglog.json` per P0.4 scope:

- `bug-gh-enoent-post-build-2026-04-27` (medium) — `spawn gh ENOENT` post-build crashes Studio; respawn recovers but the post-build push is silently dropped. Candidate fix: `which gh` preflight.
- `bug-business-type-truncation-2026-04-27` (low) — `business_type` truncates leading-adjective business descriptions to a fragment ("A bold and"). Imagen-4 safety filter rejected the resulting prompt; Unsplash fallback recovered.

---

## Outcome

- **P0.4 hotfix:** mechanical preconditions verified. Built-site spec state is correct portfolio-wide; classifier will no longer fall through to `new_site` on post-build chat.
- **P0 exit gates 1, 2, 4:** **operator-pending.** Cannot be auto-passed from this session — UI round-trip required.
- **No further code changes** until operator runs the gate verification and reports results. If gates fail again, **stop and rescope** per V3-final standing rules; do not auto-fix.
