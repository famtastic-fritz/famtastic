## 2026-06-24 — Universal intelligence-route export + Codex/GLM routing correction
Added a universal control-plane export in `shay-shay/shay_cli/intelligence_control_plane.py` plus `shay intelligence control-plane export` and `show-universal`, so the canonical route doctrine can now be written once and inspected or consumed across runtimes instead of living only inside the live Shay CLI. Wired `shay-agent-os/components/swarm/brain_client.py` to read `~/.shay/control-plane/universal-intelligence-route.json`, and added auto-refresh so registry/task-family/worker-pool truth surfaces keep the shared export current as the control plane is queried. Corrected the builder doctrine after review: Codex is now modeled as `openai-codex-gpt-5.5`, both Codex `gpt-5.4` and `gpt-5.5` route IDs are blocked anywhere the anti-drain builder policy applies, and `glm-5.2` was restored as the lead `famtastic-by-the-numbers` v1 primary builder route instead of being wrongly treated as forbidden. Verified with `uv run pytest tests/test_intelligence_layer.py -q` (74 passed), live `shay intelligence control-plane export`, live `show-universal`, and a direct bridge read proving the exported implementation policy blocks Codex while the v1 worker pool prefers `glm-5.2`.

## 2026-06-24 — Shared swarm runtime logic truth surface
Added `obsidian/01-Shay-Platform/SHAY-SWARM-RUNTIME-LOGIC.md` as the canonical shared-session swarm-runtime note so the active Shay swarm no longer has to be reconstructed from scattered code, routing docs, and post-review architecture notes. The note grounds the current runtime against `shay-agent-os/components/swarm/` code paths and ties that shell to the routing doctrine in `shay-shay/docs/agent-template-routing-matrix-2026-06-24.md` plus the dispatch logic in `obsidian/01-Shay-Platform/CAPABILITY-ENGINE.md`. Verified with a passing `py_compile` syntax gate plus a live `test_swarm.py` probe where `message_bus`, `goal_loop`, `trust_mode`, `error_recovery`, and `full_orchestrator` passed, while the standalone `worker_pool` simple-tier qwen path still timed out once. Deferred: `swarm_graph.py` remains scaffold-grade, Redis was not available for the probe, and the benchmark/scorecard runner commands described in the routing docs are still next-wave work.

## 2026-06-24 — Shay live model-probe command + capability registry routing hooks
Added a new control-plane truth surface in `shay-shay` so model capability claims can be probed, persisted, and fed back into routing instead of living only as static research. `shay_cli/model_probe.py` now writes `~/.shay/runtime/model_capability_registry.json`, `shay_cli/main.py` exposes `shay intelligence control-plane probe-model` plus `probe-registry`, and `shay_cli/intelligence_control_plane.py` re-ranks preferred routes with probe-backed scoring where capability truth exists. Verified with `.venv/bin/pytest -q tests/shay_cli/test_model_probe.py tests/shay_cli/test_zai_glm52_capabilities.py` (6 passed) plus live CLI checks for `probe-model`, `probe-registry`, and `intelligence control-plane explain`. Deferred: non-API-key/OAuth-style lanes still degrade to truthful `skipped` probe states, and the scorer is still an initial hook rather than a full dynamic worker-pool scheduler.

## 2026-06-24 — FAMtastic By the Numbers premium architecture + proof mode
Upgraded `apps/famtastic-by-the-numbers` from a static free-chart prototype into a small full-stack app with `server.js`, `lib/config.js`, `lib/paypal.js`, `lib/db.js`, `.env.example`, and `scripts/migrate-db.js`, so one-time premium unlocks now have a real API/persistence shape instead of being just UX promises. Added editable/themeable runtime product surfaces (price, CTA copy, support email, premium headlines), durable unlock state, email restore flow, local JSON proof-mode fallback when MySQL/PayPal env is absent, and deeper compatibility logic that goes beyond Life Path-only comparison. Verified with live `curl` checks against `/api/health` and `/api/config` on port `4174`, plus Playwright smoke proof covering free chart -> premium unlock -> reload persistence -> restore purchase. Deferred: browser-side PayPal approval UI is still the next production pass, and live capture in production still depends on real PayPal credentials plus a reachable MySQL database.

## 2026-06-24 — FAMtastic By the Numbers first working web app
Built `apps/famtastic-by-the-numbers` as a web-first numerology recognition system with a transparent Pythagorean V1 engine, bold landing page, core chart generation, visible math breakdown, lightweight compatibility read, daily guidance, and local save/share behavior. Added the supporting Wave 1 artifacts under `obsidian/01-Shay-Platform/famtastic-by-the-numbers-wave1/` plus `apps/famtastic-by-the-numbers/GAPS.md` so the lineage, schema, trust posture, and honest missing pieces are all on disk. Verified with `node --check`, local browser loading through `python3 -m http.server 4173`, and Playwright smoke test `node /Users/famtasticfritz/famtastic/apps/famtastic-by-the-numbers/tests/smoke.mjs`. Deferred: no payment rail, no cloud persistence, shallow compatibility logic, no Chaldean mode yet, and the research swarm itself exposed routing/tooling gaps that were logged instead of hidden.

## 2026-06-24 — Builder lane routing preference shifted toward Claude Code
Patched the Shay intelligence control-plane so `implementation-worker` now prefers the new `anthropic-claude-code-sonnet-4.6` route before Codex or GPT-5.4 mini, reducing the current Codex-heavy bias for builder asks. Also removed hardcoded chosen-route IDs from `explain_route()` so route explanations now follow each template's own `preferred_routes` order. Verified with `.venv/bin/python -m pytest tests/test_intelligence_layer.py -q` (61 passed) and live trace checks showing `build this app` now routes through the Claude Code builder lane while reviewer and attention lanes stay unchanged. Deferred: this is a routing-truth improvement, not a runtime worker-pool rewrite or HyperSwarm engine redesign.

## 2026-06-24 — Shay trace paraphrase routing + captain/worker split
Expanded `shay intelligence trace <ask>` so common paraphrases for build, attention, reviewer, and resume asks now map to dedicated intents instead of falling back to generic orchestration. The trace output now explicitly separates `brain agent` from `execution agent`, keeping Shay as the captain/router while worker lanes stay downstream execution surfaces, and reviewer/resume routes now expose swarm proof state even when the command list is not literally named `swarm`. Verified with live trace runs for the repaired asks, `python3 -m py_compile`, and a direct import/assertion harness; deferred: `pytest` is not installed in the current shell environment, so this slice was not proven through the normal repo test runner.

## 2026-06-23 — Shay ask-trace CLI + HyperSwarm truth alignment
Added `shay intelligence trace <ask>` in `shay-shay` so fuzzy asks can now be traced into normalized intent, routed task, command chain, and proof surfaces before execution. Expanded `docs/ask_intent_registry.md`, added trace coverage for attention/build/GitHub-to-Obsidian/context-compression asks, and updated CLI help/examples. Also aligned the HyperSwarm truth surface so internal lanes are represented as enabled-with-controls instead of falsely hard-blocked, and repaired the live USER.md identity drift so trace runs stop throwing false authority warnings. Deferred: natural-language coverage is still thin for competitor research, chained `research -> build prototype`, and pre-build swarm preview asks; those still require procedural commands or future intent wiring.

## 2026-06-22 — Hosting diagnostics panel + assisted domain fallback
Added a backend diagnostics panel to `site-famtastic-hosting` so admin settings now surface reseller auth health, live domain-search capability, function-parity status, branding/support visibility, and a copy-ready GoDaddy escalation packet instead of leaving reseller failures invisible. Public domain search no longer returns a hard backend failure on reseller authorization problems like `ACCESS_DENIED`; `/api/godaddy/available` now responds with structured assisted-mode payloads and the domains page keeps the lead in an assisted-contact lane. Verified with `npm run build`, a live dev curl showing assisted-mode JSON on the public lookup endpoint, and a `401` on `/api/admin/diagnostics` without admin auth proving the escalation surface stayed protected. Deferred: upstream GoDaddy reseller permission or endpoint-class correction still has to happen before self-serve live availability can be trusted again.

## 2026-06-22 — Conversation freeze capture lane
Added a native-ish freeze layer under conversation export so bounded conversation lanes can freeze a canonical source artifact before packaging it. `scripts/conversation-freeze.js` now writes `obsidian/05-Captures/freezes/<date>/<freeze-id>/freeze-manifest.json` plus `transcript.md`/`transcript.json`, registers `obsidian/05-Captures/index/freezes.jsonl`, and `scripts/conversation-export.js` can now consume `--freeze-manifest` to preserve frozen anchors and lineage fields inside the export package. Verified with a live freeze at `obsidian/05-Captures/freezes/2026-06-22/frz_20260622_71490bfe/`, a freeze-backed export at `obsidian/05-Captures/exports/2026-06-22/exp_20260622_8fcb50fb/`, and downstream consumer proof in `obsidian/05-Captures/review/convo-freeze-proof-2026-06-22.md`. Deferred: anchors are still derived from normalized saved-artifact blocks rather than native provider message IDs, and there is still no dedicated replay/query CLI over the freeze/export indexes.

## 2026-06-19 — Truth-surface drift doctrine and lane preflight
Promoted the branch/worktree recovery lesson into canonical startup doctrine so it stops living as an isolated session win. `docs/agent-startup/AGENT-STARTUP-CONTRACT.md` now names truth-surface drift and cross-lane contamination as first-class failure modes, requires a repo/branch/worktree/intended-lane preflight before meaningful writes, and makes unattended cron/launchd/autonomous runs bind to an explicit workdir and abort on lane mismatch. Mirrored the startup summary into `AGENTS.md` and `CLAUDE.md` so every major agent surface gets the same anti-drift framing on entry. Deferred: this is doctrine and startup enforcement guidance today, not yet a dedicated CLI preflight command that blocks writes automatically.

## 2026-06-18 — Shay prompt-memory target tightened under 2k
Lowered the live Shay built-in memory caps in `~/.shay/config.yaml` from 4000/4000 to 1000/900 and compacted `~/.shay/memories/MEMORY.md` plus `USER.md` to fit the intended tiny always-loaded layer. Updated `shay-shay/docs/memory-flow-audit-2026-06-18.md`, `shay-shay/docs/shay-memory-hierarchy.md`, and `obsidian/01-Shay-Platform/Agent-Capability-Matrix.md` so the truth surfaces now say the same thing: prompt memory is a thin pointer layer under roughly 2k combined, while bulky detail belongs in off-prompt spillover ledgers. Deferred: any already-running sessions still carry the boot-time snapshot until a fresh session reloads the new caps and compacted files.

## 2026-06-18 — Scram-line brief default
Changed the default human-facing planning surface from the verbose simple brief into the ultra-brief scram-line format. `plans/templates/simple-brief-template-v1.md` now leads with `Title`, `Purpose`, `Goal`, checkbox `Tasks`, `Status`, `Started`, `Ended`, `Execution`, `Research`, `Review`, `Skills`, optional `Blocked By`, and `Proof`, with exact spacing rules for resumability. Mirrored the rule into `docs/agent-startup/AGENT-STARTUP-CONTRACT.md`, `AGENTS.md`, and `CLAUDE.md` so this becomes common knowledge across sessions. Deferred: `fam-hub` still lacks a first-class scram-line brief command and there is still no dedicated validator for this shape.

## 2026-06-17 — Simple brief template v1
Added `plans/templates/simple-brief-template-v1.md` as the lightweight human brief surface for work that needs more orchestration truth than a bare task list but less machinery than the full `plan-template.json` packet. The template keeps execution readable by mapping each task to its owning agent/model under `Assignments`, points research/review to artifact files, and uses plan IDs for dependencies. Deferred: this is versioned as `v1` and documented, but it is not yet wired into `fam-hub` or a validator-driven brief CLI.

## 2026-06-17 — Shay Desktop/Desktop shortcut/Workspace runtime cutover
Cut Shay Desktop over so both `/Applications/Shay Desktop.app` and `/Users/famtasticfritz/Desktop/Shay Desktop.app` launch against `~/.shay` through the live `shay` runtime and expose the desktop-hosted dashboard on `http://127.0.0.1:9120`. Rewired Shay Workspace to the same runtime by pointing it at the shared gateway (`http://127.0.0.1:8642/health`) and the Desktop-hosted dashboard with token-aware health checks, then verified it boots past onboarding into the main shell. Added `shay-shay/docs/shay-surface-map-2026-06-17.md` plus doc updates in `web/README.md` and `website/docs/user-guide/features/web-dashboard.md` so the optional standalone `shay dashboard` surface at `9119` is no longer confused with the primary desktop-hosted app surface. Deferred: visible Hermes branding still exists inside the desktop/workspace UI, and the Workspace bundle patch should be upstreamed into its source if that app will be rebuilt later.

## 2026-06-17 — Plan autonomy doctrine + resumable task-state rule
Extended the new plan template from a passive packet shape into an execution-policy surface. `plans/plan-template.json` now requires an `execution_policy` block that says plans should run to completion without Fritz intervention by default, allows multi-swarm/parallel orchestrated swarms when dependencies permit, and requires task-list updates on every completion/block/state change so interrupted work is resumable. Mirrored the doctrine into `scripts/plans/validate-template.js`, `docs/agent-startup/AGENT-STARTUP-CONTRACT.md`, `AGENTS.md`, and `CLAUDE.md`. Deferred: this is now enforced at the template/validator/doctrine layer, but the existing task ledger still relies on agents actually writing state changes promptly rather than an automatic background reconciler.

## 2026-06-17 — Plan template enforcement + studio-filtered task listing
Added a canonical plan-shape surface so the work-packet rule is no longer just prose. `plans/plan-template.json` now defines the expected `classification`, `purpose`, `why_now`, `success_signal`, and `work_packet` fields; `scripts/plans/validate-template.js` enforces that shape; and `scripts/fam-hub` now exposes `fam-hub plan template` and `fam-hub plan validate`. Also replaced the old inline `fam-hub task list` renderer with `scripts/plans/task-list.js`, so task listing now supports `--studio`, `--stream`, `--status`, `--runner`, `--compact`, and `--json`, which makes prompts like “show me all the Shay tasks” map cleanly to `fam-hub task list --studio shay`. Verified with live runs of `fam-hub plan template`, `fam-hub plan list --status active --compact`, `fam-hub task list --studio shay --compact`, and `fam-hub plan validate`, which now honestly reports the legacy plan files that still fail the new template.

## 2026-06-17 — Obsidian memory boundary cleanup + deterministic Shay mirrors
Trimmed the recurring git dirt around `obsidian/Shay-Memory/` by drawing a hard line between canonical shared docs and runtime exhaust. `shay-agent-os/sync_lessons_to_shay.py` now writes deterministic mirror files and skips rewrites when content is unchanged, so repo-doc and lesson mirrors stop dirtying the tree with sync timestamps alone. Added `.gitignore` rules for high-churn vault paths (`lessons-mirror/`, `reflections/`, `builds/`, `inbox/`, `scratch/`) and prepared the repo for those paths to live as local memory surfaces instead of shared source history. Deferred: any already-tracked files under those ignored paths still need a one-time index cleanup (`git rm --cached`) to finish the boundary reset.

## 2026-06-17 — Command Center cha-ching meter + FAMU cruise boat clock
Added a new focus surface to Command Center so the daily brief and dashboard now punch Fritz in the face with the two things he said to keep front-and-center: money through Shay-Shay and the FAMU Alumni Cruise countdown. `scripts/command-center/build-command-center.js` now reads `command-center/data/shay-focus.json`, merges landed-income totals from `command-center/collectors/income-ledger.js`, and emits a `shay_focus` block into `command-center/state.json` plus a creative `💸 Cha-Ching + Boat Clock` section in `briefing.md` and `index.html`. Verified with a live rebuild of the Command Center artifacts. Deferred: Shay-attributed money is still a manual meter rather than an append-only ledger with a helper command.

## 2026-06-17 — Shay capability promotion rules and normalized evidence
Finished the capability-truth follow-through in `shay-shay` by normalizing process-intelligence validation evidence and turning capability promotion from a blanket manual-warning path into explicit verifier-aware evaluation rules. Capability overlays now report verifier-backed successes, supporting run IDs, suggested reality class, and promotion policy, while the intelligence truth docs now say the honest thing: promotion recommendations are automatic, but final curated status flips are still review-gated on purpose. Verified with `uv run pytest tests/test_capabilities_cmd.py tests/agent/test_process_intelligence.py tests/test_intelligence_layer.py` (73 passed). Deferred: curated matrix mutation and closeout write-back are still intentionally manual/reviewed rather than auto-applied.

## 2026-06-17 — Agent capability matrix full normalization pass
Finished the remaining rewrite of `obsidian/01-Shay-Platform/Agent-Capability-Matrix.md` so the note now uses one proof grammar all the way through instead of mixing reality-class sections with old shorthand tables. Converted the remaining UI, memory, plugin, skill, runtime-control, and local-infra sections to carry explicit reality classes and proof surfaces. Deferred: some rows still rely on prior verification output or documented truth surfaces rather than fresh live rechecks, and the matrix is still curated rather than auto-generated.

## 2026-06-17 — Agent capability matrix reality-class second pass
Rewrote the most misleading sections of `obsidian/01-Shay-Platform/Agent-Capability-Matrix.md` so the note now separates inventory status from truth class instead of pretending every row means the same thing. Added explicit reality classes (`live_verified`, `documented_present`, `curated_partial`, `seeded_target`) plus named proof surfaces for Core Agents, Communication Channels, Revenue Tools, and the uncensored-LLM section. Deferred: the matrix is still only partially converted, and the remaining sections still need the same proof-discipline rewrite before the whole note can be treated as uniform.

## 2026-06-17 — Shay capability matrix backfill and proof-surface pass
Backfilled `obsidian/01-Shay-Platform/Agent-Capability-Matrix.md` with the live memory/retrieval surfaces, runtime control surfaces, shared Claude/Studio skills, Shay Agent OS local skills, and local model/Redis support layer. Verified the runtime rows against `shay mcp list`, `shay cron list`, `shay kanban boards`, `shay profile list`, `skills_list`, `docs/capability-registry.md`, and `shay-agent-os/AGENTS.md` instead of treating the matrix itself as proof. Normalized the matrix permalink to `famtastic/01-shay-platform/agent-capability-matrix`. Deferred: the matrix is still curated/manual rather than auto-reconciled.

## 2026-06-17 — Shay capability observation overlay and guarded reconciliation
Added a low-risk capability-truth reconciliation slice in `shay-shay` by reading recent process-intelligence ledger entries and surfacing matched validation proof as `observation_overlay` data on capability records instead of leaving capability truth purely curated. Updated capability CLI output to show observed-proof counts and suggested reality class, updated the intelligence truth registry to include `runs/runs.jsonl` as a dependency, and refreshed the master checklist so it no longer claims the reconciliation slice is missing. Verified with `./.venv/bin/python -m pytest -q tests/test_capabilities_cmd.py -o addopts=''` (21 passed). Deferred: capability promotion rules are still manual/verifier-backed, and process-intelligence normalization is still incomplete.

## 2026-06-17 — Plan classification and filterable plan list
Added explicit classification metadata to the active-plan control plane so every active plan can now be filtered by `studio`, `plan_type`, and `stream` instead of guessed from titles. Updated `plans/registry.json`, patched all active `plans/<id>/plan.json` files with mirrored `classification` blocks, added `scripts/plans/list.js`, and routed `fam-hub plan list` through it so filters like `--studio shay`, `--studio site-studio`, `--plan-type summary`, and `--stream income` now work on the canonical active set. Verified with filtered CLI runs, regenerated startup context with `node scripts/agent-context-emit.js`, and re-ran `node scripts/plans/audit.js` clean. Deferred: classification is still single-owner only, and Command Center carries the fields in `state.json` before the UI exposes first-class chips/filters.

## 2026-06-16 — Active-plan drift backfill and closeout repair
Closed the follow-on drift from the registry-sync pass by backfilling 27 open task-ledger rows for the six newly visible active plans and writing `needs_tasking` closeout packets under each plan's `closeouts/` directory. Verified with `node scripts/plans/audit.js`, which now reports zero drift, zero conflicts, zero missing active plan files, and zero orphan tasks. Deferred: older active plans still rely on prior checkpoint/needs-tasking packets while carrying zero currently open ledger rows, so the next schema-hardening slice should distinguish “between task waves” from “actively task-backed.”

## 2026-06-16 — Active-plan registry sync and omission audit
Fixed a truth-surface bug where active `plans/<id>/plan.json` artifacts could exist without surfacing in `plans/registry.json`, which meant active plans created in another lane/branch could disappear from the canonical active-plan list. Synced the registry to include the omitted active plan-file set, regenerated the agent startup context, and hardened `scripts/plans/audit.js` so it now reports `Missing active plan files` when a plan file says `status: active` but the registry omits it. Deferred: six now-visible active plans still need either real task-ledger rows or closeout packets, because visibility was repaired before drift discipline.

## 2026-06-16 — Work-packet doctrine for active sessions
Added a live work-packet rule to `docs/agent-startup/AGENT-STARTUP-CONTRACT.md`, `AGENTS.md`, and `CLAUDE.md` so multi-step efforts stop living as loose conversation and must be restated with goal, tasks, branch, worktree, main-landing expectation, truth-surface updates, and proof. Updated `docs/famtastic-designs/vendor-capability-matrix.md` so the capability truth surface reflects the new packet shape and the fact that in-flight sessions need explicit reorientation if they were opened before the rule. Deferred: the plan/task ledger itself still does not store branch/worktree as first-class schema fields, so this is doctrine-first rather than backend-enforced.

## 2026-06-15 — Capability preflight/closeout enforcement slice
Added the first executable capability-truth gate to `shay-shay` with `shay capabilities preflight "<task>"` and `shay capabilities closeout "<task>"`, turning the capability layer from descriptive inventory into a real readiness/closeout control surface. Added focused tests for blocked-vs-pass exit codes, missing-prerequisite failures, and proof-surface requirements, and verified the slice with `.venv/bin/python -m pytest -q tests/test_capabilities_cmd.py -o addopts=''` (19 passed). Wrote the durable control artifact `shay-shay/docs/shay-master-checklist-2026-06-15.md` plus truth-surface doc updates so the broader reconstruction work is resumable. Deferred: matrix write-back/reality-class promotion is still manual/curated, not yet auto-reconciled from observed proof.

## 2026-06-16 — Shay intelligence truth registry + reality classes
Added a subsystem truth registry to `shay-shay` so the intelligence layer can distinguish proven-live surfaces from implemented-but-unverified and seeded ones instead of flattening them into one fake readiness bucket. Added `shay intelligence truth`, wired truth counts into `shay intelligence status`, and covered the slice with focused tests in `tests/test_intelligence_layer.py`. Verified with the targeted pytest file plus live `intelligence truth` and `intelligence status` CLI runs. Deferred: the registry is still curated code, and the event spine schema is not yet normalized into observation/interpretation/pattern/result records.

## 2026-06-16 — Shay research capture hardening
Added a durable-research capture rule so meaningful research now has to leave a structured artifact instead of dying as chat or terminal residue. Built `shay-shay/scripts/research_capture.py`, a protocol doc, and a reusable skill that write markdown notes plus an append-only JSONL ledger with separated observations, interpretations, source trace, and resume context. Verified the helper with `py_compile`, CLI help, and a live artifact write into `obsidian/Shay-Memory/research/`. Deferred: enforcement is still doctrine/helper-driven rather than a hard runtime gate.

## 2026-06-16 — Proactive brief decay + private review actions

Tightened the proactive morning-brief ranking so older transcript packets decay harder and repeated reseller-history items stop crowding the ranked focus list. Added explicit private-review action state through `obsidian/Shay-Memory/_system/private_review_actions.py` plus `private-review-actions.json`, and wired `reflect.py` / Command Center to display that state and support approve/suppress/review decisions. Verified with `py_compile`, a live `reflect.py` run, `private_review_actions.py list`, and a full Command Center rebuild. Deferred: Command Center still shows the action path rather than clickable in-page buttons, and ranking remains heuristic rather than final.


Added `shay-shay/scripts/model_eval_runner.py` plus a working local evaluation flow to compare hosted and Ollama-backed model lanes quickly. Verified live persona/soul files still exist, and confirmed the runtime is still in `service_tier: fast` with `reasoning_effort: low`. Hosted `gpt-5.4` passed the safe benchmark prompts cleanly; best local quick-pass candidates were `hermes3-64k`, `qwen3:14b`, and `phi4-mini-64k`, while experimental uncensored models stayed sandbox-grade. Discovered two real gaps: some Ollama models return empty stdout through `shay -z` despite answering via direct Ollama API, and child `delegate_task` swarm telemetry was not trustworthy for this benchmark class.

## 2026-06-16 — Identity guard CLI and immutable snapshot repair

Added Shay identity controls in `shay-shay`: `shay identity status/snapshot/restore/lock/unlock`, plus a macOS-safe immutable backup refresh path so startup checks no longer die on locked emergency files. Verified live with `shay identity status --json` and focused tests via `uvx ... pytest` (9 passed). Fast mode is still on in `~/.shay/config.yaml` (`service_tier: fast`) and reasoning is still low (`reasoning_effort: low`). Deferred: config UI still summarizes reasoning as plain `on`, and restore is emergency-backup-only for now.

## 2026-05-30 — Anti-truncation synthesis + .ralph autonomous build loop

Added `synthesize_sections()` to the swarm pipeline: generates each required plan section in its own brain call then assembles, so multi-section plans never hit the token ceiling. Verified — the 8-section ULTIMATE plan went from 6,534 chars (truncated, gate-failed 3x) to 25,846 chars (complete, gate-passed first try). Wired into `planning_loop` for any 5+ section plan. Built `.ralph/` autonomous build infrastructure in shay-desktop-electron: `prd.json` (20 gated build stories from the ULTIMATE plan), `loop.py` (deterministic driver that runs build_app per unit — typecheck-gated, auto-rollback, git-commits each passing unit, marks blockers and continues), `guardrails.md`. The loop runs on Shay's pipeline so it survives context across a multi-hour run; dry-run verified (next unit = U1 router scaffold). Honest limit: per-screen render-test auto-generation not built, so units run typecheck-gated and render-needing screens get flagged 'blocked' for assist rather than shipped unverified.

## 2026-05-30 — Lessons bridged into Shay's brain + memory-location gap audit closed

Discovered Shay's CHAT BRAIN read none of the session's lessons — they lived in Claude Code's `.wolf/cerebrum.md`/`buglog.json` and repo-root docs, outside the vault her gateway indexes. Fixed both channels: (1) appended a durable BUILD LESSONS block to `~/.shay/memories/MEMORY.md` (injected into her system prompt every session) covering the 6 hard-won rules — fix-harness-not-brain, no-score-chasing, surgical-edit-not-regen, two-gate UI builds, all-vendor-brains-cap, and the new swarm primitives. (2) Built `shay-agent-os/sync_lessons_to_shay.py` that mirrors `.wolf/cerebrum.md`, `buglog.json`→md, and the four repo-root knowledge docs (FAMTASTIC-STATE, CHANGELOG, SITE-LEARNINGS, famtastic-dna) into `obsidian/Shay-Memory/{lessons-mirror,repo-docs}/` which basic-memory + vault-search index. Installed a launchd job (`com.famtastic.shay-lessons-sync`, every 30 min) so the mirror never drifts. A parallel audit found 5 memory-location gaps (worst: SOUL.md tells Shay to prioritize repo docs she physically can't open); GAPs 1-4 closed by the sync, GAP 5 (phone asks/jobs queue) left as low-priority operational state.

## 2026-05-30 — Phase 2+3a complete: Dispatcher protocol + pipeline primitives + Gate 3a PASS

Phase 2 and Phase 3a built and gated in one session. New files in `shay-agent-os/components/swarm/`: `dispatcher.py` (Dispatcher ABC: `fan_out()`, `export_checkpoint()`, `import_checkpoint()`, `health()`); `local_swarm_dispatcher.py` (wraps Phase 1 WorkerPool + BrainChain, routes by brain/tier, JSONL checkpoint); `asyncio_dispatcher.py` (interface-complete stub, promotes to async fan-out on degradation trigger); `pipeline.py` (Phase 3a primitives: `agent()`, `parallel()`, `pipeline()`, `adversarial_verify()`, `judge_panel()`, `loop_until_dry()`, `completeness_critic()`, `run_job()`, `load_policy()`). Three admin-tunable policy YAMLs in `policies/`: `balanced.yaml` (local workers + Claude judge/synth — default), `free-maximal.yaml` (all-Ollama, adversarial verify disabled because reviewer=author), `speed-first.yaml` (all-cloud Claude, gated on brain availability). Gate 3a PASS: `agent()` ✅, `parallel()` 2/2 in 2s ✅, `run_job()` 3-phase job completed with 2,933-char synthesis ✅. Lesson recorded: adversarial_verify in job runner needs an explicit score threshold — without it, weak-model skeptics unanimously refute valid outputs (the D-6 "weak-reviews-weak" problem). Fixed: `adversarial_verify_score` key required to enable it in quality_gate. Three Obsidian notes written simultaneously: `learnings/phase1-lessons-2026-05-30.md`, `learnings/process-improvements-2026-05-30.md`, `learnings/skill-recommendations-2026-05-30.md`. Phase 3b (meta-test: "Shay, reproduce the doc arc autonomously") is the final gate before the desktop build target.

## 2026-05-30 — Phase 1 complete: Shay's swarm engine fixed and benchmark-verified

Phase 1 of the self-orchestration plan (v3.1) shipped. Three new/rewritten files in `shay-agent-os/components/swarm/`: (1) `brain_client.py` — a stdlib-only `BrainChain` (Claude→OpenRouter→Gemini→Ollama) with `BrainAvailabilityCheck`; reads keys directly from `~/.shay/.env`, no cross-package imports, confirmed all 4 brains available. (2) `goal_loop.py` — full rewrite: `_decompose()` and `_judge()` now call `BrainChain` directly with `judge_model='claude'` instead of routing to hermes3 via the dead-code `MODEL_MAP`; `synthesize()` reduce step added (the entirely missing step that caused `final_result_chars=0`); `_anchor_check()` added with keyword blocklist + goal-word overlap filter to prevent topic drift (caught 5 off-topic sub-goals in the benchmark); budget exhaustion now triggers synthesis instead of discarding work; simple/qwen2.5:1.5b tier dropped, medium is the floor. (3) `__init__.py` updated to export `BrainChain`/`BrainAvailabilityCheck`. Benchmark gate (same task that failed 2026-05-30 with `final_result_chars=0` and neuroscience drift): **all 6 D-5 quality assertions PASS** — synthesis present (5,914 chars), no hallucination sentinels, covers all 3 orchestration patterns, has a recommendation. Phase 1 is the prerequisite for Phase 2 (Dispatcher protocol) and Phase 3 (programmable pipeline / "Shay, build me the desktop app"). Buglog entry #212.

## 2026-05-30 — Shay Phone v0 shipped: installable PWA + LAN proxy (first brick of the companion app)

Built `~/famtastic/shay-phone/` — a dependency-free (Python stdlib) proxy + installable PWA so Fritz can run "Shay" from his phone tonight over the home WiFi, per his "I need this on my phone tonight" directive and the de-blocked-store decision. `server.py` binds `0.0.0.0:8787` with shared-token auth and serves three endpoints: `/api/chat` (brain chain: direct Anthropic API → OpenRouter → Gemini, with the Shay-Memory vault used as local context via a dependency-free term-scoring search), `/api/capture` (writes note/idea/diary markdown into `obsidian/Shay-Memory/inbox/`), and `/api/recent`. The PWA (`web/index.html` + manifest + service worker + Pillow-generated icons) installs to the iOS home screen via Add-to-Home-Screen. **Brain reality, again:** tonight every Claude path is capped — Codex bridge (capped), OpenRouter key (403 "Key limit exceeded"), and there is no standalone `sk-ant-api` key on the box (only the weekly-capped `sk-ant-oat` OAuth token, deliberately not used) — so chat runs on **Gemini** tonight and auto-flips to Claude the moment a real `ANTHROPIC_API_KEY=sk-ant-api…` is added to `~/.shay/.env`. Verified functionally end-to-end (auth 403 on missing token, static 200, capture wrote a real vault file, chat returned a live reply); macOS firewall is off so the phone reaches the Mac. Known limits (v0): LAN-only (no cellular/tunnel yet), no mid-run-approval/durability (that's Milestone Zero), and the server is a nohup process (no launchd yet — reboot stops it). **Follow-up same session:** Fritz confirmed the credit diagnosis was correct (he was genuinely out — bought 40 Codex credits) and asked for a multi-brain phone with switching + fallback. Added a brain registry + UI picker: `Auto` (fallback chain Claude-direct → Codex CLI → OpenRouter → Gemini) or pin one. `call_codex_cli()` shells `codex exec -s read-only --skip-git-repo-check --ephemeral -o <file> -` (answers only, no file edits) using the now-funded subscription; `call_anthropic()` uses a real `sk-ant-api…` key when present (never the `sk-ant-oat` OAuth token); new `/api/brains` reports per-brain availability. Verified: Codex CLI answered through the phone in ~22s, `/api/brains` correct, switching works. Bumped the service-worker cache (`shay-v0-2`) so installed PWAs pull the new picker UI. **Second follow-up (same session):** Fritz confirmed via OpenRouter dashboard he was NOT out of account credits — the 403 was a per-API-key spend limit (a settings issue), and flagged high OpenRouter token usage (Kimi K2.6 36.6M etc., not from the phone/this session) as a separate config-optimization item to investigate. He asked for the Claude-Code-style mid-run approval/question flow in the phone app. Built it: `ask_shay.py` bridge (`ask()`/`approve()`) lets any Shay job post a question and BLOCK until answered from the phone; proxy gained `/api/ask` (create), `/api/asks` (list pending), `/api/answer` (answer), `/api/ask?id=` (poll); the PWA gained a 🔔 Asks tab with a live badge, option buttons / free-text answer, and 5s polling. Verified end-to-end: a job called `approve("Deploy to production?")`, blocked, the pending ask surfaced via the API, answering "Approve" unblocked the job → `PROCEEDING`. Service-worker cache bumped to `shay-v0-3`. Known v0 limit: phone polls while the app is open (no APNs/Web-Push wake-up yet — that's the next follow-up alongside the tunnel + launchd durability of Milestone Zero). **Third follow-up (same session):** Fritz removed the OpenRouter per-key limit (Claude-via-OpenRouter now works through the phone — verified, `brain: Claude (OpenRouter)`) and obtained an Anthropic key (not yet placed in `~/.shay/.env`; direct path activates when added). Reordered the brain fallback chain to lean on Claude: Claude-direct → Claude-via-OpenRouter → Codex → Gemini. He also clarified "interview" means a structured multi-question review session (e.g. cron: "interview Fritz on the research from X", or "review this site"), not a single question. Built it: `create_interview()`/`answer_interview()` in the proxy, `/api/interview` (create) + `/api/answer` with `{id, answers[]}` (submit); `ask_shay.interview()` (blocking) + `queue_interview()` (fire-and-forget for cron); the PWA renders an interview as a multi-question card (title, context, review link, per-question options/text, Submit). Verified end-to-end: a cron-style job queued a 3-question pricing-research interview with context + URL, the phone saw it, submitting answers completed it, and the job unblocked with the answers array. Service-worker cache → `shay-v0-4`. A live demo interview ("Review: the Shay phone app so far") is queued for Fritz to complete on his phone.

## 2026-05-30 — Shay self-orchestration + companion-app master plan (adversarially reviewed) + Codex subscription cap discovered

Authored `obsidian/Shay-Memory/post-review/shay-self-orchestration-plan-2026-05-30.md` — a detailed, adversarially-reviewed plan to make Shay run the full J′ arc (decompose → fan-out → adversarial review → synthesize → write artifacts → record proof) autonomously, plus a full companion phone app (Claude+Codex app features as the floor, Tailscale/Cloudflare relay to Shay Desktop, answer build/approval prompts from the phone mid-run). Three phased orchestration capabilities: (1) fix the swarm by routing dispatch through Shay's gateway/subprocess brain layer + fixing the dead-code judge/decomposer in goal_loop.py + adding a real synthesize() reduce step + an embedding-based anti-drift anchor check; (2) a Dispatcher protocol with admin-tunable policies; (3) a programmable pipeline. **Major finding:** the $100 ChatGPT/Codex subscription is ALSO rate-capped — programmatic Codex calls failed with "You've hit your usage limit … try again Jun 2nd," exactly the vendor-cap failure mode that started this arc with Anthropic. This invalidated the plan's original "Codex = unlimited workhorse" premise mid-review; the brain policy was rebuilt around the only two always-available brains (Gemini Flash + local Ollama), with Codex as an auto-upgrade after its cap resets and Claude reserved as a metered final-gate. Adversarial review ran R1 (6 Gemini lenses, all revise-major, ~115k Gemini tokens, 0 Claude credits) + R2 (Gemini verification = ship; one focused Claude Sonnet pass on the quality-check framework = revise-major). Sonnet's crux finding — that the quality gates checked *form* not *correctness*, and that weak-model-reviews-weak-model is theater when Gemini/Ollama are the only free brains — drove a v2.1 hardening: deterministic tiered quality assertions, a claim-grounding gate, a reviewer-capability floor, and an explicit honest limit that certified correctness requires a strong-model spot-check. Fritz then expanded scope to **v3**: Shay must build her *own* companion app (the app becomes the Phase-3 capstone, Gate 3c), the app is his full-time phone interface (ideas→completion + Shay-generated interviews answered from the phone), Shay is note-taker/diary/pattern-finder (Part I), a first-class get-smarter learning loop (Part H: post-run reflection, auto-minted skills/agents, scheduled repo/skill ecosystem scans, longitudinal efficiency ledger), and **store approval is explicitly NOT a blocker** (personal install via PWA/sideload/TestFlight; Tailscale-app relay needs no NetworkExtension entitlement). A third Gemini review (R3) of the new scope returned revise-major with 4 majors (all fixed in v3.1): "drive Claude Code as a sub-agent" corrected to Claude/Codex-API-with-tool-use; the skill-minting + get-smarter gate de-confounded (identical task, N≥5, quality-constant); PWA Web-Push friction disclosed; and "Milestone Zero" named explicitly (relay + PWA + one captured idea, ~2–3 weeks after the Phase-1 gate) as the real near-term phone deliverable vs the months-away self-build capstone. Plan only this session (no build); ~37k Claude credits used total — R1/R2-Gemini/R3 all ran free on Gemini; Codex unavailable (capped till Jun 2).

## 2026-05-30 — Shay V2 + phone-app + swarm architecture shipped via adversarial workflow + head-to-head local-swarm benchmark

Workflow J′ (27 agents, 6 phases, 2.13M output tokens, 41 min wall-clock) delivered three canonical artifacts to `obsidian/Shay-Memory/post-review/`: `swarm-architecture-2026-05-30.md` (108k chars — 8-orchestrator comparative matrix with weighted scoring rubric, three reference policy YAMLs, pattern library, explicit `Dispatcher` protocol to avoid LangGraph lock-in, three load-bearing caveats walked back from V1 over-claims), `v2-build-plan-2026-05-30.md` (7.8k chars — lean delta-doc, 15 R2-driven corrections, honest phase-3 envelope 14→27 min, OpenRouter free-tier cap correction 1000→50 RPD), and `phone-app-plan-2026-05-30.md` (52k chars — V1→V2→V3 critique trail, FM-four-gate availability check, per-platform capability matrix). In parallel, ran `swarm-benchmark.py` against shay-agent-os SwarmOrchestrator on the **same task** (3 workers, hermes3 judge, 10-turn budget): hit `budget_exhausted` at 291.5s with 19 subgoals, `final_result_chars: 0` (no synthesis step exists in `GoalLoop`), and from sub-goal 13 onward the hermes3 judge **misread "brain coupling" as cognitive-neuroscience instead of LLM-provider coupling** and decomposed the remaining turns into working-memory-capacity questions. Comparison report at `post-review/comparison-report-2026-05-30.md` names four shay-swarm fixes (stronger judge model, anchor-check between turns, synthesis step, drop `simple` tier) as the minimum-viable upgrade to make local-swarm produce J′-quality output at near-zero cost. Positive finding: shay-agent-os MessageBus has an **in-memory Redis fallback** that works silently when the `redis` python module isn't installed, contradicting J′'s arch-doc claim that Redis is a hard dependency.

## 2026-05-29 — Shay "default to Gemini" permanently fixed: Claude Code weekly cap diagnosed + context bloat cut

Corrected the earlier "subscription depleted" diagnosis. Claude works in Desktop/web on the same account; Shay specifically hits the Claude Code rolling **weekly** usage cap (separate from chat). Burned by per-call context bloat (~55k input tokens/call, dominated by a 22.6k-token skills prompt injecting all 160 skills) and a secondary `model=default` 404 bug. Shipped three changes that hold permanently: (a) added `skills.max_count: 40` + `always_include` config and a `_apply_skill_count_cap` helper in `agent/prompt_builder.py` — verified skills block drops 22,600 → 1,274 tokens; (b) added `tools.include` allowlist on `mcp_servers.basic-memory` (27 → 7 useful tools, total roster 35 → 19); (c) fixed `agent/plugin_llm.py:586` to never send the literal string `"default"` as a model name. Net per-call savings ~24.5k tokens (~55k → ~30k). Existing 99% prompt-cache hit rate preserved. Plan at `~/.claude/plans/no-dude-this-is-zesty-minsky.md`, buglog #210.

## 2026-05-29 — Shay memory architecture rebuilt: cognee retired for basic-memory + Smart Connections

Replaced the finicky cognee vault-brain with an all-local split stack (autonomous /goal run). Diagnosed cognee as non-functional — it defaults to OpenAI embeddings (no key present) and needs flaky local LLM graph-extraction. Installed `basic-memory` (uv tool) as Shay's write+recall layer, scoped to `~/famtastic/obsidian/Shay-Memory` (deliberately NOT the whole vault, since its sync mutates frontmatter); it uses local FastEmbed `bge-small-en-v1.5` (no API key) and verified end-to-end — wrote a note, reindexed, semantic search returned it at score 0.65. Placed Smart Connections 4.5.0 + Visualizer 1.0.27 plugin files into the Obsidian vault and enabled them (human visual brain; needs one GUI step — open Obsidian, disable Restricted Mode, let it index). Retired cognee (commented out in `~/.shay/config.yaml`, preserved in backups); after gateway restart only `obsidian` + `basic-memory` (27 tools) load. Deferred: `smart-connections-mcp` (agent reuse of SC embeddings) — its third-party npm build was blocked by the security classifier and is redundant given basic-memory's own search; needs explicit build approval if wanted.

## 2026-05-29 — Shay brain "keeps switching": subscription drain diagnosed + bulk-routing leak closed

Diagnosed why Shay's interactive brain kept falling back to Gemini: the Claude Max subscription (OAuth via `CLAUDE_CODE_OAUTH_TOKEN` — the only Anthropic credential present; no `ANTHROPIC_API_KEY` exists) was depleted, returning HTTP 400 "out of extra usage" on every call. Confirmed it never switched to API billing — same token worked yesterday with 200s, zero 401s today. Proximate cause: switching the default to Opus 4 plus overnight batch/swarm work all running on the Max brain. Closed the bulk-routing leak: every bulk driver shelled out `shay -z` with no `--provider`/`--model`, inheriting the Claude default. Routed research/synthesis/orchestrator/cron jobs to Gemini (`overnight_ops.py`, `rerun-cited.py`, `mbsh-swarm-launch.py`, 3 agent cron jobs in `~/.shay/cron/jobs.json`), the raw swarm spawner (`shay-agent-os/launch-agent.py`) to local Ollama hermes3, and repointed cognee MCP off Claude. Hard-gated the `ANTHROPIC_API_KEY` fallback in `shay-shay/agent/anthropic_adapter.py` behind `SHAY_ALLOW_ANTHROPIC_API_KEY` so subscription→API billing can never happen silently. Deferred/unverified: cognee still needs a local embedding model + gateway restart to actually function (pre-existing gap); local-Ollama oneshot rendering not smoke-tested.

### 2026-05-29 - NCS Redesign Opportunity & Business Plan

**Context:** Following the successful `muapi url-to-design` generation for the National CAD Standard V7 site, demonstrating high-quality, rapid mockups.

**Decisions Made:**
*   **Business Model:** Productized service targeting standards orgs, associations, gov agencies, nonprofits.
*   **Pricing:** Tiered model (Mockup Pitch, Full Redesign & Build Package, Maintenance Retainer).
*   **Branding:** Standalone brand under the FAMtastic umbrella.
*   **Go-to-Market:** "Mockup-first, sell-the-vision" approach. Identify prospects, generate `muapi` mockups for their sites, lead with visual pitch in outreach, secure deposit, then build.

**Exact Next Steps:**

1.  **Fritz Action:** Provide GoDaddy domain inventory (domain, status, expiry) for evaluation against new business concept.
    *   **Owner:** Fritz
    *   **Deadline:** EOD Today (2026-05-29)
2.  **Claude Code Action:** Upon receiving domain inventory, identify suitable domains for the standalone redesign brand.
    *   **Owner:** Claude Code
    *   **Deadline:** Immediate upon receipt of #1.
3.  **Claude Code Action:** Research and compile a list of 5-10 initial prospects (standards orgs, associations, gov agencies, nonprofits with dated sites).
    *   **Owner:** Claude Code
    *   **Deadline:** EOD Tomorrow (2026-05-30)
4.  **Claude Code Action:** (Once #3 is complete) Generate `muapi` mockups for 2-3 top prospects' key pages.
    *   **Owner:** Claude Code
    *   **Deadline:** End of Week (2026-05-31)

**New Ideas Surfaced:**
*   `muapi` as an "AI-powered sales engineer" for pre-sales.
*   The potential for a "nominal fee" for the initial mockup to qualify leads.

**What to Build or Validate First:**
*   **Validate:** The effectiveness of the "mockup-first" cold outreach. This is the critical sales hypothesis.
*   **Build:** The internal workflow for taking an approved mockup from `muapi` to a full-fledged design system and code implementation. This will be needed post-sale.

---

## 2026-05-30 — Shay surgical-edit harness: existing-file routing + anchor fidelity
Closed the root cause behind ralph U1 repeatedly blocking on App.tsx (TS17008). `surgical_patch` was orphaned — `build_app` full-regenerated even existing files, dropping closing tags on the render-spine. Rewired `build_app` to route EXISTING files through `surgical_patch` (anchored edits, never full-regen) and NEW files through `multi_file_code_job`, with a render-spine heuristic forcing ≥2 small additive anchors. ADAPTED the hermes-agent fork's `fuzzy_find_and_replace` for whitespace-tolerant anchoring (research confirmed reuse over rebuild). Two further fixes were decisive: the edit-planning prompt truncated the file to 6000 chars (hiding App.tsx's routing block → brain hallucinated anchors) now raised to 24000; and the ralph loop now re-execs under Shay's 3.13 venv so the 3.10+ hermes primitives import. Shay then drove U1 herself in 22s (two clean micro-patches), passing typecheck AND full vite build. The build surfaced a separate pre-existing CSS bug (missing `}` in Soul.module.css) — fixed. Deferred: per-screen render-test autogen so the ralph loop is render-gated, not just typecheck-gated.

## 2026-05-30 — Autonomous desktop build run: 11/20 units landed, triple-gated
Turned the ralph loop loose on the full U2–U20 desktop queue after grounding each unit with the real `window.hermesAPI` surface (extracted live from index.d.ts) and broadening the render-smoke stub to the whole window namespace (hermesAPI/hermes/shay/shay*Rpc). The API grounding converted the U2-class blocker (brain inventing `getSessions`) into a clean 33s pass. Final: 11 of 20 units built, committed, and verified — cumulative typecheck:web + typecheck:node + production `npm run build` all green, working tree clean (9 blocked units fully rolled back with prior-art notes). Deferred: 9 blocked units split into two classes — anchor-miss on shared existing files (wider-context fix, known) and TS/brace errors in LARGER NEW files (new-file generation still full-gens; needs the same micro-decomposition the surgical path gives existing files).

## 2026-05-31 — All-night arc: desktop 20/20 + functional launch + monitoring + Phase 3A
Drove the adversarially-reviewed master plan autonomously. Phase 0 (scoped loop hygiene), 1A (deterministic brace-drop repair), 1B (re-anchor), recovered all 20 desktop units (5 mechanically, last 2 hand-wired). Phase 2 launch test found+fixed two latent runtime bugs (dead preload sandbox/node:fs bridge; window.hermesAPI undefined) — app now boots with a live IPC bridge. Built the monitoring spine (/api/arc feed + arc.html + Telegram + Web Push installable on Android), re-ran Shay's companion research with the correct clone-and-exceed mandate, and shipped Phase 3A (multi-provider tool registry). Phase 3 install/vault-mutation tracks (obsidian-wiki, SkillNet, memory spike, Kanban) deferred to a guarded greenlight (ARC-DECISIONS-2026-05-31.md). Recorded capability backlog (phone-side control v2, brief-reader near-term, agent identity+credentials v2/v3).

## 2026-05-31 — Shay Desktop overnight UI sprint + learning loop
Ran Shay's post-learning loop (8 lessons captured into her pipeline store, mirrored to Obsidian + .wolf/cerebrum) and minted 7 new skills under shay-agent-os/skills/ (render-spine-guard, visual-qa-gate, dead-wire-detector, typed-screen-manifest, settings-store-snapshot-cache, claude-desktop-style-match, micro-patch-living-file). Fixed a real bug in Shay Desktop: the Agents screen had 100% dead CSS (kebab classNames with an orphaned, never-imported module stylesheet) — added a global stylesheet and import. Styled SettingsShell field controls (checkboxes/inputs) and widened the status pill label. Rebuilt/repackaged/reinstalled v0.4.3; smoke gate passed (0 console errors), Chat scored 9/10 on the vision gate; the `onChatChunk` vision flag was verified a harness artifact, not a real bug. Punch-list (chat-tab truncation, settings sub-nav micro-clips, profile-avatar verify) deferred to the morning review.

## 2026-06-02 — Command Center Phase 1: real monitoring + income + de-noised ideas
Audited the sprawl of half-built "moving parts" (pipeline agents HUNG, fake `command-center/index.html`, ~900 lines of token-optimizer idea noise) and built the real keystone: a zero-dependency Node service (`command-center/`, launchd port 7878) that monitors live agent health via PID+heartbeat freshness, tracks only real landed income (Stripe/PayPal webhooks with HMAC sig verification + manual entry, append-only ledger), and surfaces curated dollar-bearing opportunities from `revenue-plans/` while collapsing 475 generator-noise lines. Added a kill-switch guardrail for the planned fully-autonomous-send agents, a Python heartbeat contract (`pipeline/lib/heartbeat.py` + agents-registry), and patched the hung scout (heartbeats + Reddit timeout). Verified live in-browser. Deferred (Phase 2/3/4): live payment creds + public tunnel, the actual lead-gen/customer-response/follow-up agents, and scheduled autonomous orchestration.

## 2026-06-02 — Jamari '26 graduation celebration site (standalone gift)
Built a 3-page responsive celebration + future-dev site for Jamari Anthony Jacquet (Monarch High, Class of 2026) under `sites/site-jamari-graduation/dist/`, with out-of-the-box Three.js 3D: an animated graduation cap + confetti hero (`index.html`) and a particle-network "Future Developer" scene (`journey.html`), plus a masonry photo gallery with lightbox (`gallery.html`). Optimized 18 graduation photos from 65MB → 6.6MB (Pillow, EXIF-baked, 1600px/q82), generated an OG image + favicon, and a parallel swarm wrote a full social-media campaign + blast at `dist/campaign/CAMPAIGN.md`. An independent QA reviewer pass caught and closed a year-counter bug, a dead parallax line, and a stale lightbox alt; head `<style>` blocks and all inline styles were extracted to `css/site.css` per project rules. Deployment is hands-off-ready (drag-and-drop or git-connect) via `netlify.toml` + `DEPLOY.md` — could not deploy from the sandbox (Netlify API firewalled, no token).
## 2026-06-16 — Proactive briefing ranking + private review surface

Tightened the proactive Shay loop by ranking a morning-brief subset inside `obsidian/Shay-Memory/_system/proactive_os.py`, adding `private-review.json` output from `reflect.py`, and wiring `scripts/command-center/build-command-center.js` to show a protected private-context review section alongside the public brief. Added verification coverage for ranking/private-review behaviors, cleaned generated Command Center artifacts to pass `git diff --check`, and regenerated the live reflection + dashboard outputs. Remaining gap: ranked focus still overweights some older reseller-history decision packets, and the private review surface is display-only until approve/suppress actions exist downstream.
