#!/usr/bin/env bash
set -euo pipefail

if [[ -z "${OPENAI_API_KEY:-}" ]]; then
  echo "[error] OPENAI_API_KEY not loaded"
  exit 1
fi

echo "[ok] Secrets loaded. OPENAI_API_KEY length=${#OPENAI_API_KEY}"