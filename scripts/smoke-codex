#!/usr/bin/env bash
set -euo pipefail
echo -n "Codex (code-free via LiteLLM) smoke… "
out="$(MODEL=code-free ./scripts/codex-cli <<<"print a one-line python hello world" 2>/dev/null || true)"
[ -n "$out" ] && echo "✅" || { echo "❌"; exit 1; }