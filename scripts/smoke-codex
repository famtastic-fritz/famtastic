#!/usr/bin/env bash
set -euo pipefail
echo -n "Codex (native) smoke… "
out="$(echo 'print a one-line python hello world' | ./scripts/codex-cli 2>/dev/null || true)"
[ -n "$out" ] && echo "✅" || { echo "❌ (install/verify codex CLI)"; exit 1; }