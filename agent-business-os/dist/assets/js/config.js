/* Runtime config for the Agent Business OS front-end.
   Edit these values to go live — no build step required. */
window.ABOS_CASHTAG       = '$FAMtasticFritz';  // Cash App cashtag — turns on "Pay with Cash App" buttons.
window.ABOS_LEAD_ENDPOINT = '';            // optional backend (Azure SWA '/api/lead'). Empty = Netlify Forms post + localStorage backup.
window.ABOS_BOOKING_URL   = '';            // e.g. 'https://cal.com/your-handle/strategy-call'. Empty = CTAs scroll to the form.
