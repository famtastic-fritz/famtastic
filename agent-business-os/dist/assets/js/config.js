/* Runtime config for the Agent Business OS front-end.
   Edit these two values to go live — no build step required. */
window.ABOS_LEAD_ENDPOINT = '/api/lead';   // lead-capture function (Azure SWA). Empty string = localStorage only.
window.ABOS_BOOKING_URL   = '';            // e.g. 'https://cal.com/your-handle/strategy-call'. Empty = CTAs scroll to the form.
